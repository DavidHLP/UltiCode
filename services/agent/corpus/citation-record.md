> Provenance: agent-authored synthetic example; not a real UltiCode submission, DTO, or user-authorized material.


Every retrieved chunk should carry its document ID, version, chunk ID, source path, source position, access scope, and sample/real marker. A citation is valid only when the quoted fragment exists in that recorded source; a conclusion beyond the fragment must be labeled as an assumption.
