# 证据不足时的正确行为

> Provenance: project-authored-for-u02。由本项目助手为 U02 学习切片撰写；不是用户作品，也不是授权第三方材料。

当检索没有命中可用资料时，正确行为是说明「没有材料支持给出答案」，而不是用相似但无关的片段硬答。
当题目要求的数据本身不在输入中时（例如只有状态、却问哪一行代码出错），正确行为是明确拒答或请求补充信息。
拒答与「没有引用」必须能被区分：没有引用不等于引用已核实，缺失的检查不得被当作通过。
