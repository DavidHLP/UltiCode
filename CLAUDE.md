# Claude Code compatibility entry

@AGENTS.md

Follow the imported root guide and the nearest nested `AGENTS.md` for every task in this repository. They are the single source of truth for project rules.

Treat `.claude/rules/` as optional Claude tooling hints only: they do not define project conventions and must be ignored where they duplicate or conflict with `AGENTS.md`. Do not duplicate project rules here.
