---
phase: "24"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified:
  - "ecosystem.config.cjs"
autonomous: true
requirements:
  - "INFRA-01"
  - "INFRA-02"
must_haves:
  truths:
    - "PM2 ecosystem.config.cjs uses dotenv npm package instead of custom fs/readFileSync .env parser"
    - "All existing env var fallbacks in app definitions remain unchanged"
    - "CLAUDE.md documents Maven build order (recommend-api mvn install before backend-spring)"
  artifacts:
    - path: "ecosystem.config.cjs"
      provides: "PM2 process manager configuration"
      contains: "require('dotenv').config()"
    - path: "CLAUDE.md"
      provides: "Backend Startup Issues section with Maven build order"
      contains: "recommendation && mvn install -DskipTests"
  key_links:
    - from: "ecosystem.config.cjs"
      to: "node_modules/dotenv"
      via: "require('dotenv').config() at top of file"
      pattern: "dotenv.*config"
---

<objective>
Replace the custom .env fs/readFileSync parser in ecosystem.config.cjs with the `dotenv` npm package (INFRA-01), and document Maven build order in CLAUDE.md (INFRA-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@ecosystem.config.cjs
@CLAUDE.md

## Key Context

**Current custom .env parser (lines 1-21 of ecosystem.config.cjs):**
```javascript
// Load .env for PM2 process environment (fallback without dotenv)
try {
  const fs = require('fs');
  const envPath = require('path').resolve('.env');
  if (fs.existsSync(envPath)) {
    const envContent = fs.readFileSync(envPath, 'utf8');
    envContent.split('\n').forEach(line => {
      const trimmed = line.trim();
      if (trimmed && !trimmed.startsWith('#')) {
        const eqIdx = trimmed.indexOf('=');
        if (eqIdx > 0) {
          const key = trimmed.slice(0, eqIdx).trim();
          const val = trimmed.slice(eqIdx + 1).trim().replace(/^["']|["']$/g, '');
          if (!(key in process.env)) process.env[key] = val;
        }
      }
    });
  }
} catch (e) {
  // env vars must be set externally
}
```

**Required replacement per D-01, D-02:**
- Add `require('dotenv').config()` at the top of ecosystem.config.cjs
- Keep all existing `env` definitions in each app block (fallbacks unchanged per D-03)
- Remove the entire custom .env parser block (lines 1-21)

**Maven build order (D-06, INFRA-02):**
- `recommendation` must be built with `mvn install -DskipTests` BEFORE `backend-spring`
- This is because `backend-spring/pom.xml` line 215 declares dependency on `recommend-api`
- Currently documented in CLAUDE.md lines 102 and 398-405, but should also be in "Backend Startup Issues" section

**Decisions from 24-CONTEXT.md:**
- D-01: Replace custom .env fs/readFileSync parser with `dotenv` npm package
- D-02: `dotenv` config should be called at top of ecosystem.config.cjs before module exports
- D-03: All existing env var fallbacks remain unchanged
- D-04: No runtime env var validation or schema enforcement needed
- D-05: Build order documented in CLAUDE.md (Backend Startup Issues section)
- D-06: Maven build order: `recommend-api` must be installed before `backend-spring`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dotenv npm package</name>
  <files>package.json</files>
  <action>
    Add `dotenv` as a dependency in package.json. Run `pnpm add dotenv` to install it.
    After installation, verify package.json contains `"dotenv": "^16.0.0"` (or current latest) in dependencies.
  </action>
  <read_first>
    package.json
  </read_first>
  <acceptance_criteria>
    - package.json contains `"dotenv": "^16.0.0"` in dependencies
    - node_modules/dotenv exists after install
  </acceptance_criteria>
  <verify>
    <automated>grep '"dotenv":' package.json && test -d node_modules/dotenv</automated>
  </verify>
  <done>dotenv installed and listed in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Replace custom .env parser with dotenv in ecosystem.config.cjs</name>
  <files>ecosystem.config.cjs</files>
  <action>
    Replace lines 1-21 (the entire custom .env parser block) with a single line at the very top of the file:
    ```javascript
    require('dotenv').config();
    ```
    Keep the rest of the file unchanged (apps array starting at line 23). The `dotenv` package automatically reads `.env` from the current working directory and loads variables into `process.env` if not already set, matching the original behavior. All existing `env` object fallbacks in each app block (NODE_ENV, SPRING_PROFILES_ACTIVE, NACOS_PORT, REDIS_PASSWORD, JWT_SECRET, etc.) must remain exactly as-is.
  </action>
  <read_first>
    ecosystem.config.cjs
  </read_first>
  <acceptance_criteria>
    - Line 1 of ecosystem.config.cjs is: `require('dotenv').config();`
    - No `fs.readFileSync` or custom .env parsing code remains
    - All app `env` definitions unchanged (NODE_ENV, NACOS_PORT, REDIS_PASSWORD, JWT_SECRET, etc. with their || '' fallbacks)
  </acceptance_criteria>
  <verify>
    <automated>head -1 ecosystem.config.cjs | grep -q "require('dotenv').config();" && ! grep -q "readFileSync" ecosystem.config.cjs && ! grep -q "fs.existsSync" ecosystem.config.cjs</automated>
  </verify>
  <done>ecosystem.config.cjs uses dotenv npm package instead of custom parser</done>
</task>

<task type="auto">
  <name>Task 3: Document Maven build order in CLAUDE.md Backend Startup Issues</name>
  <files>CLAUDE.md</files>
  <action>
    In CLAUDE.md, find the "Backend Startup Issues" section (around line 273). Add a bullet point after line 275 that documents the Maven build order requirement:

    Add this line to the "Backend Startup Issues" section:
    `- Maven build order: Run `cd recommendation && mvn install -DskipTests` before `backend-spring` — backend-spring/pom.xml declares dependency on recommend-api (line 215)`

    This supplements the existing documentation in the "Recommendation Services" section (lines 398-405) by making the build dependency explicit in the startup issues section.
  </action>
  <read_first>
    CLAUDE.md
  </read_first>
  <acceptance_criteria>
    - CLAUDE.md "Backend Startup Issues" section contains text referencing "recommendation && mvn install -DskipTests"
    - CLAUDE.md "Backend Startup Issues" section mentions backend-spring depends on recommend-api
  </acceptance_criteria>
  <verify>
    <automated>grep -A 15 "Backend Startup Issues" CLAUDE.md | grep -q "mvn install -DskipTests" && grep -A 15 "Backend Startup Issues" CLAUDE.md | grep -q "recommend-api"</automated>
  </verify>
  <done>Maven build order documented in CLAUDE.md Backend Startup Issues section</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **INFRA-01 verified**: `grep "require('dotenv').config()" ecosystem.config.cjs` returns a match at line 1
2. **INFRA-02 verified**: `grep "mvn install -DskipTests" CLAUDE.md` returns a match
3. **No custom parser remains**: `grep "readFileSync\|fs.existsSync" ecosystem.config.cjs` returns no matches
4. **All app env fallbacks intact**: Each app block in ecosystem.config.cjs still has its original env vars with || '' fallbacks
</verification>

<success_criteria>
1. PM2 ecosystem.config.cjs uses dotenv npm package instead of custom .env parser (INFRA-01 complete)
2. Build documentation specifies recommend-api must `mvn install` before backend-spring (INFRA-02 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/24-pm2-build-infrastructure/24-01-SUMMARY.md`
</output>
