---
phase: 07-code-quality-dependencies
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/lib/socket.ts
  - console/src/i18n/utils/storage.ts
  - console/src/composables/useLocale.ts
  - console/src/features/sider/NavUser.vue
  - management/src/views/analytics/components/ScoringRuleSelector.vue
  - backend-spring/pom.xml
  - recommendation/pom.xml
  - .gitignore
autonomous: true
requirements:
  - QUAL-04
  - DEP-01
  - DEP-02
  - DEP-03

must_haves:
  truths:
    - "No console.log or console.warn exists in production frontend code (console.error is acceptable)"
    - "management/.env is not tracked by git"
    - "pom.xml contains no SNAPSHOT dependencies"
    - "Backend compiles and frontends build cleanly after all changes"
  artifacts:
    - path: "console/src/lib/socket.ts"
      provides: "WebSocket client without production console.warn statements"
    - path: "console/src/i18n/utils/storage.ts"
      provides: "i18n storage utilities without unguarded console.warn statements"
    - path: "management/src/views/analytics/components/ScoringRuleSelector.vue"
      provides: "Scoring rule selector without production console.warn"
    - path: "backend-spring/pom.xml"
      provides: "Backend build config with stable version (no SNAPSHOT)"
      contains: "1.0.0"
    - path: "recommendation/pom.xml"
      provides: "Recommendation module build config with stable version"
      contains: "1.0.0"
  key_links:
    - from: "backend-spring/pom.xml"
      to: "recommendation/pom.xml"
      via: "recommend-api dependency version must match"
      pattern: "recommend-api.*1\\.0\\.0"
---

<objective>
Remove console.log/warn from production frontend code, replace SNAPSHOT dependencies with stable versions, and untrack management/.env from git.

Purpose: Production code should not emit debug output to the browser console. SNAPSHOT dependencies make builds non-reproducible. Tracked .env files risk secret exposure.
Output: Clean frontend builds, stable pom.xml versions, management/.env removed from git tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-code-quality-dependencies/07-CONTEXT.md
@.planning/phases/07-code-quality-dependencies/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unguarded console.log/warn from frontend production code</name>
  <files>
    console/src/lib/socket.ts,
    console/src/i18n/utils/storage.ts,
    console/src/composables/useLocale.ts,
    console/src/features/sider/NavUser.vue,
    management/src/views/analytics/components/ScoringRuleSelector.vue
  </files>
  <action>
Per D-08, remove all console.log and console.warn statements from production code in console/ and management/. Per D-09, keep console.error. Per D-10, keep statements inside DEV guards.

Exact removals (6 instances total, confirmed by research):

**console/ -- 5 removals:**

1. `console/src/lib/socket.ts` line 264: `console.warn("[WebSocket] Auth error - stopping reconnect attempts")` -- remove entirely (the reconnection stopping logic still runs, just without the warn)

2. `console/src/lib/socket.ts` line 379: `console.warn("[WebSocket] Cannot subscribe to community: not connected")` -- remove entirely

3. `console/src/i18n/utils/storage.ts` line 60: `console.warn("vue-sonner not available:", error)` -- remove the console.warn call; keep the surrounding catch block logic

4. `console/src/i18n/utils/storage.ts` line 265: `console.warn("Failed to read locale from storage:", e)` -- remove the console.warn call; keep the surrounding catch block logic

5. `console/src/i18n/utils/storage.ts` line 295: `console.warn("Storage write failed, attempting fallback:", e)` -- remove the console.warn call; keep the surrounding catch block logic

6. `console/src/composables/useLocale.ts` line 33: `console.warn(\`Unsupported locale: ${newLocale}\`)` -- remove the console.warn call

7. `console/src/features/sider/NavUser.vue` line 73: `console.warn("Failed to load notification count:", error)` -- remove the console.warn call; keep the surrounding catch block logic

**management/ -- 1 removal:**

8. `management/src/views/analytics/components/ScoringRuleSelector.vue`: 1 unguarded console.warn about scoring rules load failure -- remove the console.warn call; keep the surrounding catch block logic

**KEEP (do NOT touch -- already DEV-guarded or non-production):**
- `console/src/utils/request.ts` line 376: guarded by `isDevelopment`
- `management/src/stores/auth.ts` lines 108, 131: guarded by `import.meta.env.DEV`
- `management/src/stores/analytics/composables/useAnalyticsReports.ts` line 89: guarded by `import.meta.env.DEV`
- `management/src/utils/request.ts` line 307: guarded by `isDevelopment`
- `management/src/i18n/index.ts` line 97: guarded by `import.meta.env.DEV`
- `management/src/i18n/check.ts`: CLI-only tool, not imported by production code
- `console/src/features/submit/submitQueue.spec.ts`: test file
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next && grep -rn "console\.\(log\|warn\)" console/src/ management/src/ --include="*.ts" --include="*.vue" | grep -v "\.spec\." | grep -v "test\|check\.ts\|node_modules" | grep -v "import\.meta\.env\.DEV\|isDevelopment" | head -20</automated>
  </verify>
  <done>
All 6 unguarded console.log/warn statements removed from production frontend code. DEV-guarded and test-only statements are untouched. No console.log/warn remains in production code paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace SNAPSHOT dependencies and untrack management/.env</name>
  <files>
    backend-spring/pom.xml,
    recommendation/pom.xml,
    .gitignore
  </files>
  <action>
Per D-11, D-12, D-13.

**Step 1: Replace SNAPSHOT in backend-spring/pom.xml**
- Line 13: Change `<version>0.0.1-SNAPSHOT</version>` to `<version>1.0.0</version>`
- Line 199: Change `<version>1.0.0-SNAPSHOT</version>` (for recommend-api) to `<version>1.0.0</version>`

**Step 2: Replace SNAPSHOT in recommendation/pom.xml**
- Line 9: Change `<version>1.0.0-SNAPSHOT</version>` to `<version>1.0.0</version>`

**Step 3: Rebuild recommendation module to local Maven repo**
Run: `cd recommendation && mvn install -DskipTests`
This publishes the new `1.0.0` version to the local Maven repo so backend-spring can resolve it.

**Step 4: Verify backend compiles with new versions**
Run: `cd backend-spring && ./mvnw compile -q`

**Step 5: Untrack management/.env from git**
Run: `git rm --cached management/.env`
The `.gitignore` already has patterns covering `.env` files (line 53-55), so no .gitignore changes are needed. Verify with `git ls-files management/.env` which should return empty.

**Step 6: Verify no other secrets tracked (D-13)**
Run: `git ls-files | grep -i "\.env"` -- should return empty.
Run: `git ls-files | grep -iE "credentials|secret|\.pem|\.key"` -- should return empty.
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next && grep -c "SNAPSHOT" backend-spring/pom.xml recommendation/pom.xml && git ls-files management/.env && git ls-files | grep -i "\.env" | wc -l</automated>
  </verify>
  <done>
No SNAPSHOT dependencies in backend-spring/pom.xml or recommendation/pom.xml. management/.env is not tracked by git. No .env files are tracked by git. Recommendation module republished to local Maven repo. Backend compiles with stable versions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repository -> Public visibility | Tracked .env files could expose secrets |
| Maven local repo -> Build resolution | SNAPSHOT deps create non-reproducible builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | management/.env in git | mitigate | `git rm --cached management/.env` removes tracked file; .gitignore prevents re-tracking |
| T-07-06 | Repudiation | SNAPSHOT dependencies | mitigate | Stable versions ensure reproducible builds and auditable dependency trees |
| T-07-07 | Information Disclosure | Console output in production | accept | console.warn statements do not contain secrets, but removing them reduces information surface |
</threat_model>

<verification>
1. No SNAPSHOT deps: `grep -c SNAPSHOT backend-spring/pom.xml recommendation/pom.xml` returns 0 for both
2. management/.env untracked: `git ls-files management/.env` returns empty
3. No .env files tracked: `git ls-files | grep "\.env"` returns empty
4. Frontend builds: `cd console && pnpm build` succeeds
5. Frontend builds: `cd management && pnpm build` succeeds
6. Backend compiles: `cd backend-spring && ./mvnw compile` succeeds
7. No production console.log/warn: grep confirms only DEV-guarded or test-only instances remain
</verification>

<success_criteria>
- Zero unguarded console.log/warn in production frontend code
- Zero SNAPSHOT dependencies in backend-spring/pom.xml and recommendation/pom.xml
- management/.env is not tracked by git
- No other .env files tracked by git
- Both frontends build successfully
- Backend compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-quality-dependencies/07-03-SUMMARY.md`
</output>
