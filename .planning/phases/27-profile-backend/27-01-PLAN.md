---
wave: 1
depends_on: []
requirements_addressed: [PROFILE-01, PROFILE-03]
files_modified:
  - backend-spring/src/main/java/com/ulticode/modules/user/dto/ProfileVO.java
  - backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java
  - backend-spring/src/main/java/com/ulticode/modules/user/service/UserService.java
  - backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java
autonomous: false
---

<objective>

**Wave 1: Profile Backend Core**

Implement user profile viewing, editing, and avatar upload functionality:
- GET /users/{id}/profile — returns ProfileVO (user + stats + social counts)
- POST /users/me/avatar — accepts MultipartFile, stores to local disk, returns URL

</objective>

---

<files_to_read>

- `backend-spring/src/main/java/com/ulticode/modules/user/dto/UserStatsDTO.java` — @Data + @JsonInclude(NON_NULL) DTO pattern
- `backend-spring/src/main/java/com/ulticode/modules/user/dto/UserVO.java` — existing toVO/toPublicVO pattern
- `backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java` — existing service + FollowMapper integration point
- `backend-spring/src/main/java/com/ulticode/modules/follow/mapper/FollowMapper.java` — countByFollowingId/countByFollowerId queries
- `backend-spring/src/main/java/com/ulticode/common/util/SecurityUtil.java` — getCurrentUserId()
- `backend-spring/src/main/java/com/ulticode/modules/user/entity/User.java` — User entity fields

</files_to_read>

---

<tasks>

## Task 1: Create ProfileVO.java

<read_first>
backend-spring/src/main/java/com/ulticode/modules/user/dto/UserStatsDTO.java
backend-spring/src/main/java/com/ulticode/modules/user/dto/UserVO.java
</read_first>

<acceptance_criteria>
- `grep -n "class ProfileVO" backend-spring/src/main/java/com/ulticode/modules/user/dto/ProfileVO.java` returns the class declaration
- ProfileVO has fields: id, username, name, avatar, bio, company, location, website, joinedAt, preferredLanguage, totalSolved, submissionCount, globalRank, acceptanceRate, followerCount, followingCount, achievementCount
- ProfileVO has static factory: `fromUser(User user, UserStatsDTO stats, int followerCount, int followingCount, int achievementCount)`
- Class uses @Data + @JsonInclude(NON_NULL)
</acceptance_criteria>

<action>

**File:** `backend-spring/src/main/java/com/ulticode/modules/user/dto/ProfileVO.java`

Create new DTO:
```java
package com.ulticode.modules.user.dto;

import com.fasterxml.jackson.annotation.JsonInclude;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@JsonInclude(JsonInclude.Include.NON_NULL)
public class ProfileVO {
    private String id;
    private String username;
    private String name;
    private String avatar;
    private String bio;
    private String company;
    private String location;
    private String website;
    private LocalDateTime joinedAt;
    private String preferredLanguage;
    private int totalSolved;
    private Long submissionCount;
    private Integer globalRank;
    private Double acceptanceRate;
    private int followerCount;
    private int followingCount;
    private int achievementCount;

    public static ProfileVO fromUser(User user, UserStatsDTO stats, int followerCount, int followingCount, int achievementCount) {
        ProfileVO vo = new ProfileVO();
        vo.setId(user.getId());
        vo.setUsername(user.getUsername());
        vo.setName(user.getName());
        vo.setAvatar(user.getAvatar());
        vo.setBio(user.getBio());
        vo.setCompany(user.getCompany());
        vo.setLocation(user.getLocation());
        vo.setWebsite(user.getWebsite());
        vo.setJoinedAt(user.getJoinedAt());
        vo.setPreferredLanguage(user.getPreferredLanguage());
        vo.setTotalSolved(stats != null ? stats.getTotalSolved() : 0);
        vo.setSubmissionCount(stats != null ? stats.getSubmissionCount() : 0L);
        vo.setGlobalRank(stats != null ? stats.getGlobalRank() : null);
        vo.setAcceptanceRate(stats != null ? stats.getAcceptanceRate() : null);
        vo.setFollowerCount(followerCount);
        vo.setFollowingCount(followingCount);
        vo.setAchievementCount(achievementCount);
        return vo;
    }
}
```

</action>

## Task 2: Add methods to UserService interface

<read_first>
backend-spring/src/main/java/com/ulticode/modules/user/service/UserService.java
</read_first>

<acceptance_criteria>
- `grep -n "getUserProfile" backend-spring/src/main/java/com/ulticode/modules/user/service/UserService.java` returns the interface method
- `grep -n "uploadAvatar" backend-spring/src/main/java/com/ulticode/modules/user/service/UserService.java` returns the interface method
</acceptance_criteria>

<action>

**File:** `backend-spring/src/main/java/com/ulticode/modules/user/service/UserService.java`

Add two methods to the interface:
```java
import org.springframework.web.multipart.MultipartFile;

// In UserService interface:
ProfileVO getUserProfile(String id);
String uploadAvatar(MultipartFile file);
```

</action>

## Task 3: Implement UserServiceImpl additions

<read_first>
backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java
backend-spring/src/main/java/com/ulticode/modules/follow/mapper/FollowMapper.java
backend-spring/src/main/java/com/ulticode/common/util/SecurityUtil.java
</read_first>

<acceptance_criteria>
- `grep -n "FollowMapper" backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java` shows FollowMapper injected via constructor
- `grep -n "getUserProfile" backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java` shows the implementation
- `grep -n "uploadAvatar" backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java` shows the implementation
- `grep -rn "uploads/avatars" backend-spring/src/main/java/com/ulticode/` returns file storage logic
</acceptance_criteria>

<action>

**File:** `backend-spring/src/main/java/com/ulticode/modules/user/service/impl/UserServiceImpl.java`

1. Add imports:
```java
import com.ulticode.modules.follow.mapper.FollowMapper;
import com.ulticode.modules.user.dto.ProfileVO;
import org.springframework.web.multipart.MultipartFile;
import org.springframework.web.util.UriComponentsBuilder;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.UUID;
```

2. Add FollowMapper field and update constructor:
```java
private final FollowMapper followMapper;

public UserServiceImpl(UserMapper userMapper, SubmissionMapper submissionMapper,
                       ProblemMapper problemMapper, ProblemTagRelationMapper problemTagRelationMapper,
                       FollowMapper followMapper) {
    this.userMapper = userMapper;
    this.submissionMapper = submissionMapper;
    this.problemMapper = problemMapper;
    this.problemTagRelationMapper = problemTagRelationMapper;
    this.followMapper = followMapper;
}
```

3. Add getUserProfile implementation:
```java
@Override
public ProfileVO getUserProfile(String id) {
    User user = findById(id)
            .orElseThrow(() -> new BusinessException(ErrorCode.USER_NOT_FOUND));

    UserStatsDTO stats = getUserStatsById(id);

    int followerCount = 0;
    int followingCount = 0;
    try {
        if (followMapper != null) {
            Long fc = followMapper.countByFollowingId(id);
            Long foc = followMapper.countByFollowerId(id);
            followerCount = fc != null ? fc.intValue() : 0;
            followingCount = foc != null ? foc.intValue() : 0;
        }
    } catch (Exception e) {
        log.warn("Failed to get follow counts for user {}: {}", id, e.getMessage());
    }

    return ProfileVO.fromUser(user, stats, followerCount, followingCount, 0);
}
```

4. Add uploadAvatar implementation:
```java
@Override
public String uploadAvatar(MultipartFile file) {
    String userId = SecurityUtil.getCurrentUserId();
    if (userId == null) {
        throw new BusinessException(ErrorCode.UNAUTHORIZED);
    }

    if (file == null || file.isEmpty()) {
        throw new BusinessException(ErrorCode.BAD_REQUEST, "File is required");
    }

    String contentType = file.getContentType();
    if (contentType == null || !contentType.startsWith("image/")) {
        throw new BusinessException(ErrorCode.BAD_REQUEST, "Only image files are allowed");
    }

    String originalFilename = file.getOriginalFilename();
    String ext = "";
    if (originalFilename != null && originalFilename.contains(".")) {
        ext = originalFilename.substring(originalFilename.lastIndexOf("."));
    }
    String uuid = UUID.randomUUID().toString();
    String filename = uuid + ext;

    Path uploadDir = Paths.get("uploads/avatars");
    try {
        Files.createDirectories(uploadDir);
        Path filePath = uploadDir.resolve(filename);
        file.transferTo(filePath.toFile());
    } catch (Exception e) {
        log.error("Failed to save avatar for user {}: {}", userId, e.getMessage());
        throw new BusinessException(ErrorCode.INTERNAL_SERVER_ERROR, "Failed to save avatar");
    }

    String avatarUrl = "/uploads/avatars/" + filename;

    User user = findById(userId)
            .orElseThrow(() -> new BusinessException(ErrorCode.USER_NOT_FOUND));
    user.setAvatar(avatarUrl);
    userMapper.updateById(user);

    log.info("Avatar uploaded for user {}: {}", userId, avatarUrl);
    return avatarUrl;
}
```

</action>

## Task 4: Add controller endpoints

<read_first>
backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java
</read_first>

<acceptance_criteria>
- `grep -n "GET.*profile" backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java` returns the new endpoint
- `grep -n "POST.*avatar" backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java` returns the new endpoint
- `grep -n "@RateLimit" backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java` shows rate limit on avatar upload
</acceptance_criteria>

<action>

**File:** `backend-spring/src/main/java/com/ulticode/modules/user/controller/UserController.java`

1. Add imports:
```java
import com.ulticode.modules.user.dto.ProfileVO;
import org.springframework.web.multipart.MultipartFile;
```

2. Add two new endpoints:
```java
@GetMapping("/{id}/profile")
public Result<ProfileVO> getUserProfile(@PathVariable String id) {
    ProfileVO profile = userService.getUserProfile(id);
    return Result.success(profile);
}

@PostMapping("/me/avatar")
@RateLimit(key = "user:avatar", limit = 10, period = 60)
public Result<String> uploadAvatar(@RequestParam("file") MultipartFile file) {
    String avatarUrl = userService.uploadAvatar(file);
    return Result.success(avatarUrl);
}
```

</action>

---

</tasks>

<verification>

**Profile viewing:**
1. Start backend: `cd backend-spring && ./mvnw spring-boot:run`
2. Login and get session cookie
3. `curl -s http://localhost:9001/users/u-admin-001/profile -b /tmp/cookies.txt | jq .` returns profile with username, avatar, bio, stats
4. Stats include: totalSolved, submissionCount, globalRank, acceptanceRate, followerCount, followingCount, achievementCount

**Profile editing:**
5. `curl -s -X PUT http://localhost:9001/users/me/profile -H "Content-Type: application/json" -d '{"bio":"Hello world","company":"ACME"}' -b /tmp/cookies.txt | jq .` returns updated profile

**Avatar upload:**
6. `curl -s -X POST http://localhost:9001/users/me/avatar -F "file=@/tmp/test.png" -b /tmp/cookies.txt | jq .` returns avatar URL

</verification>

<must_haves>

- GET /users/{id}/profile returns ProfileVO with all 17 fields
- PUT /users/me/profile (existing) updates bio, company, location, website
- POST /users/me/avatar stores file to uploads/avatars/{uuid}.{ext}, updates user.avatar, returns URL
- ProfileVO includes stats (totalSolved, submissionCount, globalRank, acceptanceRate) from UserStatsDTO
- ProfileVO includes followerCount/followingCount from FollowMapper (graceful fallback to 0)
- ProfileVO includes achievementCount (placeholder 0)

</must_haves>
