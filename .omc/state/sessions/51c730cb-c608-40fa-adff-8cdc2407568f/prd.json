{
  "version": "1.0",
  "stories": [
    {
      "id": "US-001",
      "title": "Fix deprecated next() callback in router navigation guard",
      "priority": "high",
      "acceptanceCriteria": [
        "No Vue Router deprecation warning about next() callback in console/src/router/index.ts",
        "Navigation guard returns values directly instead of calling next(value)",
        "TypeScript compiles without errors",
        "Auth-protected routes still redirect to /login when unauthenticated",
        "Authenticated users accessing /login or /register still redirect to forum-home"
      ],
      "passes": true
    }
  ]
}
