---
title: "2026-06-24"
type: daily
tags: [daily, ingest]
status: living
updated: 2026-06-24
sources: []
---

# 2026-06-24 — Ingest journal

> Sidebar-menu unification: three code-level CRs reviewed, all findings fixed,
> then the `docs/architecture/` copies retired into this wiki.

## Ingested

- Three code-level CRs on commits `a47423c4d..fc266ce10` (claude/glm-5.2,
  codex/MiniMax-M3, opencode/deepseek) — reviewed the *landed code*, not the
  plan doc
- The `fc266ce10` fix history (`SidebarParentItem` `:open=undefined`
  → controlled-closed regression)

## Created / updated pages

- `wiki/concepts/sidebar-menu.md` — Trade-offs expanded (CSS cascade order,
  `color-mix` progressive enhancement, both collapsibles uncontrolled-only,
  @beta status of `SidebarNavUser`/`SidebarIconButton`); `sources` + Related
  pruned of the retired `docs/architecture/` files
- `wiki/log.md` — this session's fix entry
- `shared/sidebar-menu/README.md` (created — outside the wiki, but the
  consumer-facing companion to this concept page)

## Queries filed

- _none_

## Lint findings

- _none_

## Notes

- **The HIGH-1 lesson**: a spec that only ever exercises `defaultOpen=true`
  cannot catch a controlled-closed regression (the pre-`fc266ce10` test passed
  while the component was broken). The new `defaultOpen=false` guard on
  `SidebarParentItem` is the structural fix; making *both* collapsibles purely
  uncontrolled removes the foot-gun entirely — there is no `:open` left to bind
  `undefined`.
- `docs/architecture/` is for transient analysis. Once a concept page absorbs
  the knowledge, the docs copies are redundant. The six CRs stay in git history
  at `66cd1be64` — process artifacts don't belong in the knowledge base.
- Verified green before retiring the docs: `shared/sidebar-menu` 41/41 tests +
  `vue-tsc` EXIT 0; console + management `vue-tsc` EXIT 0.
