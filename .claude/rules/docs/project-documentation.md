---
paths:
  - "AGENTS.md"
  - "**/AGENTS.md"
  - "CLAUDE.md"
  - "**/CLAUDE.md"
  - "docs/**/*.md"
---

# Documentation changes

- Use `docs/index.md` to locate the canonical topic. Keep agent rules in root/nearest `AGENTS.md`; avoid copying policy into several documents.
- Check changed commands, paths and claims against current sources. Keep session logs and task ledgers out of durable project documentation.
- For documentation-only work, inspect the diff and changed links/paths; do not run application builds unless an executable example or behavior change needs them.
