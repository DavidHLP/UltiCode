---
phase: 41-dependency-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-spring/pom.xml
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
user_setup: []

must_haves:
  truths:
    - "springdoc version property is updated to 2.8.17"
    - "Testcontainers Redis dependency is present with test scope"
    - "swagger-ui loads at /swagger-ui.html without errors"
    - "/api-docs returns valid OpenAPI JSON"
  artifacts:
    - path: "backend-spring/pom.xml"
      provides: "Updated springdoc version and new testcontainers-redis dependency"
      contains: "springdoc.version>2.8.17</springdoc.version"
    - path: "backend-spring/pom.xml"
      provides: "Testcontainers Redis test dependency"
      contains: "testcontainers-redis"
  key_links:
    - from: "backend-spring/pom.xml"
      to: "springdoc dependency"
      via: "version property ${springdoc.version}"
---

<objective>
Update springdoc to 2.8.17 and add Testcontainers Redis dependency in backend-spring/pom.xml. Verify swagger-ui and /api-docs work correctly after the upgrade.
</objective>

<context>
@backend-spring/pom.xml
</context>

<interfaces>
<!-- Existing pattern in pom.xml for version property -->
<springdoc.version property>: 2.6.0 (to be changed to 2.8.17)

<!-- Existing testcontainers dependencies pattern -->
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>testcontainers</artifactId>
  <version>1.11.3</version>
  <scope>test</scope>
</dependency>
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>mysql</artifactId>
  <version>1.11.3</version>
  <scope>test</scope>
</dependency>

<!-- New dependency to add -->
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>testcontainers-redis</artifactId>
  <version>1.11.3</version>
  <scope>test</scope>
</dependency>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update springdoc version property to 2.8.17</name>
  <files>backend-spring/pom.xml</files>
  <action>
    In backend-spring/pom.xml, find the springdoc.version property (line 21: currently 2.6.0) and update it to 2.8.17. The dependency at lines 125-127 uses ${springdoc.version} so the version property change will propagate to the dependency.
  </action>
  <verify>
    <automated>grep -n 'springdoc.version>2.8.17' backend-spring/pom.xml</automated>
  </verify>
  <done>springdoc.version property set to 2.8.17 in pom.xml</done>
</task>

<task type="auto">
  <name>Task 2: Add Testcontainers Redis test-scoped dependency</name>
  <files>backend-spring/pom.xml</files>
  <action>
    In backend-spring/pom.xml, add a new test-scoped dependency for testcontainers-redis after the existing testcontainers-mysql dependency (after line 198). Use groupId: org.testcontainers, artifactId: testcontainers-redis, version: 1.11.3 (same version as other testcontainers deps, not using BOM since existing pattern uses explicit versions).
  </action>
  <verify>
    <automated>grep -n 'testcontainers-redis' backend-spring/pom.xml</automated>
  </verify>
  <done>Testcontainers Redis dependency present in pom.xml with test scope</done>
</task>

<task type="auto">
  <name>Task 3: Verify springdoc startup and swagger-ui</name>
  <files>backend-spring/pom.xml</files>
  <action>
    Start the backend with ./mvnw spring-boot:run from backend-spring/ directory. Wait for startup to complete (watch for "Started" log message). Then verify swagger-ui loads: curl -s -o /dev/null -w "%{http_code}" http://localhost:9001/swagger-ui.html should return 200. Stop the server after verification.
  </action>
  <verify>
    <automated>cd backend-spring && ./mvnw spring-boot:run -DskipTests > /tmp/ulticode-springdoc-test.log 2>&1 &
sleep 45
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:9001/swagger-ui.html)
kill %1 2>/dev/null
echo "swagger-ui HTTP: $HTTP_CODE"
grep -q "Started " /tmp/ulticode-springdoc-test.log && echo "STARTUP: OK"</automated>
  </verify>
  <done>Backend starts successfully and swagger-ui returns HTTP 200</done>
</task>

<task type="auto">
  <name>Task 4: Verify /api-docs returns valid OpenAPI JSON</name>
  <files>backend-spring/pom.xml</files>
  <action>
    Start the backend again (or reuse from previous task if still running). Verify /api-docs returns valid JSON: curl -s http://localhost:9001/api-docs | python3 -c "import sys,json; d=json.load(sys.stdin); print('openapi:', d.get('openapi', 'MISSING')); print('paths:', len(d.get('paths', {})))" should print openapi version and path count. Stop the server after verification.
  </action>
  <verify>
    <automated>cd backend-spring && ./mvnw spring-boot:run -DskipTests > /tmp/ulticode-springdoc-test2.log 2>&1 &
sleep 45
API_RESPONSE=$(curl -s http://localhost:9001/api-docs)
kill %1 2>/dev/null
echo "$API_RESPONSE" | python3 -c "import sys,json; d=json.load(sys.stdin); print('openapi:', d.get('openapi')); print('paths:', len(d.get('paths', {})))"</automated>
  </verify>
  <done>/api-docs returns valid OpenAPI JSON with openapi field and paths section</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. pom.xml has springdoc.version=2.8.17 and testcontainers-redis dependency
2. Backend compiles and starts successfully
3. /swagger-ui.html returns HTTP 200
4. /api-docs returns valid JSON with openapi and paths fields
</verification>

<success_criteria>
- DEPS-01: springdoc version updated to 2.8.17, swagger-ui at /swagger-ui.html returns 200, /api-docs returns valid OpenAPI JSON
- DEPS-02: Testcontainers Redis dependency present in pom.xml with test scope
</success_criteria>

<output>
After completion, create `.planning/phases/41-dependency-upgrades/41-01-SUMMARY.md`
</output>
