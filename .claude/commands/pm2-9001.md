Start all owner backends (auth/admin/app) and open their logs.
```bash
cd "/home/david/project/UltiCode-Public-Next" && pm2 start ecosystem.config.cjs --only ulticode-auth,ulticode-admin,ulticode-app && pm2 logs ulticode-auth
```