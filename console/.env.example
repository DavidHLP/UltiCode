# API Configuration
# Local Vite gateway seam; proxies /api/auth, /api/admin and /api to owner services.
VITE_API_BASE_URL=/api

# Test Credentials (development only)
# Leave empty in production or use actual test credentials
VITE_TEST_USERNAME=
VITE_TEST_PASSWORD=
