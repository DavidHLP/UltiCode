---
phase: 35
plan: 01
name: fix-flyway-url
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - DEPS-02
files_modified:
  - .github/workflows/ci.yml
---

<objective>
修复 CI workflow 中 Flyway 下载 URL 返回 404 的问题
</objective>

<tasks>

<task>
<read_first>
  - .github/workflows/ci.yml
  - .planning/phases/35-flyway-url/35-CONTEXT.md
</read_first>
<action>
更新 `.github/workflows/ci.yml` 中 `migrate-validate` job 的 Flyway 下载部分：

1. 将第 204 行的版本号 `11.3.4` 改为 `10.17.0`
2. 将第 204 行的 URL 从 `https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/11.3.4/flyway-commandline-11.3.4-linux-x64.tar.gz` 改为 `https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/10.17.0/flyway-commandline-10.17.0-linux-x64.tar.gz`
3. 将第 209 行的 `FLYWAY_DIR` 从 `flyway-11.3.4` 改为 `flyway-10.17.0`
</action>
<acceptance_criteria>
- grep "10.17.0" .github/workflows/ci.yml 返回非空结果
- grep "flyway-10.17.0" .github/workflows/ci.yml 返回非空结果
- URL 格式正确: `https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/10.17.0/flyway-commandline-10.17.0-linux-x64.tar.gz`
</acceptance_criteria>
<verify>
```bash
grep -n "10.17.0" .github/workflows/ci.yml
grep -n "flyway-10.17.0" .github/workflows/ci.yml
```
</verify>
</task>

</tasks>

<verification>
1. 运行 `grep "10.17.0" .github/workflows/ci.yml` 确认版本号已更新
2. 运行 `grep "flyway-10.17.0" .github/workflows/ci.yml` 确认目录名已更新
3. 提交更改
</verification>

<success_criteria>
- CI workflow 中 Flyway 下载 URL 不再返回 404
- DEPS-02 requirement 已满足
</success_criteria>
