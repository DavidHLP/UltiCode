{
  "version": "1.0",
  "stories": [
    {
      "id": "US-001",
      "title": "修复 GET /admin/solutions 500 错误",
      "acceptanceCriteria": [
        "数据库 solutions 表添加 is_pinned 列 (已通过 ALTER TABLE 手动执行)",
        "V27 迁移文件存在于 db-manager/migrations/",
        "API 不再返回 Unknown column 'is_pinned' 错误"
      ],
      "passes": true,
      "verification": "docker exec 确认列已添加，pm2 logs 无 SQL 错误"
    }
  ]
}
