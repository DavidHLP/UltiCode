---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db-manager/migrations/V24__submissions_seed.sql
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-04

must_haves:
  truths:
    - "User can view submission history with correct status values (AC/WA/TLE/MLE/RE/CE) matching enum exactly"
    - "Status distribution approximates: AC 45-55%, WA 20-30%, TLE 8-12%, RE 5-10%, MLE 3-5%, CE 2-5%"
    - "All status values have no leading/trailing whitespace (exact match to submission_statuses table)"
    - "Every submission references a valid user_id and problem_id (no orphaned FKs)"
  artifacts:
    - path: "db-manager/migrations/V24__submissions_seed.sql"
      provides: "~200 submission INSERT statements with varied status distribution"
      min_lines: 200
  key_links:
    - from: "V24__submissions_seed.sql"
      to: "users table"
      via: "user_id FK"
      pattern: "user_id IN (user-emma, user-yuki, user-sara, user-lily, user-max, user-alex, user-chen, user-raj, user-kim, user-sophie, user-john, user-mike, user-lisa, user-bob, user-alice, admin)"
    - from: "V24__submissions_seed.sql"
      to: "problems table"
      via: "problem_id FK"
      pattern: "problem_id IN (1..32)"

<objective>
Generate V24 migration: ~200 submission INSERTs with correct status distribution and no whitespace bugs
</objective>

<tasks>

<task>
<read_first>
  - db-manager/migrations/V17__recommendation_seed_submissions.sql
  - db-manager/migrations/V23__solutions_seed.sql
</read_first>
<action>
Create db-manager/migrations/V24__submissions_seed.sql with:
1. SET FOREIGN_KEY_CHECKS=0; at top
2. START TRANSACTION;
3. ~200 INSERT statements covering all 32 problems
4. Status distribution for ~200 subs:
   - AC: ~105 (52%)
   - WA: ~50 (25%)
   - TLE: ~20 (10%)
   - RE: ~15 (7.5%)
   - MLE: ~8 (4%)
   - CE: ~6 (3%)
5. Each INSERT uses UUID() for id
6. Status values: EXACT 'AC', 'WA', 'TLE', 'MLE', 'RE', 'CE' (NO whitespace)
7. Cover all 32 problems, 5-8 submissions per problem
8. Languages: typescript, javascript, python, java, cpp, bash
9. runtime: 0 for non-AC, actual ms for AC (5-200ms range)
10. memory: 0 for non-AC, actual MB for AC (10-256MB range)
11. Use valid user_ids: user-emma, user-yuki, user-sara, user-lily, user-max, user-alex, user-chen, user-raj, user-kim, user-sophie, user-john, user-mike, user-lisa, user-bob, user-alice, admin
12. created_at: spread across 2025-01 to 2026-04
13. COMMIT; at end
14. SET FOREIGN_KEY_CHECKS=1; at end

Example INSERT (NO whitespace in status):
INSERT INTO `submissions` (`id`,`problem_id`,`user_id`,`language`,`code`,`status`,`runtime`,`memory`,`notes`,`created_at`,`runtime_percentile`,`memory_percentile`) VALUES (UUID(),1,'user-emma','typescript','// two-sum solution','AC',65,42.3,NULL,'2026-01-20 14:10:00',78.5,65.2);
</action>
<acceptance_criteria>
  - grep -c "VALUES" db-manager/migrations/V24__submissions_seed.sql >= 195
  - grep " 'AC'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~105 (52% of 200)
  - grep " 'WA'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~50 (25%)
  - grep " 'TLE'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~20 (10%)
  - grep " 'RE'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~15 (7.5%)
  - grep " 'MLE'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~8 (4%)
  - grep " 'CE'" db-manager/migrations/V24__submissions_seed.sql | wc -l is ~6 (3%)
  - NO status value contains leading/trailing whitespace (e.g., no ' AC' or 'AC ')
  - All user_ids are valid (user-emma, user-yuki, user-sara, user-lily, user-max, user-alex, user-chen, user-raj, user-kim, user-sophie, user-john, user-mike, user-lisa, user-bob, user-alice, admin)
  - All problem_ids are in range 1-32
  - grep "TRIM" db-manager/migrations/V24__submissions_seed.sql returns empty (no TRIM needed if values are correct)
</acceptance_criteria>
</task>

<task>
<read_first>
  - db-manager/migrations/V24__submissions_seed.sql
</read_first>
<action>
Run the migration using db-manager CLI:
cd db-manager && python -m db_manager.cli migrate

If db-manager fails, execute directly:
docker exec ulticode-mysql mysql -u ulticode -pulticode ulticode < db-manager/migrations/V24__submissions_seed.sql
</action>
<acceptance_criteria>
  - python -m db_manager.cli status shows V24 as applied
  - OR docker exec confirms V24 was applied
  - SELECT COUNT(*) FROM submissions >= 330 (existing 130 + ~200 new)
</acceptance_criteria>
</task>

<task>
<read_first>
  - (database query)
</read_first>
<action>
Verify V24 migration results:

1. Count check:
   SELECT COUNT(*) FROM submissions;
   Expected: >= 330

2. Status distribution check:
   SELECT status, COUNT(*) as cnt FROM submissions GROUP BY status;
   Expected: AC ~50%, WA ~25%, TLE ~10%, RE ~8%, MLE ~4%, CE ~3%

3. Whitespace check (SUB-03 verification):
   SELECT DISTINCT status FROM submissions;
   Expected: AC, WA, TLE, MLE, RE, CE (no whitespace, no leading/trailing spaces)

4. FK validation:
   SELECT COUNT(*) FROM submissions s WHERE NOT EXISTS (SELECT 1 FROM users u WHERE u.id = s.user_id);
   Expected: 0 orphaned user_id

   SELECT COUNT(*) FROM submissions s WHERE NOT EXISTS (SELECT 1 FROM problems p WHERE p.id = s.problem_id);
   Expected: 0 orphaned problem_id

5. Per-problem coverage:
   SELECT problem_id, COUNT(*) as cnt FROM submissions GROUP BY problem_id ORDER BY problem_id;
   All 32 problems should have submissions
</action>
<acceptance_criteria>
  - submissions table has >= 330 rows
  - status distribution matches expected percentages (within 5% margin)
  - SELECT DISTINCT status returns exactly 6 rows: AC, WA, TLE, MLE, RE, CE
  - No orphaned FKs (0 orphaned user_id or problem_id)
  - All 32 problems have at least 1 submission
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Read V24 migration file and count INSERT statements
2. Run migration via db-manager or direct mysql
3. Query database to verify:
   - Row count >= 330
   - Status distribution matches targets
   - DISTINCT status returns clean values (no whitespace)
   - No orphaned FKs
   - All 32 problems covered
</verification>
