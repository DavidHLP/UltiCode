---
phase: 32-contest-reminder-trigger
plan: 02
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java
  - backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/contest/scheduler/ContestScheduler.java
autonomous: true
requirements:
  - NOTIF-04
must_haves:
  truths:
    - "Metadata Map passed to createNotification() is persisted to the notifications table"
    - "NotificationPayload sent via WebSocket includes the metadata Map"
  artifacts:
    - path: "backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java"
      provides: "createNotification() signature accepts metadata parameter"
      min_lines: 3
    - path: "backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java"
      provides: "notification.setMetadata(metadata) called before insert"
      min_lines: 2
    - path: "backend-spring/src/main/java/com/ulticode/modules/contest/scheduler/ContestScheduler.java"
      provides: "metadata Map passed to createNotification()"
      min_lines: 5
  key_links:
    - from: "ContestScheduler"
      to: "NotificationService.createNotification()"
      via: "metadata parameter"
      pattern: "Map.*metadata.*createNotification"
    - from: "NotificationServiceImpl.createNotification()"
      to: "notification.setMetadata()"
      via: "insert before"
      pattern: "setMetadata.*insert"
    - from: "NotificationPayload"
      to: "notification.getMetadata()"
      via: "NotificationVO mapping"
      pattern: "notification\\.getMetadata\\(\\)"
---

<objective>
Close the metadata persistence gap identified in 32-VERIFICATION.md. Add `Map<String, Object> metadata` parameter to `NotificationService.createNotification()` interface and implementation, call `notification.setMetadata(metadata)` before insert, and update `ContestScheduler` to pass the metadata map it already builds.
</objective>

<context>
@backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java
@backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java
@backend-spring/src/main/java/com/ulticode/modules/notification/entity/Notification.java

## Key Facts

- `Notification` entity has `Map<String, Object> metadata` field with `JacksonTypeHandler` already configured (line 31 of Notification.java)
- `@Data` on Notification provides `setMetadata()` automatically
- `ContestScheduler` already builds the metadata Map (lines 114-118) but cannot pass it because `createNotification()` has no parameter for it
- `NotificationServiceImpl.createNotification()` at line 183 already calls `notification.getMetadata()` when building `NotificationPayload` for WebSocket push, but it is always null because `setMetadata()` is never called
- `AdminNotificationServiceImpl` already shows the correct pattern at line 93: `notification.setMetadata(metadata)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metadata parameter to NotificationService interface</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java</files>
  <read_first>
    backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java
  </read_first>
  <action>
    In the `NotificationService` interface, update the `createNotification` method signature (lines 77-89) to add a `Map<String, Object> metadata` parameter as the last parameter:

    Change:
    ```java
    NotificationVO createNotification(String userId, String type, String category,
                                      String title, String body, String link);
    ```
    To:
    ```java
    NotificationVO createNotification(String userId, String type, String category,
                                      String title, String body, String link,
                                      Map<String, Object> metadata);
    ```

    Also update the Javadoc comment above the method to document the metadata parameter.
  </action>
  <verify>
    <automated>grep -n "Map.*metadata" backend-spring/src/main/java/com/ulticode/modules/notification/service/NotificationService.java</automated>
  </verify>
  <done>NotificationService.createNotification() signature includes Map<String, Object> metadata parameter</done>
</task>

<task type="auto">
  <name>Task 2: Implement metadata persistence in NotificationServiceImpl</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java</files>
  <read_first>
    backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java
  </read_first>
  <action>
    In `NotificationServiceImpl`, update the `createNotification` method (lines 159-190):

    1. Update the method signature to match the interface:
    ```java
    public NotificationVO createNotification(String userId, String type, String category,
                                            String title, String body, String link,
                                            Map<String, Object> metadata)
    ```

    2. After setting all other fields on the Notification object (after line 169: `notification.setLink(link)`), add:
    ```java
    notification.setMetadata(metadata);
    ```
    This must be inserted before `notificationMapper.insert(notification)` at line 172.

    The existing `notification.getMetadata()` call at line 183 for `NotificationPayload` will now return the actual metadata instead of null.
  </action>
  <verify>
    <automated>grep -n "setMetadata" backend-spring/src/main/java/com/ulticode/modules/notification/service/impl/NotificationServiceImpl.java</automated>
  </verify>
  <done>notification.setMetadata(metadata) is called before insert; NotificationPayload gets real metadata</done>
</task>

<task type="auto">
  <name>Task 3: Update ContestScheduler to pass metadata to createNotification</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/contest/scheduler/ContestScheduler.java</files>
  <read_first>
    backend-spring/src/main/java/com/ulticode/modules/contest/scheduler/ContestScheduler.java
  </read_first>
  <action>
    In `ContestScheduler`, update the `createNotification()` call (lines 130-136) to pass the metadata Map that is already built at lines 114-118.

    The current call (lines 130-136) is:
    ```java
    notificationService.createNotification(
            participant.getUserId(),
            "CONTEST_REMINDER",
            "CONTEST",
            title,
            "",
            link);
    ```

    Change it to:
    ```java
    notificationService.createNotification(
            participant.getUserId(),
            "CONTEST_REMINDER",
            "CONTEST",
            title,
            "",
            link,
            metadata);
    ```

    The `metadata` variable is the `Map<String, Object>` already constructed at lines 114-118 containing `contestId`, `contestTitle`, `startTime`, and `dedupKey`.
  </action>
  <verify>
    <automated>grep -A5 "notificationService.createNotification" backend-spring/src/main/java/com/ulticode/modules/contest/scheduler/ContestScheduler.java | grep -c "metadata"</automated>
  </verify>
  <done>ContestScheduler passes metadata Map to createNotification()</done>
</task>

</tasks>

<verification>
## Overall Phase Verification

After all tasks complete, verify:

1. **Interface updated:** `NotificationService.java` has `Map<String, Object> metadata` in `createNotification` signature
2. **Implementation updated:** `NotificationServiceImpl.java` calls `notification.setMetadata(metadata)` before insert
3. **Scheduler updated:** `ContestScheduler.java` passes `metadata` as the last argument to `createNotification`
4. **Compilation:** `cd backend-spring && ./mvnw compile -q` succeeds with no errors
</verification>

<success_criteria>
- NotificationService.createNotification() accepts metadata parameter (interface updated)
- NotificationServiceImpl.createNotification() calls setMetadata() before insert (implementation updated)
- ContestScheduler passes metadata Map to createNotification() (caller updated)
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-contest-reminder-trigger/32-02-SUMMARY.md`
</output>
