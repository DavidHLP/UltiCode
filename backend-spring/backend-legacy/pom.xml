<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.ulticode</groupId>
		<artifactId>ulticode-backend</artifactId>
		<version>1.0.0</version>
	</parent>
	<artifactId>backend-legacy</artifactId>
	<name>UltiCode Backend (legacy single-module transitional module)</name>
	<description>Transitional module that preserves the pre-reactor single-module layout. Phase 7 of the microservice migration retires this module.</description>

	<dependencies>
		<!-- P7-MIGRATE-PROBLEM-001: canonical Problem administration write domain. -->
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-problem-domain</artifactId>
		</dependency>
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-contest-domain</artifactId>
		</dependency>
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-submission-domain</artifactId>
		</dependency>
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-notification-domain</artifactId>
		</dependency>
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-moderation-domain</artifactId>
		</dependency>
		<!--
		  P1-INFRA-002: backend-common holds the dependency-free atoms
		  (Result, PageResult, PaginationRequest, TraceIdUtil, TimeSource,
		  TimeSourceHolder, RpcResult, namespaced error-code base types,
		  trace/id metadata). The version is pinned by the reactor parent's
		  dependencyManagement, so no explicit <version> is needed here.
		-->
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-common</artifactId>
		</dependency>

		<!-- P7-WEB-SECURITY-INFRA-001: canonical rate-limit deep module. -->
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-web-security</artifactId>
		</dependency>

		<!--
		  P4-CUTOVER-001: provider-owned Dubbo contract module for App.
		  backend-legacy implements the Provider (ProblemAdministrationProvider)
		  and consumes it via @DubboReference (ProblemCutoverService) while
		  still running as a single JVM. When backend-app is physically
		  separated, the Provider class moves to backend-app and the Consumer
		  stays in backend-legacy.
		-->
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-app-api</artifactId>
		</dependency>
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-auth-api</artifactId>
		</dependency>

		<!-- P7-ADMIN-BULK-001: AdminBulkExecutor relocated to backend-admin. -->
		<dependency>
			<groupId>com.ulticode</groupId>
			<artifactId>backend-admin</artifactId>
		</dependency>

		<!-- Spring Boot Starters -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-json</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-websocket</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aop</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!-- Spring Cache abstraction (required for @EnableCaching, @Cacheable, @CacheEvict) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>

		<!-- Spring Boot Actuator: health, info, prometheus endpoints -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!-- Prometheus registry: exposes /actuator/prometheus in OpenMetrics format -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<!--
		  P1-OBS-001: OpenTelemetry trace propagation through HTTP + Dubbo.
		  Spring Boot 3.2.5 manages both versions through its own BOM, so
		  we do NOT need a separate opentelemetry-bom import. The Micrometer
		  Tracing bridge maps Observation API to the OpenTelemetry SDK; the
		  OTLP exporter is optional at Phase 1 — a no-op default keeps the
		  pipeline active without a collector reachable. The
		  W3C traceparent header is the default propagation type, so an
		  inbound HTTP request with `traceparent: 00-<id>-<span>-01` gets
		  joined into the same trace.
		-->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-exporter-otlp</artifactId>
		</dependency>

		<!-- Caffeine: in-process L1 cache for monitoring endpoints -->
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>

		<!-- MyBatis-Plus -->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-spring-boot3-starter</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>

		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-jsqlparser</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>

		<!-- MySQL -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Redisson for distributed locks and advanced Redis features -->
		<dependency>
			<groupId>org.redisson</groupId>
			<artifactId>redisson-spring-boot-starter</artifactId>
			<version>${redisson.version}</version>
		</dependency>

		<!--
		  P1-INFRA-003: Dubbo 3.3.6 starter + Triple protocol + Nacos registry.
		  Registry-only; no @DubboReference or @DubboService is introduced in
		  Phase 1. The Nacos registry adapter is `dubbo-registry-nacos`,
		  loaded by Dubbo via SPI when `dubbo.registry.address=nacos://...`.
		  The reactor parent does NOT import `dubbo-bom` (avoids the
		  Spring Boot 2.x transitives) so explicit <version> tags here pin
		  the two artifacts at ${dubbo.version}=3.3.6; Dubbo's own
		  transitive `nacos-client:2.5.1` is shipped by `dubbo-registry-nacos`.
		-->
		<dependency>
			<groupId>org.apache.dubbo</groupId>
			<artifactId>dubbo-spring-boot-starter</artifactId>
			<version>${dubbo.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.dubbo</groupId>
			<artifactId>dubbo-registry-nacos</artifactId>
			<version>${dubbo.version}</version>
		</dependency>

		<!-- JWT -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- SpringDoc OpenAPI (Swagger) -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Hutool - Java utility library -->
		<dependency>
			<groupId>cn.hutool</groupId>
			<artifactId>hutool-all</artifactId>
			<version>${hutool.version}</version>
		</dependency>

		<!-- MapStruct -->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>

		<!-- OWASP Java Encoder for XSS output encoding -->
		<dependency>
			<groupId>org.owasp.encoder</groupId>
			<artifactId>encoder</artifactId>
			<version>1.4.0</version>
		</dependency>

		<!-- MeiliSearch -->
		<dependency>
			<groupId>com.meilisearch.sdk</groupId>
			<artifactId>meilisearch-java</artifactId>
			<version>0.20.1</version>
		</dependency>
		<dependency>
			<groupId>com.squareup.okhttp3</groupId>
			<artifactId>okhttp</artifactId>
			<version>4.12.0</version>
		</dependency>
		<dependency>
			<groupId>com.squareup.okhttp3</groupId>
			<artifactId>mockwebserver</artifactId>
			<version>4.12.0</version>
			<scope>test</scope>
		</dependency>

		<!-- Test dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>1.21.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mysql</artifactId>
			<version>1.21.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.redis</groupId>
			<artifactId>testcontainers-redis</artifactId>
			<version>2.2.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.tngtech.archunit</groupId>
			<artifactId>archunit-junit5</artifactId>
			<version>1.2.0</version>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<finalName>app</finalName>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.14</version>
				<configuration>
					<excludes>
						<exclude>**/*Mapper.java</exclude>
						<exclude>**/*Mapper.xml</exclude>
						<exclude>**/entity/*.java</exclude>
						<exclude>**/*DTO.java</exclude>
						<exclude>**/*VO.java</exclude>
						<exclude>**/*BO.java</exclude>
						<exclude>**/*Response.java</exclude>
						<exclude>**/*Request.java</exclude>
						<exclude>**/*Config.java</exclude>
						<exclude>**/*Properties.java</exclude>
						<exclude>**/*Application.java</exclude>
					</excludes>
					<rules>
						<rule>
							<element>BUNDLE</element>
							<limits>
								<limit>
									<counter>LINE</counter>
									<value>COVEREDRATIO</value>
									<minimum>0.05</minimum>
								</limit>
								<limit>
									<counter>BRANCH</counter>
									<value>COVEREDRATIO</value>
									<minimum>0.02</minimum>
								</limit>
							</limits>
						</rule>
					</rules>
				</configuration>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<phase>initialize</phase>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!--
			  Mockito 5 + ByteBuddy need -Djdk.attach.allowAttachSelf
			  to self-attach under Oracle JDK 17.0.2 (which is what
			  `mvn test` picks up in the main worktree). Without
			  this, every @Mock / @Spy test errors with
			  "Could not initialize inline Byte Buddy mock maker".
			  @{argLine} preserves the jacoco -javaagent:... argLine
			  that the jacoco-maven-plugin injects at the prepare-agent
				  phase; we just append the attach flag.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>