---
phase: 43
plan: 01
name: Raise JaCoCo thresholds
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-spring/pom.xml
autonomous: true
requirements:
  - JAC-01
---

<objective>
Raise JaCoCo coverage thresholds in backend-spring/pom.xml: LINE from 3% to 5%, BRANCH from 1% to 3%. This improves quality signal as planned in JAC-01.
</objective>

<read_first>
- `backend-spring/pom.xml` — locate the jacoco-maven-plugin configuration (lines ~265-324)
</read_first>

<tasks>

<task>
<type>execute</type>
<files>
  - backend-spring/pom.xml
</files>
<action>
In `backend-spring/pom.xml`, find the `jacoco-maven-plugin` configuration section. Locate the `<limit>` block with `<counter>LINE</counter>`. Change `<minimum>0.03</minimum>` to `<minimum>0.05</minimum>`.

Then find the `<limit>` block with `<counter>BRANCH</counter>`. Change `<minimum>0.01</minimum>` to `<minimum>0.03</minimum>`.

Both changes are in the `<rules><rule><limits>` section of the jacoco-maven-plugin configuration.
</action>
<verify>
grep -A2 "counter>LINE" backend-spring/pom.xml | grep "minimum" returns 0.05
grep -A2 "counter>BRANCH" backend-spring/pom.xml | grep "minimum" returns 0.03
</verify>
<acceptance_criteria>
- pom.xml contains `<minimum>0.05</minimum>` for LINE counter
- pom.xml contains `<minimum>0.03</minimum>` for BRANCH counter
- No other JaCoCo configuration values are changed
</acceptance_criteria>
</task>

<task>
<type>verify</type>
<files>
  - backend-spring/pom.xml
</files>
<action>
Run `cd backend-spring && ./mvnw verify -q` to verify JaCoCo check passes with new thresholds. The build should succeed because Phase 42 E2E tests added coverage that exceeds these new thresholds.
</action>
<verify>
mvn verify exits with code 0 (build passes)
JaCoCo report shows LINE coverage >= 5% and BRANCH coverage >= 3%
</verify>
<acceptance_criteria>
- `mvn verify` succeeds with exit code 0
- JaCoCo check does not fail on coverage thresholds
- Coverage report generated at target/site/jacoco/index.html
</acceptance_criteria>
</task>

</tasks>

<verification>
Run `cd backend-spring && ./mvnw verify` and confirm:
1. JaCoCo check passes (no "Coverage check failed" error)
2. BUILD SUCCESS in output
3. JaCoCo report at target/site/jacoco/index.html shows LINE >= 5%, BRANCH >= 3%
</verification>

<success_criteria>
1. pom.xml LINE minimum changed from 0.03 to 0.05 ✓
2. pom.xml BRANCH minimum changed from 0.01 to 0.03 ✓
3. `mvn verify` succeeds with new thresholds ✓
4. JaCoCo report confirms coverage exceeds new thresholds ✓
</success_criteria>
