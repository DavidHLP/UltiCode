---
wave: 1
depends_on: ["41-01"]
files_modified:
  - "backend-spring/src/test/java/com/ulticode/modules/auth/controller/RateLimitIntegrationTest.java"
autonomous: false
---

# Phase 42: Rate Limiting E2E Tests — Plan 42-01

**Requirement:** TEST-01

**Goal:** Verify rate limiting via E2E tests with Testcontainers Redis

**Success Criteria (what must be TRUE):**
1. E2E test class runs with Testcontainers Redis container
2. Rate-limited endpoint returns 429 after exceeding limit
3. Each test flushes Redis keys to avoid false 429s
4. Auth endpoint rate limit tier verified (auth/register = 5/min)

---

## Context

- **Phase 41** added `testcontainers-redis:2.2.2` dependency to `pom.xml`
- `RateLimitAspect` uses Redis Lua script for atomic INCR + EXPIRE on keys `rate-limit:<key>:<ip>`
- `POST /auth/register` has `@RateLimit(key = "register", limit = 5, period = 60)` — 5 requests per 60 seconds
- `POST /auth/login` has `@RateLimit(key = "login", limit = 10, period = 60)` — 10 requests per 60 seconds
- Testcontainers Redis shared via static `@Container` field (same pattern as MySQL testcontainer)
- `StringRedisTemplate` available in Spring test context for Redis key flushing

---

## Tasks

### Task 1: Create RateLimitIntegrationTest.java

<read_first>
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/src/main/java/com/ulticode/modules/auth/controller/AuthController.java`
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/src/main/java/com/ulticode/common/aspect/RateLimitAspect.java`
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/src/main/java/com/ulticode/common/annotation/RateLimit.java`
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/pom.xml` (lines 199-204: testcontainers-redis dependency)
</read_first>

<action>
Create file: `backend-spring/src/test/java/com/ulticode/modules/auth/controller/RateLimitIntegrationTest.java`

The test class must include:
1. `@SpringBootTest` + `@AutoConfigureMockMvc` — full Spring context, real Redis, real security filters
2. Static `@Container` field for shared Redis testcontainer: `new RedisContainer<>("redis:7-alpine").withExposedPorts(6379)`
3. `@DynamicPropertySource` method that overrides `spring.data.redis.host` and `spring.data.redis.port` with the container's mapped port
4. `StringRedisTemplate` injection for Redis key management
5. `@BeforeEach` method that flushes `rate-limit:*` keys before each test using `stringRedisTemplate.execute(new GenericRedisScript("FLUSHDB"))` or `stringRedisTemplate.getConnectionFactory().getConnection().flushDb()`
6. Two nested test classes:
   - `RegisterRateLimitTests` — sends 6 unique `POST /auth/register` requests (unique username/email per request using `System.nanoTime()`), verifies 6th returns 200/201, then sends 7th and verifies it returns 429 with `status().isTooManyRequests()`
   - `LoginRateLimitTests` — sends 11 unique `POST /auth/login` requests, verifies 11th returns 429 (limit is 10/min for login)
7. Use `objectMapper.writeValueAsString()` with `RegisterDTO`/`LoginDTO` for request bodies
8. Assert response `code` is non-zero on 429 and `message` contains rate limit text
9. Test data: usernames like `ratelimit-register-{nanoTime}`, emails like `ratelimit-{nanoTime}@test.com` to avoid "user already exists" false failures

RegisterDTO has fields: `username`, `email`, `password`
LoginDTO has fields: `username`, `password`
</action>

<acceptance_criteria>
- [ ] `RateLimitIntegrationTest.java` file exists at correct path
- [ ] Grep: `class RateLimitIntegrationTest` present in file
- [ ] Grep: `@Container` annotation present with Redis container
- [ ] Grep: `@DynamicPropertySource` method overrides spring.data.redis properties
- [ ] Grep: `FLUSHDB` or `flushDb` present for Redis key flushing in `@BeforeEach`
- [ ] Grep: `@SpringBootTest` and `@AutoConfigureMockMvc` annotations present
- [ ] Grep: `rate-limit` key pattern used for Redis flush
- [ ] Grep: `isTooManyRequests()` assertion present (HTTP 429)
- [ ] Grep: `RegisterDTO` and `LoginDTO` used for request bodies
- [ ] Grep: `System.nanoTime()` used in username/email to ensure uniqueness
</acceptance_criteria>

---

### Task 2: Verify compilation and test run

<read_first>
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/pom.xml` (lines 199-204: testcontainers-redis)
- `/home/davidhlp/project/UltiCode-Public-Next/backend-spring/src/test/java/com/ulticode/modules/auth/controller/RateLimitIntegrationTest.java` (the file created in Task 1)
</read_first>

<action>
Run compilation check to ensure the test class compiles correctly, then run the test:

```bash
cd /home/davidhlp/project/UltiCode-Public-Next/backend-spring && ./mvnw test -Dtest=RateLimitIntegrationTest -DfailIfNoTests=false -q 2>&1 | tail -30
```

If compilation fails, fix the import errors or annotation issues based on the error output.
</action>

<acceptance_criteria>
- [ ] `mvn test -Dtest=RateLimitIntegrationTest` exits with exit code 0 (tests pass, or skip if no tests found yet)
- [ ] No compilation errors in the output
- [ ] Test execution output contains "Redis" (container startup) or "RateLimitIntegrationTest" (test run)
</acceptance_criteria>

---

## Verification Criteria

After both tasks complete, the following must be TRUE:

1. **File exists:** `backend-spring/src/test/java/com/ulticode/modules/auth/controller/RateLimitIntegrationTest.java`
2. **Testcontainers Redis wired:** `@Container` static Redis starts and `@DynamicPropertySource` overrides `spring.data.redis.*` properties
3. **Redis flushed per test:** `@BeforeEach` flushes `rate-limit:*` keys so each test starts clean
4. **Register 5/min limit tested:** 6 requests succeed, 7th returns 429
5. **Login 10/min limit tested:** 11th request returns 429
6. **Unique test data:** Each request uses unique username/email (timestamp/nano appended) to avoid "user already exists" false failures
7. **Response structure verified:** 429 response has non-zero `code` and rate limit message
8. **mvn test passes:** `./mvnw test -Dtest=RateLimitIntegrationTest` exits 0

---

## Dependencies

- **Blocked by Phase 41** — requires `testcontainers-redis` dependency added to `pom.xml` (DEPS-02)
- Phase 41 must be completed before Phase 42 execution
