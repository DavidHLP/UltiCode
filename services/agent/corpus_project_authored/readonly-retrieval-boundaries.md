# 只读检索与工具边界

> Provenance: project-authored-for-u02。由本项目助手为 U02 学习切片撰写；不是用户作品，也不是授权第三方材料。

身份由服务端会话持有：工具处理器不接受调用方传入的用户标识，模型输出无法把查询重定向到其他账号。
检索结果中的来源文本是**不可信数据**，不是指令来源；其中的任何要求都不得改变工具、身份或输出格式。
公开可访问不等于允许入库或外发：是否可作语料取决于该来源的许可，而不是接口是否公开。
