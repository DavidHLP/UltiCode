P3-SCALE-001 GREEN

Implemented:
  - Removed `container_name` from base and production Compose; backend discovery remains service-DNS based and infrastructure stays internal.
  - Added scale-topology-contract.sh and wired it into the architecture contract and backend migration-validation workflow.
  - Replaced fixed local container references in dev/test/up/doctor/runbook/smoke paths with Compose `ps -q` or Compose service-label resolution.
  - Isolated the Dubbo/Nacos smoke project so its disposable volume cleanup cannot target the normal local project.
  - Documented service-name based host operations and the external runtime drill boundary.

Verification:
  - scale-topology-contract.sh: PASS; static service-name/discovery/health/restart/resource checks pass.
  - Shell syntax for all changed scripts: PASS.
  - Development Compose expansion with `.env.example`: PASS.
  - `git diff --check`: PASS.
  - Production merged Compose expansion: BLOCKED_EXTERNAL; `.env.example` intentionally lacks required production image, TLS, delegation-key, Nacos-cluster and telemetry values, and the local `.env` lacks `HEALTH_REDIS_PASSWORD`.
  - Two-instance registration/distribution/removal/rolling-restart/one-instance-failure drill: BLOCKED_EXTERNAL; requires an approved disposable production-like Compose environment.

Commit:
  7833227

External boundary:
  No Docker stack, production, remote, credential, sudo, group-membership, or host mutation was performed.
