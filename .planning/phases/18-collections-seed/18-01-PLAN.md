---
phase: 18
name: "Collections Seed (V25)"
objective: "Generate V25__collections_seed.sql with ~50 collections and ~200+ collection_items covering difficulty tiers, topic tags, company use cases, and contest types"
wave: 1
depends_on: []
requirements_addressed: [COL-01, COL-02, COL-03, COL-04]
autonomous: true
---

## Task 1: Generate V25__collections_seed.sql

<read_first>
- db-manager/migrations/V8__collection_schema.sql — collections + collection_items schemas, existing seed pattern
- db-manager/migrations/V15__featured_problem_lists.sql — problem_lists IDs for FK references (list-essentials, list-intervals, list-sliding-window, list-interview-100, list-database, list-concurrency, list-graph-advanced)
- .planning/phases/18-collections-seed/18-CONTEXT.md — all 8 implementation decisions
</read_first>

<acceptance_criteria>
- grep 'INSERT INTO.*collections' V25__collections_seed.sql | wc -l → ≥48 (≈50 target, some margin)
- grep 'INSERT INTO.*collection_items' V25__collections_seed.sql | wc -l → ≥150 (≥3 items per collection, some margin)
- grep "target_type.*'PROBLEM_LIST'" V25__collections_seed.sql | wc -l → ≥20
- grep "target_type.*'PROBLEM'" V25__collections_seed.sql | wc -l → ≥3 (difficulty tier collections use PROBLEM)
- grep "icon.*'[A-Za-z]'" V25__collections_seed.sql | wc -l → ≥48 (icon column populated)
- grep "color.*'[a-z]*'" V25__collections_seed.sql | wc -l → ≥48 (color column populated)
- grep "SET FOREIGN_KEY_CHECKS=0" V25__collections_seed.sql → 1 occurrence
- grep "SET FOREIGN_KEY_CHECKS=1" V25__collections_seed.sql → 1 occurrence
- grep "START TRANSACTION" V25__collections_seed.sql → 1 occurrence
- grep "COMMIT" V25__collections_seed.sql → 1 occurrence
</acceptance_criteria>

<action>
Generate `db-manager/migrations/V25__collections_seed.sql` following the seed pattern:

Structure:
```
SET FOREIGN_KEY_CHECKS=0;
START TRANSACTION;

-- Header comment with phase info

-- ============================================================================
-- Difficulty Collections (3): Easy, Medium, Hard
-- References individual PROBLEMS directly (target_type='PROBLEM')
-- ============================================================================
-- Easy: 算法入门精选 (icon=Star, color=emerald)
INSERT INTO `collections` (...) VALUES (UUID(),'user-alex','算法入门精选','...','Star','emerald',...);
-- Collection items: reference individual problem IDs directly

-- Medium: 算法进阶挑战 (icon=Flame, color=amber)
-- Hard: 专家级精选 (icon=Zap, color=rose)

-- ============================================================================
-- Topic/Tag Collections (~20): Array, String, Tree, Graph, DP, etc.
-- References PROBLEM_LIST entries (target_type='PROBLEM_LIST')
-- ============================================================================

-- ============================================================================
-- Company/Use Case Collections (~15): FANG, FLAGM, BAT, etc.
-- References PROBLEM_LIST entries
-- ============================================================================

-- ============================================================================
-- Contest Type Collections (~5): Hot 100, 剑指Offer, etc.
-- References PROBLEM_LIST entries
-- ============================================================================

-- ============================================================================
-- Featured List Collections (~3): Direct references to V15 lists
-- list-interview-100, list-database, list-concurrency, list-graph-advanced
-- with icon+color styling
-- ============================================================================

COMMIT;
SET FOREIGN_KEY_CHECKS=1;
```

Collection distribution target (~50 total):
- Difficulty (3): Easy/Medium/Hard tier — uses target_type='PROBLEM'
- Topic (18): Array, String, String Two Pointers, Hash Table, Tree, Binary Tree, Graph, DFS/BFS, Dynamic Programming, Greedy, Binary Search, Sliding Window, Stack, Queue, Heap, Backtracking, Divide & Conquer, Bit Manipulation
- Company (15): FANG, FLAGM, BAT+字节+美团, Startup, 竞赛入门, ACM进阶, 华为面试, 腾讯面试, 阿里面试, 字节面试, 美团面试, 京东面试, 微软面试, 谷歌面试, Meta面试
- Contest (5): LeetCode Hot 100, 剑指Offer, 周赛精选, 双周赛压轴, 面试算法100
- Featured (3): list-interview-100, list-database, list-concurrency — direct references

Icon mapping:
- Difficulty Easy: Star, emerald
- Difficulty Medium: Flame, amber
- Difficulty Hard: Zap, rose
- Topics: alternating sky/teal/orange/cyan colors, icons: Square(Type), Type(String), GitBranch(Tree), Share2(Graph), Layers(DP), Undo2(Backtracking), Search(Binary Search), ListOrdered(Stack/Queue), ArrowUpDown(Heap)
- Company: Building2, violet
- Contest: Trophy, amber
- Featured: Trophy, amber

FK targets (from V15 problem_lists):
- list-essentials
- list-intervals
- list-sliding-window
- list-interview-100
- list-database
- list-concurrency
- list-graph-advanced

Admin user IDs: user-alex, user-david, user-sara, user-chen (assign to collections randomly but consistently)

Each collection: UUID(), name, description, icon, color, sort_order, is_default=0, created_at=NOW(3), updated_at=NOW(3)

Each collection_item: UUID(), collection_id (ref to above), target_id (problem_list id or problem int), target_type ('PROBLEM_LIST' or 'PROBLEM'), sort_order, note (optional), created_at=NOW(3)

Note: Do NOT use is_default=1 for any V25 collection (those are reserved for user-private favorites from V8)
</action>
