---
phase: 07-code-quality-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-spring/src/main/java/com/ulticode/infrastructure/redis/RedisService.java
  - backend-spring/src/main/java/com/ulticode/modules/monitoring/service/impl/MonitoringServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/auth/service/OAuthService.java
  - backend-spring/src/main/java/com/ulticode/modules/recommendation/service/RecommendationDataService.java
  - backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java
  - backend-spring/src/main/java/com/ulticode/security/jwt/JwtTokenProvider.java
  - backend-spring/src/main/java/com/ulticode/security/jwt/JwtAuthenticationFilter.java
  - backend-spring/src/main/java/com/ulticode/websocket/WebSocketAuthChannelInterceptor.java
  - backend-spring/src/main/java/com/ulticode/modules/websocket/interceptor/JwtChannelInterceptor.java
  - backend-spring/src/main/java/com/ulticode/modules/recommendation/scheduler/RecommendationScheduler.java
  - backend-spring/src/main/java/com/ulticode/modules/recommendation/controller/RecommendationDataController.java
  - backend-spring/src/main/java/com/ulticode/modules/recommendation/service/impl/RecommendationServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/search/service/impl/SearchServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/problemlist/service/impl/ProblemListServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/queue/service/impl/QueueServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/backup/scheduler/BackupScheduler.java
  - backend-spring/src/main/java/com/ulticode/modules/backup/service/impl/BackupServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminCommentServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminSolutionServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminUserServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminSubmissionServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminForumServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/i18n/service/impl/I18nServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/moderation/service/impl/ModerationServiceImpl.java
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "No catch(Exception e) or catch(Throwable e) blocks remain in production backend code"
    - "Every catch block targets specific exception types OR has a D-03 comment explaining why broad catch is intentional"
    - "No catch(Throwable e) blocks exist anywhere -- JVM errors propagate"
    - "Backend compiles cleanly after all exception handling changes"
  artifacts:
    - path: "backend-spring/src/main/java/com/ulticode/infrastructure/redis/RedisService.java"
      provides: "Redis infrastructure wrapper with D-03 commented broad catches"
      contains: "broad catch:"
    - path: "backend-spring/src/main/java/com/ulticode/modules/auth/service/OAuthService.java"
      provides: "OAuth service with specific JsonProcessingException catches"
      contains: "JsonProcessingException"
    - path: "backend-spring/src/main/java/com/ulticode/security/jwt/JwtTokenProvider.java"
      provides: "JWT provider with specific JWT exception multi-catches"
      contains: "ExpiredJwtException"
  key_links:
    - from: "OAuthService.java"
      to: "BusinessException"
      via: "rethrow as BusinessException instead of RuntimeException"
      pattern: "BusinessException"
    - from: "JwtTokenProvider.java"
      to: "JWT exception types"
      via: "multi-catch for ExpiredJwtException | MalformedJwtException | SignatureException"
      pattern: "ExpiredJwtException.*MalformedJwtException"
---

<objective>
Replace all broad catch(Exception e) and catch(Throwable e) blocks with specific exception types across the backend codebase.

Purpose: Broad exception catches hide real failure modes and make debugging harder. Precise exception handling surfaces actual errors and prevents silent failures.
Output: 26 backend files with refined exception handling, compilation verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-code-quality-dependencies/07-CONTEXT.md
@.planning/phases/07-code-quality-dependencies/07-RESEARCH.md
</context>

<interfaces>
<!-- Existing exception hierarchy that executors must use -->

From backend-spring/src/main/java/com/ulticode/common/exception/BusinessException.java:
```java
// BusinessException wraps domain errors with ErrorCode enum
// Usage: throw new BusinessException(ErrorCode.UNKNOWN_ERROR, "message", cause);
```

From backend-spring/src/main/java/com/ulticode/common/exception/ErrorCode.java:
```java
// Error codes include: UNKNOWN_ERROR, AUTH_INVALID_CREDENTIALS, AUTH_TOKEN_EXPIRED,
// UNAUTHORIZED, INTERNAL_ERROR, etc.
```

From backend-spring/src/main/java/com/ulticode/common/exception/GlobalExceptionHandler.java:
```java
// Handles BusinessException, validation errors, auth errors, and generic Exception
// Generic handler returns "Unknown error" -- never ex.getMessage()
```

From backend-spring/src/main/java/com/ulticode/modules/admin/service/AdminAnalyticsService.java:
```java
public interface AdminAnalyticsService {
    UserActivityReportVO getUserActivityReport(Integer days);
    ProblemCompletionReportVO getProblemCompletionReport(Integer days);
    ContestParticipationReportVO getContestParticipationReport(Integer days);
    RevenueReportVO getRevenueReport(Integer days);
    PerformanceReportVO getPerformanceReport();
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace broad catches with specific exception types (Category E targets)</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/auth/service/OAuthService.java,
    backend-spring/src/main/java/com/ulticode/security/jwt/JwtTokenProvider.java,
    backend-spring/src/main/java/com/ulticode/security/jwt/JwtAuthenticationFilter.java,
    backend-spring/src/main/java/com/ulticode/websocket/WebSocketAuthChannelInterceptor.java,
    backend-spring/src/main/java/com/ulticode/modules/websocket/interceptor/JwtChannelInterceptor.java,
    backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java,
    backend-spring/src/main/java/com/ulticode/modules/backup/service/impl/BackupServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/recommendation/service/impl/RecommendationServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/recommendation/controller/RecommendationDataController.java,
    backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminUserServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/i18n/service/impl/I18nServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/monitoring/service/impl/MonitoringServiceImpl.java
  </files>
  <action>
Per D-01 and D-02, replace broad catch(Exception e) with specific exception types in the following files. For each catch block, analyze the try body to determine what exceptions are actually thrown:

1. **OAuthService.java (4 catches at lines 103, 127, 187, 208):** All parse JSON from OAuth providers. Replace with `catch (JsonProcessingException e)` and throw `BusinessException(ErrorCode.AUTH_INVALID_CREDENTIALS, "OAuth token exchange failed", e)` instead of `RuntimeException`. Add import for `com.fasterxml.jackson.core.JsonProcessingException`.

2. **JwtTokenProvider.java (1 catch at line 187):** Replace with multi-catch: `catch (ExpiredJwtException e)` throwing `BusinessException(ErrorCode.AUTH_TOKEN_EXPIRED)`, then `catch (MalformedJwtException | SignatureException e)` throwing `BusinessException(ErrorCode.UNAUTHORIZED, "Invalid token")`. Note: lines 99-105 and 159-165 already use specific catches -- leave those alone.

3. **JwtAuthenticationFilter.java (1 catch at line 82):** Same JWT exception pattern as JwtTokenProvider line 187. Replace with `catch (ExpiredJwtException e)` and `catch (MalformedJwtException | SignatureException e)`.

4. **WebSocketAuthChannelInterceptor.java (1 catch at line 61):** JWT parsing in WebSocket handshake. Replace with same JWT multi-catch pattern.

5. **JwtChannelInterceptor.java (2 catches at lines 73, 97):** JWT parsing in channel interceptor. Replace with same JWT multi-catch pattern. Note: lines 69 and 93 already catch `WebSocketAuthenticationException` -- keep those.

6. **CodeExecutionService.java (1 catch at line 436):** Process execution. Replace with `catch (IOException | InterruptedException e)`. If interrupted, restore the interrupt flag with `Thread.currentThread().interrupt()`.

7. **BackupServiceImpl.java (1 catch at line 324):** Process execution for backup. Replace with `catch (IOException | InterruptedException e)`. Restore interrupt flag if interrupted. Note: lines 220 and 238 already use specific catches -- leave those.

8. **RecommendationServiceImpl.java (2 catches at lines 146, 180):** Dubbo RPC calls. Replace with `catch (RpcException e)` and throw `BusinessException(ErrorCode.UNKNOWN_ERROR, "Recommendation service unavailable", e)`. Add import for `org.apache.dubbo.rpc.RpcException`.

9. **RecommendationDataController.java (2 catches at lines 41, 55):** These wrap service calls in try-catch then wrap in Result.failure(). Per research, these should simply let exceptions propagate to `GlobalExceptionHandler`. Remove the try-catch blocks entirely and let the service methods throw -- the GlobalExceptionHandler will produce the correct Result response.

10. **AdminUserServiceImpl.java (4 catches at lines 113, 170, 188, 210):** File I/O operations for avatar upload and CSV export. Analyze each try block: if it does file operations, use `catch (IOException e)`; if it wraps DB queries, add D-03 comment (per D-03, keep broad catch with explanation).

11. **I18nServiceImpl.java (1 catch at line 93):** File I/O for i18n resource loading. Replace with `catch (IOException e)`.

12. **MonitoringServiceImpl.java (2 catches at lines 66, 133):** `InetAddress.getLocalHost()` calls. Replace with `catch (UnknownHostException e)`. The remaining catches in this file (lines 158, 208, 256, 289, 321, 350, 360, 370, 419, 430) are health checks -- handle those in Task 2.

Per D-04: Search for any `catch(Throwable` patterns and remove them entirely -- JVM errors must propagate.
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/backend-spring && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>
All 26 Category E catch blocks replaced with specific exception types or removed (RecommendationDataController). No catch(Throwable e) blocks remain. Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add D-03 comments to legitimate broad catches (Categories A, B, C)</name>
  <files>
    backend-spring/src/main/java/com/ulticode/infrastructure/redis/RedisService.java,
    backend-spring/src/main/java/com/ulticode/modules/monitoring/service/impl/MonitoringServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/search/service/impl/SearchServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/problemlist/service/impl/ProblemListServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/queue/service/impl/QueueServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/backup/scheduler/BackupScheduler.java,
    backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminForumServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminCommentServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminSolutionServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/admin/service/impl/AdminSubmissionServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/moderation/service/impl/ModerationServiceImpl.java,
    backend-spring/src/main/java/com/ulticode/modules/recommendation/scheduler/RecommendationScheduler.java,
    backend-spring/src/main/java/com/ulticode/modules/recommendation/service/RecommendationDataService.java
  </files>
  <action>
Per D-03, for catch blocks where the try body genuinely can throw many exception types and they all map to the same error response, keep `catch (Exception e)` BUT add a comment on the line above: `// broad catch: all failures map to same error response`.

For each file:

1. **RedisService.java (30 catches):** All are infrastructure boundary wrappers around RedisTemplate. Add D-03 comment `// broad catch: all Redis failures map to same degraded response (infrastructure boundary)` before each catch. Also replace `throw new RuntimeException(...)` with `throw new BusinessException(ErrorCode.UNKNOWN_ERROR, "Redis operation failed", e)` where applicable. Add `import com.ulticode.common.exception.BusinessException;` and `import com.ulticode.common.exception.ErrorCode;` if not present.

2. **MonitoringServiceImpl.java (10 remaining catches at lines 158, 208, 256, 289, 321, 350, 360, 370, 419, 430):** Health check methods where any failure = unhealthy. Add D-03 comment: `// broad catch: any failure means service is unhealthy`. Leave the 2 catches at lines 66, 133 alone -- Task 1 handles those.

3. **SearchServiceImpl.java (2 catches at lines 63, 146):** MeiliSearch to DB fallback pattern. Add D-03 comment: `// broad catch: fallback to database search on MeiliSearch failure`.

4. **SubmissionServiceImpl.java (1 catch at line 116):** Queue enqueue failure fallback. Add D-03 comment: `// broad catch: enqueue failure falls back to system error status`.

5. **ProblemListServiceImpl.java (1 catch at line 102):** Categories table may not exist. Add D-03 comment: `// broad catch: table may not exist in all deployments`.

6. **QueueServiceImpl.java (2 catches at lines 71, 124):** Queue operation wrappers. Add D-03 comment: `// broad catch: all queue failures map to same error response`.

7. **BackupScheduler.java (1 catch at line 33):** Scheduled backup failure. Add D-03 comment: `// broad catch: scheduler resilience -- log and continue`.

8. **AdminForumServiceImpl.java (1 catch at line 236):** DB query wrapper. Add D-03 comment: `// broad catch: all failures map to same error response`.

9. **AdminCommentServiceImpl.java (1 catch at line 241):** DB query wrapper. Add D-03 comment: `// broad catch: all failures map to same error response`.

10. **AdminSolutionServiceImpl.java (1 catch at line 226):** DB query wrapper. Add D-03 comment: `// broad catch: all failures map to same error response`.

11. **AdminSubmissionServiceImpl.java (1 catch at line 309):** DB query wrapper. Add D-03 comment: `// broad catch: all failures map to same error response`.

12. **ModerationServiceImpl.java (1 catch at line 225):** DB query wrapper. Add D-03 comment: `// broad catch: all failures map to same error response`.

13. **RecommendationScheduler.java (4 catches at lines 63, 105, 131, 177):** Scheduler resilience. Add D-03 comment: `// broad catch: scheduler resilience -- log failure and continue`.

14. **RecommendationDataService.java (4 catches at lines 93, 123, 157, 215):** Dubbo/data collection wrappers. Add D-03 comment: `// broad catch: data collection failure -- log and use defaults`.

After all changes, verify no `catch(Throwable` patterns exist anywhere.
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/backend-spring && ./mvnw compile -q 2>&1 | tail -5 && echo "---COMPILER_OK---" && grep -rn "catch.*Exception e" src/main/java/ | grep -v "broad catch\|IOException\|InterruptedException\|JsonProcessingException\|ExpiredJwtException\|MalformedJwtException\|SignatureException\|MessagingException\|UnknownHostException\|RpcException\|NumberFormatException\|NoSuchAlgorithm\|WebSocketAuthenticationException\|UnsupportedJwtException\|IllegalArgumentException" | wc -l</automated>
  </verify>
  <done>
All Category A/B/C broad catches have D-03 explanatory comments. No catch(Throwable e) blocks exist anywhere. All remaining catch(Exception e) blocks have a preceding D-03 comment. Backend compiles cleanly. Grep for uncommented broad catches returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuthService -> GitHub/Google APIs | External HTTP responses parsed as JSON |
| JwtTokenProvider -> Client tokens | Untrusted JWT strings from HTTP requests |
| CodeExecutionService -> Process | OS process execution for code judging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | OAuthService catch blocks | mitigate | Specific JsonProcessingException catch prevents leaking stack traces from unexpected exceptions |
| T-07-02 | Information Disclosure | JwtTokenProvider catch blocks | mitigate | Specific JWT exception multi-catch gives precise error codes instead of generic "auth failed" |
| T-07-03 | Denial of Service | CodeExecutionService InterruptedException | mitigate | Restore interrupt flag with Thread.currentThread().interrupt() when catching InterruptedException |
</threat_model>

<verification>
1. Backend compilation succeeds: `cd backend-spring && ./mvnw compile`
2. No uncommented broad catches remain: `grep -rn "catch.*Exception e" backend-spring/src/main/java/ | grep -v "broad catch" | grep -v "specific exception types" | wc -l` returns 0
3. No catch(Throwable anywhere: `grep -rn "catch.*Throwable" backend-spring/src/main/java/ | wc -l` returns 0
4. All D-03 commented catches are legitimate infrastructure/fallback patterns
</verification>

<success_criteria>
- All 84 broad catch blocks are either replaced with specific types or have D-03 explanatory comments
- Zero catch(Throwable e) blocks exist in production code
- `./mvnw compile` succeeds with no errors
- Grep for uncommented `catch(Exception e)` returns zero results
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-quality-dependencies/07-01-SUMMARY.md`
</output>
