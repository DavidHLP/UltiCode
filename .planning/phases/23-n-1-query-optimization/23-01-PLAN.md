---
phase: 23-n-1-query-optimization
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java
  - backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java
  - backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java
  - backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java
  - backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - Contest rankings page loads all participants with user info in single query
    - Problem list page loads tags and difficulty in single query (not lazy load)
    - Submission list page loads problem title/slug in single query with JOIN FETCH
  artifacts:
    - path: backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java
      provides: JOIN FETCH query for rankings with user data
      min_lines: 3
    - path: backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java
      provides: Batch query for tags
      min_lines: 3
    - path: backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java
      provides: JOIN FETCH query for submissions with problem data
      min_lines: 3
  key_links:
    - from: ContestParticipantMapper.java
      to: RankingServiceImpl.java
      via: new mapper method called in getContestRanking
    - from: ProblemMapper.java
      to: ProblemServiceImpl.java
      via: new mapper method called in paged queries
    - from: SubmissionMapper.java
      to: SubmissionServiceImpl.java
      via: new mapper method called in findByUserId/findByProblemId
---

<objective>
Eliminate N+1 database queries for contest rankings, problem list, and submission list by adding JOIN FETCH queries to the mappers and wiring them into the services.
</objective>

<context>
@backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java
@backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java
@backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java
@backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java
@backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java
@backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JOIN FETCH query to ContestParticipantMapper for rankings with user data (PERF-01)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java</files>
  <action>
Add a new method `selectParticipantsWithUserByContestId` using @Select annotation with JOIN FETCH on the users table to load participant + username/name/avatar in a single query.

```java
@Select("SELECT cp.*, u.username, u.name, u.avatar " +
        "FROM contest_participants cp " +
        "LEFT JOIN users u ON cp.user_id = u.id " +
        "WHERE cp.contest_id = #{contestId} " +
        "ORDER BY cp.final_rank ASC, cp.total_score DESC, cp.total_penalty ASC")
List<ContestParticipantWithUser> selectParticipantsWithUserByContestId(@Param("contestId") String contestId);
```

Create a inner static record or DTO `ContestParticipantWithUser` in the mapper file to hold: all ContestParticipant fields + username, name, avatar.

Add a @Results annotation to map the joined columns to the DTO fields. The participant fields map by default (column/field name match), the user fields map via explicit @Result mappings.

<read_first>backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java</read_first>
<acceptance_criteria>
grep -c "selectParticipantsWithUserByContestId" backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java | grep -q "1"
grep -c "JOIN FETCH" backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java | grep -q "1"
grep -c "ContestParticipantWithUser" backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java | grep -q "1"
</acceptance_criteria>
  <verify>grep -c "selectParticipantsWithUserByContestId" backend-spring/src/main/java/com/ulticode/modules/contest/mapper/ContestParticipantMapper.java</verify>
  <done>ContestParticipantMapper has selectParticipantsWithUserByContestId with JOIN FETCH and @Results mapping</done>
</task>

<task type="auto">
  <name>Task 2: Add batch tag query to ProblemMapper for problem list (PERF-02)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java</files>
  <action>
Add a new method `selectTagsByProblemIds` using @Select annotation with IN clause to batch-fetch all tag relations for a list of problem IDs in a single query.

```java
@Select("<script>" +
        "SELECT ptr.problem_id, pt.name as tag_name " +
        "FROM problem_tag_relations ptr " +
        "LEFT JOIN problem_tags pt ON ptr.tag_id = pt.id " +
        "WHERE ptr.problem_id IN " +
        "<foreach collection='problemIds' item='id' open='(' separator=',' close=')'>" +
        "#{id}</foreach>" +
        "</script>")
List<ProblemTagDTO> selectTagsByProblemIds(@Param("problemIds") List<Long> problemIds);
```

Create a inner static record `ProblemTagDTO` in the mapper file with fields: problemId (Long), tagName (String).

Add @Results to map `problem_id` -> problemId and `tag_name` -> tagName.

<read_first>backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java</read_first>
<acceptance_criteria>
grep -c "selectTagsByProblemIds" backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java | grep -q "1"
grep -c "problem_tag_relations" backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java | grep -q "1"
grep -c "ProblemTagDTO" backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java | grep -q "1"
</acceptance_criteria>
  <verify>grep -c "selectTagsByProblemIds" backend-spring/src/main/java/com/ulticode/modules/problem/mapper/ProblemMapper.java</verify>
  <done>ProblemMapper has selectTagsByProblemIds batch query method</done>
</task>

<task type="auto">
  <name>Task 3: Add JOIN FETCH query to SubmissionMapper for submission list with problem data (PERF-03)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java</files>
  <action>
Add a new method `findByUserIdWithProblem` using @Select annotation with JOIN FETCH on problems table to load submission + problem title/slug in a single query.

```java
@Select("SELECT s.*, p.title as problem_title, p.slug as problem_slug " +
        "FROM submissions s " +
        "LEFT JOIN problems p ON s.problem_id = p.id " +
        "WHERE s.user_id = #{userId} " +
        "ORDER BY s.created_at DESC")
List<SubmissionWithProblem> findByUserIdWithProblem(@Param("userId") String userId, org.baomidou.mybatisplus.extension.plugins.pagination.Page page);
```

Create a inner static record `SubmissionWithProblem` in the mapper file with fields: all Submission fields + problemTitle (String), problemSlug (String).

Add @Results annotation mapping: problem_title -> problemTitle, problem_slug -> problemSlug.

Add similar method `findByProblemIdWithProblem` for findByProblemId variant.

<read_first>backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java</read_first>
<acceptance_criteria>
grep -c "findByUserIdWithProblem" backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java | grep -q "1"
grep -c "JOIN FETCH" backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java | grep -q "1"
grep -c "SubmissionWithProblem" backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java | grep -q "1"
</acceptance_criteria>
  <verify>grep -c "findByUserIdWithProblem" backend-spring/src/main/java/com/ulticode/modules/submission/mapper/SubmissionMapper.java</verify>
  <done>SubmissionMapper has findByUserIdWithProblem with JOIN FETCH and @Results mapping</done>
</task>

<task type="auto">
  <name>Task 4: Wire JOIN FETCH into RankingServiceImpl.getContestRanking (PERF-01)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java</files>
  <action>
Update RankingServiceImpl to use the new `selectParticipantsWithUserByContestId` method instead of `selectPage` + stream map.

Replace lines 37-48 in getContestRanking:
- Call `participantMapper.selectParticipantsWithUserByContestId(contestId)` to get List<ContestParticipantWithUser>
- In toRankingVO, populate username/name/avatar from the DTO fields directly instead of leaving them null
- Implement proper pagination: apply `page * limit` skip and `limit` take on the list, or switch to a paginated SQL version

If pagination is needed, add a `selectParticipantsWithUserByContestIdPaged` variant that accepts LIMIT/OFFSET parameters.

<read_first>backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java</read_first>
<acceptance_criteria>
grep -c "selectParticipantsWithUserByContestId" backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java | grep -q "1"
grep -c "problemMapper.selectById" backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java | grep -q "0"
</acceptance_criteria>
  <verify>grep -c "selectParticipantsWithUserByContestId" backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java</verify>
  <done>RankingServiceImpl.getContestRanking uses JOIN FETCH query, no N+1 problemMapper calls</done>
</task>

<task type="auto">
  <name>Task 5: Wire batch tag fetch into ProblemServiceImpl.paged queries (PERF-02)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java</files>
  <action>
Update ProblemServiceImpl to use batch tag loading after fetching problem page.

In the paged query method (around line 126-134):
1. Execute `problemMapper.selectPage(problemPage, queryWrapper)` as before
2. Extract problem IDs from result.getRecords()
3. Call `problemMapper.selectTagsByProblemIds(problemIds)` to get List<ProblemTagDTO>
4. Group the DTOs by problemId into a Map<Long, List<String>> (tag names)
5. In toVO, set `vo.setTags(tagMap.getOrDefault(problem.getId(), List.of()))` instead of empty list

<read_first>backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java</read_first>
<acceptance_criteria>
grep -c "selectTagsByProblemIds" backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java | grep -q "1"
grep -c "problemTagRelationMapper" backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java | grep -q "0"
</acceptance_criteria>
  <verify>grep -c "selectTagsByProblemIds" backend-spring/src/main/java/com/ulticode/modules/problem/service/impl/ProblemServiceImpl.java</verify>
  <done>ProblemServiceImpl.paged queries use batch tag loading instead of N+1</done>
</task>

<task type="auto">
  <name>Task 6: Wire JOIN FETCH into SubmissionServiceImpl.findByUserId/findByProblemId (PERF-03)</name>
  <files>backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java</files>
  <action>
Update SubmissionServiceImpl to use the new JOIN FETCH methods instead of calling `problemMapper.selectById` per submission.

In toVO (around line 305):
- Check if the submission has problemTitle/problemSlug pre-loaded (from SubmissionWithProblem)
- If available, use them directly to build SubmissionVO.ProblemInfo
- If not available (called from findById which uses plain selectById), fall back to problemMapper.selectById as before

In findByUserId and findByProblemId:
- Replace `submissionMapper.findByUserId(pageParam, userId)` with `submissionMapper.findByUserIdWithProblem(userId, pageParam)`
- Update the stream mapping to use the enriched data from SubmissionWithProblem

<read_first>backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java</read_first>
<acceptance_criteria>
grep -c "findByUserIdWithProblem\|findByProblemIdWithProblem" backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java | grep -q "1"
</acceptance_criteria>
  <verify>grep -c "findByUserIdWithProblem" backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SubmissionServiceImpl.java</verify>
  <done>SubmissionServiceImpl uses JOIN FETCH methods for list queries, reducing problem N+1</done>
</task>

</tasks>

<verification>
cd backend-spring && ./mvnw compile -q 2>&1 | head -20
</verification>

<success_criteria>
1. ContestParticipantMapper has selectParticipantsWithUserByContestId with JOIN FETCH returning username/name/avatar
2. ProblemMapper has selectTagsByProblemIds batch query returning problemId + tagName pairs
3. SubmissionMapper has findByUserIdWithProblem with JOIN FETCH returning problem title/slug
4. RankingServiceImpl.getContestRanking uses the JOIN FETCH method, eliminating user N+1
5. ProblemServiceImpl.paged queries use batch tag loading, eliminating tag N+1
6. SubmissionServiceImpl.findByUserId/findByProblemId use JOIN FETCH, eliminating problem N+1 per row
7. All mappers compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-n-1-query-optimization/23-01-SUMMARY.md`
</output>
