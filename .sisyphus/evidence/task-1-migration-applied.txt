# V28 Migration Evidence - Add version column to problem_lists

## Date: 2026-05-07

## Migration Applied
- SQL executed directly via docker exec:
  ```sql
  ALTER TABLE problem_lists ADD COLUMN version INT NOT NULL DEFAULT 1 AFTER banner_order;
  CREATE INDEX idx_version ON problem_lists (version);
  ```

## Verification Results

### Column exists in problem_lists table:
```
Field   Type    Null    Key     Default    Extra
version int     NO      MUL     1
```

### Index exists:
```
Table           Non_unique  Key_name    Column_name
problem_lists   1           idx_version  version
```

## API Test Results (CORRECT ENDPOINT)

### Endpoint Tested: GET /admin/problem-lists?page=1&limit=10
### Authentication: Cookie-based (admin login)

**Response:**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [...9 problem lists...],
    "total": 9,
    "page": 1,
    "pageSize": 10,
    "totalPages": 1
  }
}
```

**Result:** ✅ SUCCESS - `code=0`, 9 items returned, no 500 error

## Conclusion
- ✅ Migration applied successfully (via docker exec directly)
- ✅ `version` column added with correct type (INT NOT NULL DEFAULT 1)
- ✅ `idx_version` index created
- ✅ `/admin/problem-lists` API returns HTTP 200 with valid data
- ✅ Original 500 error FIXED

## Note
- db-manager CLI could not be used (Flyway not configured in this database)
- Migration was applied directly via MySQL docker exec
- No git files were modified (database change only)
