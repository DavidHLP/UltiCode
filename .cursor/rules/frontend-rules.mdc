---
description: UltiCode Vue 3 frontend project rules
alwaysApply: true
globs: ["apps/console/**/*.ts", "apps/console/**/*.vue", "apps/management/**/*.ts", "apps/management/**/*.vue"]
---

# UltiCode Vue 3 前端项目规则

## API 调用

- Console: 直接 `apiGet/apiPost` 调用
- Management: 封装为 `xxxApi.method()` 带类型
- 所有调用必须通过 `@/utils/request.ts`

## 状态管理 (Pinia)

- 使用 Composition API 风格 (setup store)
- Store 文件：`camelCase.ts`，导出 `useXxxStore`

## 组件

- 必须使用 `<script setup lang="ts">`
- Props: `defineProps<{ title: string }>()`
- Emits: `defineEmits<{ update: [value: string] }>()`

## i18n

- 所有可见文本通过 `t()` 函数
- 新增 key 必须同时添加 en 和 zh
- DataTable 列名：`table.columnNames.{columnId}` (camelCase)

## CSS

- Tailwind CSS v4 + shadcn-vue + Lucide icons
- Prettier: no semicolons, single quotes, 100 char