---
phase: 03-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-spring/src/test/java/com/ulticode/modules/auth/service/impl/AuthServiceImplTest.java
  - backend-spring/src/test/java/com/ulticode/security/jwt/JwtTokenProviderTest.java
  - backend-spring/src/test/java/com/ulticode/security/jwt/JwtPropertiesTest.java
  - backend-spring/src/test/java/com/ulticode/security/csrf/CsrfServiceTest.java
  - backend-spring/src/test/java/com/ulticode/modules/auth/service/PasswordResetServiceTest.java
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "AuthServiceImpl login/register/refresh flows tested with mocked dependencies"
    - "JwtTokenProvider generates valid JWTs and validates/invalidates tokens correctly"
    - "JwtProperties rejects null/blank secrets and warns on short secrets"
    - "CsrfService generateToken stores in Redis, validateAndRotateToken rotates correctly"
    - "PasswordResetService forgotPassword silent-returns for unknown email, sends for known; resetPassword revokes sessions"
  artifacts:
    - path: "backend-spring/src/test/java/com/ulticode/modules/auth/service/impl/AuthServiceImplTest.java"
      provides: "AuthServiceImpl unit tests"
      contains: "@DisplayName"
    - path: "backend-spring/src/test/java/com/ulticode/security/jwt/JwtTokenProviderTest.java"
      provides: "JwtTokenProvider unit tests"
      contains: "generateAccessToken"
    - path: "backend-spring/src/test/java/com/ulticode/security/jwt/JwtPropertiesTest.java"
      provides: "JwtProperties validation tests"
      contains: "validateSecret"
    - path: "backend-spring/src/test/java/com/ulticode/security/csrf/CsrfServiceTest.java"
      provides: "CsrfService unit tests"
      contains: "generateToken"
    - path: "backend-spring/src/test/java/com/ulticode/modules/auth/service/PasswordResetServiceTest.java"
      provides: "PasswordResetService unit tests"
      contains: "forgotPassword"
  key_links:
    - from: "AuthServiceImplTest.java"
      to: "AuthServiceImpl.java"
      via: "@InjectMocks AuthServiceImpl"
      pattern: "AuthServiceImpl"
    - from: "JwtTokenProviderTest.java"
      to: "JwtTokenProvider.java"
      via: "@InjectMocks JwtTokenProvider with ReflectionTestUtils for JwtProperties"
      pattern: "ReflectionTestUtils.setField"
    - from: "PasswordResetServiceTest.java"
      to: "PasswordResetService.java"
      via: "@InjectMocks PasswordResetService"
      pattern: "passwordResetService"
---

<objective>
Create unit tests for auth and security modules (AuthServiceImpl, JwtTokenProvider, JwtProperties, CsrfService, PasswordResetService).

Purpose: Provide regression safety for the security fixes from Phases 1-2 (CSRF migration, JWT validation, password reset email wiring). These are the highest-risk modules that were modified.

Output: 5 test files, all passing with `./mvnw test -pl backend-spring -Dtest="AuthServiceImplTest,JwtTokenProviderTest,JwtPropertiesTest,CsrfServiceTest,PasswordResetServiceTest"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@.planning/phases/03-test-coverage/03-PATTERNS.md

<interfaces>
<!-- Source interfaces the executor needs. No codebase exploration required. -->

From AuthServiceImpl.java (backend-spring/src/main/java/com/ulticode/modules/auth/service/impl/AuthServiceImpl.java):
```java
public class AuthServiceImpl implements AuthService {
    // Dependencies injected via constructor:
    // UserMapper userMapper, UserService userService, JwtTokenProvider jwtTokenProvider,
    // JwtProperties jwtProperties, PasswordEncoder passwordEncoder, CsrfService csrfService,
    // CookieConfig cookieConfig
    public LoginResponse login(LoginDTO loginDTO, HttpServletResponse response)
    public LoginResponse register(RegisterDTO registerDTO, HttpServletResponse response)
    public LoginResponse refresh(String refreshToken, HttpServletResponse response)
    public void logout(HttpServletResponse response)
}
```

From JwtTokenProvider.java:
```java
public class JwtTokenProvider {
    // Uses @InjectMocks with JwtProperties
    public String generateAccessToken(String userId, String username, String role)
    public String generateRefreshToken(String userId)
    public Claims parseToken(String token)
    public String getUserIdFromToken(String token)
    public String getUsernameFromToken(String token)
    public String getRoleFromToken(String token)
    public boolean validateToken(String token)
    public boolean isTokenExpired(String token)
}
```

From JwtProperties.java:
```java
public class JwtProperties {
    private String secret;
    @PostConstruct public void validateSecret()
    // Inner classes: AccessTokenConfig (expiration=900000L), RefreshTokenConfig (expiration=604800000L), CookieConfig
}
```

From CsrfService.java:
```java
public class CsrfService {
    // Constructor injection: RedisTemplate<String, String> redisTemplate
    // Static: CSRF_PREFIX = "csrf:", TOKEN_TTL = Duration.ofHours(24)
    public String generateToken(String userId)
    public String validateAndRotateToken(String userId, String token)
    public void clearUserTokens(String userId)
}
```

From PasswordResetService.java:
```java
public class PasswordResetService {
    // Constructor injection: UserMapper, PasswordEncoder, RefreshTokenService, EmailService
    // Field: @Value("${app.frontend-url}") String frontendUrl
    public void forgotPassword(String email)
    public void resetPassword(String token, String newPassword)
}
```

From ErrorCode.java (relevant codes):
```java
AUTH_INVALID_CREDENTIALS(10001)
AUTH_USER_INACTIVE(10002)
AUTH_USER_BANNED(10006)
AUTH_USERNAME_TAKEN(10003)
AUTH_EMAIL_TAKEN(10004)
AUTH_INVALID_RESET_TOKEN(10007)
AUTH_RESET_TOKEN_EXPIRED(10009)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JwtTokenProviderTest, JwtPropertiesTest, and CsrfServiceTest</name>
  <files>
    backend-spring/src/test/java/com/ulticode/security/jwt/JwtTokenProviderTest.java
    backend-spring/src/test/java/com/ulticode/security/jwt/JwtPropertiesTest.java
    backend-spring/src/test/java/com/ulticode/security/csrf/CsrfServiceTest.java
  </files>
  <read_first>
    backend-spring/src/main/java/com/ulticode/security/jwt/JwtTokenProvider.java
    backend-spring/src/main/java/com/ulticode/security/jwt/JwtProperties.java
    backend-spring/src/main/java/com/ulticode/security/csrf/CsrfService.java
    backend-spring/src/test/java/com/ulticode/modules/queue/service/QueueServiceTest.java
    backend-spring/src/test/java/com/ulticode/modules/admin/service/impl/AdminSubmissionServiceImplTest.java
  </read_first>
  <acceptance_criteria>
    - JwtTokenProviderTest.java contains @Nested classes for generateAccessToken, generateRefreshToken, validateToken, claim extraction
    - JwtTokenProviderTest uses ReflectionTestUtils.setField to inject TEST_SECRET ("a".repeat(64)) and AccessTokenConfig/RefreshTokenConfig into jwtProperties
    - JwtTokenProviderTest verifies: token generation produces non-blank token, self-generated token validates true, different secret validates false, null/empty/malformed tokens validate false, claims extraction works
    - JwtPropertiesTest.java tests validateSecret: null secret throws NullPointerException with message "JWT secret must not be null", blank secret throws IllegalStateException, short secret (<32 chars) logs warning but does not throw, valid 64-char secret passes without exception
    - CsrfServiceTest.java mocks RedisTemplate and ValueOperations, setUp returns valueOperations from redisTemplate.opsForValue()
    - CsrfServiceTest tests: generateToken returns "tokenId:tokenValue" format and calls valueOperations.set(), generateToken throws for null/empty userId, validateAndRotateToken returns null for invalid format, validateAndRotateToken returns null for wrong stored value, validateAndRotateToken calls generateToken on success (rotation), clearUserTokens calls redisTemplate.delete
    - All three files use @ExtendWith(MockitoExtension.class), @Nested, @DisplayName, AssertJ assertThat/assertThatThrownBy
    - All three files compile and pass: cd backend-spring && ./mvnw test -Dtest="JwtTokenProviderTest,JwtPropertiesTest,CsrfServiceTest"
  </acceptance_criteria>
  <action>
    Create three unit test files following the established project pattern (@ExtendWith(MockitoExtension.class), @Mock, @InjectMocks, @Nested, @DisplayName).

    **JwtTokenProviderTest.java** (package com.ulticode.security.jwt):
    - Use @Mock JwtProperties, @InjectMocks JwtTokenProvider
    - @BeforeEach: inject TEST_SECRET = "a".repeat(64) into jwtProperties via ReflectionTestUtils.setField, also inject new AccessTokenConfig() and new RefreshTokenConfig()
    - @Nested "generateAccessToken()": test generatesValidJwt_withCorrectClaims (assert token non-blank, getUserIdFromToken returns USER_ID, getUsernameFromToken returns USERNAME, getRoleFromToken returns ROLE), test selfGeneratedToken_validates (validateToken true), test differentSecret_failsValidation (change secret to "b".repeat(64), validateToken false)
    - @Nested "generateRefreshToken()": test generatesRefreshToken_withTypeClaim (token non-blank, getUserIdFromToken returns USER_ID, validates true)
    - @Nested "validateToken()": test malformedToken_returnsFalse ("not.a.jwt"), nullToken_returnsFalse (null), emptyToken_returnsFalse (""), expiredToken_returnsFalse (use ReflectionTestUtils to set accessToken.expiration to 1L, generate, Thread.sleep 10ms, validate false)
    - @Nested "Claim extraction": test getUserIdFromToken returns correct userId, getUsernameFromToken returns correct username, getRoleFromToken returns correct role

    **JwtPropertiesTest.java** (package com.ulticode.security.jwt):
    - No Mockito needed -- test the POJO directly. Create new JwtProperties() instance per test.
    - @Nested "validateSecret()": test nullSecret_throws (assertThatThrownBy is NullPointerException with message containing "JWT secret must not be null"), test blankSecret_throws (set to "   ", assertThatThrownBy is IllegalStateException), test shortSecret_doesNotThrow (set to "a".repeat(16), assertThatCode does not throw), test validSecret_succeeds (set to "a".repeat(64), assertThatCode does not throw)
    - @Nested "Default values": test defaultAccessTokenExpiration is 900000L, test defaultRefreshTokenExpiration is 604800000L

    **CsrfServiceTest.java** (package com.ulticode.security.csrf):
    - Use @Mock RedisTemplate<String,String>, @Mock ValueOperations<String,String>, @InjectMocks CsrfService
    - @BeforeEach: when(redisTemplate.opsForValue()).thenReturn(valueOperations)
    - @Nested "generateToken()": test storesInRedis (call generateToken("user-1"), assert result contains ":", split and assert 2 parts, verify valueOperations.set called with anyString, anyString, any(Duration)), test nullUserId_throws (assertThatThrownBy for null userId is IllegalArgumentException), test emptyUserId_throws (assertThatThrownBy for "" is IllegalArgumentException)
    - @Nested "validateAndRotateToken()": test invalidFormat_returnsNull (pass "no-colon"), test emptyToken_returnsNull (pass ""), test validToken_returnsNewToken (mock valueOperations.get returns stored value matching token, then generate new token -- verify valueOperations.set called), test wrongStoredValue_returnsNull (mock valueOperations.get returns "wrong-value", assert null), test nullUserId_throws
    - @Nested "clearUserTokens()": test callsDelete (verify redisTemplate.delete called with any collection), test nullUserId_throws, test emptyUserId_throws
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/backend-spring && ./mvnw test -Dtest="JwtTokenProviderTest,JwtPropertiesTest,CsrfServiceTest" -pl .</automated>
  </verify>
  <done>
    JwtTokenProviderTest, JwtPropertiesTest, and CsrfServiceTest compile and pass. Token generation/validation, config validation, and CSRF lifecycle are all covered with AssertJ assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AuthServiceImplTest and PasswordResetServiceTest</name>
  <files>
    backend-spring/src/test/java/com/ulticode/modules/auth/service/impl/AuthServiceImplTest.java
    backend-spring/src/test/java/com/ulticode/modules/auth/service/PasswordResetServiceTest.java
  </files>
  <read_first>
    backend-spring/src/main/java/com/ulticode/modules/auth/service/impl/AuthServiceImpl.java
    backend-spring/src/main/java/com/ulticode/modules/auth/service/PasswordResetService.java
    backend-spring/src/main/java/com/ulticode/modules/auth/dto/LoginDTO.java
    backend-spring/src/main/java/com/ulticode/modules/auth/dto/RegisterDTO.java
    backend-spring/src/main/java/com/ulticode/modules/auth/dto/LoginResponse.java
    backend-spring/src/main/java/com/ulticode/modules/user/entity/User.java
    backend-spring/src/main/java/com/ulticode/security/jwt/JwtProperties.java
    backend-spring/src/main/java/com/ulticode/modules/email/service/EmailService.java
    backend-spring/src/main/java/com/ulticode/modules/refreshtoken/service/RefreshTokenService.java
    backend-spring/src/test/java/com/ulticode/modules/admin/service/impl/AdminSubmissionServiceImplTest.java
  </read_first>
  <acceptance_criteria>
    - AuthServiceImplTest.java contains @Nested classes for login(), register(), refresh()
    - AuthServiceImplTest login tests cover: valid credentials, non-existent user (AUTH_INVALID_CREDENTIALS), wrong password (AUTH_INVALID_CREDENTIALS), inactive user (AUTH_USER_INACTIVE), banned user (AUTH_USER_BANNED)
    - AuthServiceImplTest register tests cover: valid registration (delegates to login), duplicate username (AUTH_USERNAME_TAKEN), duplicate email (AUTH_EMAIL_TAKEN)
    - AuthServiceImplTest refresh tests cover: valid refresh token (returns new tokens), invalid refresh token (throws)
    - AuthServiceImplTest uses helper method createActiveUser() returning User with id, username, password, role, isActive, isBanned fields set
    - AuthServiceImplTest uses helper method mockResponse() returning mock(HttpServletResponse.class)
    - PasswordResetServiceTest.java contains @Nested classes for forgotPassword(), resetPassword()
    - PasswordResetServiceTest forgotPassword tests cover: non-existent email (silent return, no emailService.sendEmail call), existing user (sends email, updates token hash)
    - PasswordResetServiceTest resetPassword tests cover: valid token (resets password, revokes sessions via refreshTokenService.revokeAllUserTokens, clears token hash), invalid token (throws AUTH_INVALID_RESET_TOKEN), expired token (throws AUTH_RESET_TOKEN_EXPIRED)
    - Both files use AssertJ assertThat/assertThatThrownBy
    - All tests pass: cd backend-spring && ./mvnw test -Dtest="AuthServiceImplTest,PasswordResetServiceTest"
  </acceptance_criteria>
  <action>
    Create two unit test files following the established project pattern.

    **AuthServiceImplTest.java** (package com.ulticode.modules.auth.service.impl):
    - Dependencies to @Mock: UserMapper userMapper, UserService userService, JwtTokenProvider jwtTokenProvider, JwtProperties jwtProperties, PasswordEncoder passwordEncoder, CsrfService csrfService
    - @InjectMocks AuthServiceImpl authService
    - Constants: USER_ID="test-user-123", USERNAME="testuser", PASSWORD="password123", ACCESS_TOKEN="access-token-value", REFRESH_TOKEN="refresh-token-value", CSRF_TOKEN="csrf-token-value"
    - Helper: createActiveUser() returns User with id=USER_ID, username=USERNAME, password="encoded-password", role="USER", isActive=true, isBanned=false
    - Helper: mockResponse() returns mock(HttpServletResponse.class)
    - @Nested "login()":
      - login_validCredentials_returnsLoginResponse: create LoginDTO with username+password, mock userMapper.selectOne returns user, passwordEncoder.matches returns true, jwtTokenProvider.generateAccessToken returns ACCESS_TOKEN, jwtTokenProvider.generateRefreshToken returns REFRESH_TOKEN, csrfService.generateToken returns CSRF_TOKEN. Assert response.getCsrfToken() equals CSRF_TOKEN, verify all collaborators called.
      - login_nonExistentUser_throwsException: userMapper.selectOne returns null. assertThatThrownBy with ErrorCode.AUTH_INVALID_CREDENTIALS.
      - login_wrongPassword_throwsException: userMapper.selectOne returns user, passwordEncoder.matches returns false. assertThatThrownBy with ErrorCode.AUTH_INVALID_CREDENTIALS.
      - login_inactiveUser_throwsException: user with isActive=false. assertThatThrownBy with ErrorCode.AUTH_USER_INACTIVE (read the source to verify the exact ErrorCode field name).
      - login_bannedUser_throwsException: user with isBanned=true. assertThatThrownBy with ErrorCode.AUTH_USER_BANNED.
    - @Nested "register()":
      - register_validRegistration_delegatesToLogin: mock userMapper.selectOne returns null for both username and email checks, passwordEncoder.encode returns "encoded". Verify register does not throw and returns LoginResponse (it internally calls login).
      - register_duplicateUsername_throwsException: first userMapper.selectOne returns existing user. assertThatThrownBy with ErrorCode.AUTH_USERNAME_TAKEN.
      - register_duplicateEmail_throwsException: first selectOne returns null (no username match), second selectOne returns existing user. assertThatThrownBy with ErrorCode.AUTH_EMAIL_TAKEN.
    - @Nested "refresh()":
      - refresh_validToken_returnsNewTokens: jwtTokenProvider.getUserIdFromToken returns USER_ID, userMapper.selectById returns active user. Assert response is not null and csrfToken matches.
      - refresh_invalidToken_throwsException: jwtTokenProvider.getUserIdFromToken throws or returns null. assertThatThrownBy.

    **PasswordResetServiceTest.java** (package com.ulticode.modules.auth.service):
    - Dependencies to @Mock: UserMapper userMapper, PasswordEncoder passwordEncoder, RefreshTokenService refreshTokenService, EmailService emailService
    - @InjectMocks PasswordResetService passwordResetService
    - Use ReflectionTestUtils.setField(passwordResetService, "frontendUrl", "http://localhost:9002") to inject the @Value field
    - @Nested "forgotPassword()":
      - forgotPassword_nonExistentEmail_returnsSilently: userMapper.selectOne returns null. Call forgotPassword("nonexistent@example.com"). verify(emailService, never()).sendEmail(any()).
      - forgotPassword_existingUser_sendsEmail: create User with id, username, email. userMapper.selectOne returns user. passwordEncoder.encode returns "hashed-token". Call forgotPassword("test@example.com"). verify(emailService).sendEmail(any(SendEmailDTO.class)). verify(userMapper).updateById(user). assertThat(user.getPasswordResetTokenHash()).isEqualTo("hashed-token"). assertThat(user.getPasswordResetExpiresAt()).isNotNull().
    - @Nested "resetPassword()":
      - resetPassword_validToken_resetsAndRevokes: create User with passwordResetTokenHash="$2a$10$hash", passwordResetExpiresAt=now+15min. userMapper.selectList returns List.of(user). passwordEncoder.matches returns true. passwordEncoder.encode returns "new-hash". Call resetPassword("plain-token", "newPassword123"). verify(refreshTokenService).revokeAllUserTokens(user.getId()). assertThat(user.getPasswordResetTokenHash()).isNull().
      - resetPassword_invalidToken_throwsException: user with token hash, but passwordEncoder.matches returns false. assertThatThrownBy with ErrorCode.AUTH_INVALID_RESET_TOKEN.
      - resetPassword_expiredToken_throwsException: user with passwordResetExpiresAt=now-15min (past). assertThatThrownBy with ErrorCode.AUTH_RESET_TOKEN_EXPIRED.

    IMPORTANT: Read each source file first to verify exact method signatures, field names, and ErrorCode constants before writing tests. The source code is the source of truth.
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/backend-spring && ./mvnw test -Dtest="AuthServiceImplTest,PasswordResetServiceTest" -pl .</automated>
  </verify>
  <done>
    AuthServiceImplTest and PasswordResetServiceTest compile and pass. Login/register/refresh flows and password reset email/revocation flows are fully covered with edge cases for banned, inactive, and non-existent users.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> Source under test | Test code accesses production classes via public API only |
| Test code -> Mockito mocks | Mock objects simulate dependencies without real I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | Test files | accept | Test files do not contain real secrets. JwtTokenProviderTest uses "a".repeat(64) as test secret, which is obviously synthetic |
| T-03-02 | S (Spoofing) | AuthServiceImplTest | accept | Tests mock authentication dependencies; no real auth tokens are generated that could be misused |
| T-03-03 | I (Information Disclosure) | PasswordResetServiceTest | accept | Test uses dummy email addresses (nonexistent@example.com), no real user data exposed |
</threat_model>

<verification>
1. All 5 test files compile: `cd backend-spring && ./mvnw test-compile`
2. All auth/security tests pass: `cd backend-spring && ./mvnw test -Dtest="AuthServiceImplTest,JwtTokenProviderTest,JwtPropertiesTest,CsrfServiceTest,PasswordResetServiceTest"`
3. No test leaves residual state -- all tests use fresh mocks via @BeforeEach
</verification>

<success_criteria>
- AuthServiceImplTest covers login (5 scenarios), register (3 scenarios), refresh (2 scenarios)
- JwtTokenProviderTest covers generateAccessToken, generateRefreshToken, validateToken, claim extraction
- JwtPropertiesTest covers validateSecret (4 scenarios) and default values
- CsrfServiceTest covers generateToken, validateAndRotateToken, clearUserTokens
- PasswordResetServiceTest covers forgotPassword (2 scenarios) and resetPassword (3 scenarios)
- All tests pass individually and as a group
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-01-SUMMARY.md`
</output>
