# Claude Code compatibility entry

@AGENTS.md

Follow the imported root guide and the nearest nested `AGENTS.md` for every task in this repository. They are the single source of truth for project rules.

Treat `.claude/rules/` as path-scoped language, framework, and Claude workflow guardrails that complement the project guides. `AGENTS.md` remains authoritative and wins every conflict; do not copy an entire guide into a rule.
