/**
 * Queue and execution ports owned by the shared {@code judge-runtime}
 * library.
 *
 * <p>The ports are consumed by Judge Worker wiring and the explicit App
 * compatibility path; they do not make the queue or sandbox an App-owned
 * persistence domain.</p>
 */
package com.ulticode.modules.queue.port;
