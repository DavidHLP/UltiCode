---
phase: 25-large-file-refactoring
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java
autonomous: false
requirements:
  - REF-01
must_haves:
  truths:
    - ForumServiceImpl reduced from 693 lines to facade (delegation only)
    - ForumPostService handles post operations independently
    - ForumCommentService handles comment operations independently
    - ForumVoteService handles vote enrichment independently
    - All 3 new services use constructor injection per D-18
    - All 3 new services are 250 lines or less per D-19
    - Controller interfaces unchanged per D-20
    - ./mvnw test passes after refactor per D-21
  artifacts:
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumPostService.java
      provides: Post operations interface
      min_lines: 10
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumPostServiceImpl.java
      provides: Post operations implementation
      min_lines: 150
      max_lines: 250
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumCommentService.java
      provides: Comment operations interface
      min_lines: 10
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumCommentServiceImpl.java
      provides: Comment operations implementation
      min_lines: 150
      max_lines: 250
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumVoteService.java
      provides: Vote enrichment interface
      min_lines: 5
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumVoteServiceImpl.java
      provides: Vote enrichment implementation
      min_lines: 30
      max_lines: 80
    - path: backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java
      provides: Facade delegating to 3 services
      min_lines: 30
      max_lines: 80
  key_links:
    - from: forum/controller/ForumController.java
      to: forum/service/ForumService.java
      via: constructor injection
      pattern: @RequiredArgsConstructor.*ForumService
    - from: ForumServiceImpl.java
      to: ForumPostService,ForumCommentService,ForumVoteService
      via: constructor injection + delegation
      pattern: private final Forum(Post|Comment|Vote)Service
---

<objective>
Split monolithic ForumServiceImpl (693 lines) into facade + 3 focused services: ForumPostService, ForumCommentService, ForumVoteService.

Per D-01 (facade pattern): Keep ForumService interface + ForumServiceImpl as facade delegating to 3 new focused services. No new public APIs -- controller interfaces unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java
@backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumService.java

# Service structure (constructor-injected dependencies in ForumServiceImpl):
# - ForumPostMapper, ForumCommentMapper, ForumCommunityMapper, ForumCommunityMemberMapper, ForumTagMapper, ForumUserMapper, UserService, VoteService
#
# Method groups to extract:
# POST OPERATIONS (lines 46-269): findAllPosts, findPostById, findMyPosts, createPost, updatePost, deletePost, getPostThread, recordShare, recordView
# COMMENT OPERATIONS (lines 272-392): createComment, updateComment, deleteComment
# COMMUNITY/OTHER (lines 395-530): findAllCommunities, findCommunityBySlugOrId, findPostsByCommunity, joinCommunity, leaveCommunity, findAllTags, getQuickFilters
#
# Vote enrichment (used in convertToPostVO): voteService.getVoteStatus() call stays in ForumVoteService
#
# Helper methods (shared): convertToPostVO, convertToCommentVO, convertToCommunityVO, convertToTagVO, buildCommentTree, findReplies, batchLoadAuthors, ensureForumUserExists, generatePermalink
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForumPostService interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumPostService.java
    backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumPostServiceImpl.java
  </files>
  <action>
Create ForumPostService interface in forum/service/ with these method signatures extracted from ForumServiceImpl:
- List&lt;ForumPostVO&gt; findAllPosts(String userId)
- PageResult&lt;ForumPostVO&gt; findAllPosts(String userId, int page, int pageSize)
- ForumPostVO findPostById(String id, String userId)
- List&lt;ForumPostVO&gt; findMyPosts(String userId)
- PageResult&lt;ForumPostVO&gt; findMyPosts(String userId, int page, int pageSize)
- ForumPostVO createPost(CreatePostDTO dto, String userId)
- ForumPostVO updatePost(String id, UpdatePostDTO dto, String userId)
- void deletePost(String id, String userId)
- ForumPostThreadVO getPostThread(String postId, String userId)
- void recordShare(String postId)
- void recordView(String postId)

Create ForumPostServiceImpl implementing constructor injection:
- Dependencies: ForumPostMapper, ForumCommunityMapper, ForumCommunityMemberMapper, ForumUserMapper, UserService, VoteService
- Move ensureForumUserExists() and generatePermalink() as private methods
- Move convertToPostVO() as a public method (needed by facade for toVO conversion)
- Keep batchLoadAuthors() as private helper
- Size target: 150-250 lines
</action>
  <verify>
grep -c "public interface ForumPostService" backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumPostService.java && \
grep -c "public class ForumPostServiceImpl" backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumPostServiceImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumPostServiceImpl.java | awk '{print ($1 &lt;= 250 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>ForumPostService interface exists with 11 methods; ForumPostServiceImpl is 250 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 2: Create ForumCommentService interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumCommentService.java
    backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumCommentServiceImpl.java
  </files>
  <action>
Create ForumCommentService interface with these method signatures:
- ForumCommentVO createComment(String postId, CreateCommentDTO dto, String userId)
- ForumCommentVO updateComment(String id, UpdateCommentDTO dto, String userId)
- void deleteComment(String id, String userId)

Create ForumCommentServiceImpl implementing constructor injection:
- Dependencies: ForumCommentMapper, ForumPostMapper, ForumUserMapper, UserService
- Move convertToCommentVO() as public method
- Move buildCommentTree() and findReplies() as public methods (needed by facade)
- Ensure forum user exists via ensureForumUserExists() -- inject ForumUserMapper + UserService
- Size target: 150-200 lines
</action>
  <verify>
grep -c "public interface ForumCommentService" backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumCommentService.java && \
grep -c "public class ForumCommentServiceImpl" backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumCommentServiceImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumCommentServiceImpl.java | awk '{print ($1 &lt;= 250 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>ForumCommentService interface exists with 3 methods; ForumCommentServiceImpl is 250 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 3: Create ForumVoteService interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumVoteService.java
    backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumVoteServiceImpl.java
  </files>
  <action>
Create ForumVoteService interface with a single method:
- VoteResultVO getPostVoteStatus(String userId, String postId)

Create ForumVoteServiceImpl implementing constructor injection:
- Dependencies: VoteService (from vote module)
- Delegates to voteService.getVoteStatus(userId, postId, EdgeOperationTargetType.FORUM_POST)
- Size target: 30-50 lines
</action>
  <verify>
grep -c "public interface ForumVoteService" backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumVoteService.java && \
grep -c "public class ForumVoteServiceImpl" backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumVoteServiceImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumVoteServiceImpl.java | awk '{print ($1 &lt;= 80 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>ForumVoteService interface and implementation created, 80 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 4: Refactor ForumServiceImpl into thin facade</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java
  </files>
  <action>
Refactor ForumServiceImpl to become a thin facade (target: 30-80 lines):

1. Inject all 3 new services via constructor: ForumPostService, ForumCommentService, ForumVoteService
2. Also keep ForumCommunityMapper, ForumTagMapper, ForumCommunityMemberMapper (community/tag operations stay here)
3. Remove all post, comment, and vote business logic -- delegate to services
4. Keep community operations: findAllCommunities, findCommunityBySlugOrId, findPostsByCommunity, joinCommunity, leaveCommunity, findAllTags, getQuickFilters
5. Keep helper methods: convertToCommunityVO, convertToTagVO, batchLoadAuthors
6. For getPostThread: delegate to forumPostService.getPostThread(), then enrich comments via forumCommentService.buildCommentTree()
7. All method implementations should be one-liners delegating to the appropriate service

Verify no new public methods added -- all existing ForumService interface methods preserved.
</action>
  <verify>
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java | awk '{print ($1 &lt;= 80 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}' && \
grep "private final ForumPostService\|private final ForumCommentService\|private final ForumVoteService" backend-spring/src/main/java/com/ulticode/modules/forum/service/impl/ForumServiceImpl.java | wc -l | awk '{print ($1 == 3 ? "PASS: 3 services injected" : "FAIL: " $1 " services")}'
  </verify>
  <done>ForumServiceImpl is 80 lines or fewer, injects all 3 services, delegates all post/comment operations</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify REF-01 Wave 1 tests pass</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/forum/service/ForumService.java
  </files>
  <action>
After Tasks 1-4 complete, run forum tests to verify the refactor did not break existing behavior.

Execute in backend-spring directory:
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Forum*" -q 2&gt;&amp;1 | tail -30

Expected: BUILD SUCCESS with all forum tests passing.
</action>
  <verify>
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Forum*" -q 2&gt;&amp;1 | grep -E "BUILD SUCCESS|BUILD FAILURE|Tests run:"
  </verify>
  <done>All forum tests pass; ForumServiceImpl is facade with 80 lines or fewer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forum service -&gt; vote service | Internal module call; VoteService is trusted |
| forum service -&gt; user service | Internal module call; UserService is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tamper | ForumPostServiceImpl.createPost | mitigate | Community membership check preserved from original; forum user FK constraint maintained |
| T-25-02 | Information | ForumCommentVO conversion | accept | No new data exposure; only existing VO fields populated |
| T-25-03 | Denial | ForumVoteServiceImpl | accept | Thin delegation; no new attack surface |
</threat_model>

<verification>
cd backend-spring &amp;&amp; ./mvnw compile -q &amp;&amp; echo "COMPILE OK"
</verification>

<success_criteria>
- ForumPostService.java interface exists with 11 methods
- ForumPostServiceImpl.java 250 lines or fewer
- ForumCommentService.java interface exists with 3 methods
- ForumCommentServiceImpl.java 250 lines or fewer
- ForumVoteService.java interface exists with 1 method
- ForumVoteServiceImpl.java 80 lines or fewer
- ForumServiceImpl.java 80 lines or fewer (facade)
- All 3 services use constructor injection
- No new public API methods introduced
- ./mvnw compile succeeds
- ./mvnw test -Dtest="*Forum*" succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/25-large-file-refactoring/25-01-SUMMARY.md`
</output>

---

## WAVE 2 -- REF-02: CodeExecutionService Split

<objective>
Split CodeExecutionService (643 lines) into SandboxService (security/cgroup) + CodeExecutionHelper (per-language logic), with original becoming thin facade.

Per D-08: SandboxService (security/cgroup) + CodeExecutionHelper (per-language logic), original becomes thin facade.
</objective>

<context>
@backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java

# CodeExecutionService structure:
# Dependencies: DockerSandboxConfig sandboxConfig, ObjectMapper objectMapper
#
# Methods to extract to SandboxService (security/cgroup/Docker):
# - executeInSandbox() -- lines 97-153: Docker process lifecycle, timeout, memory
# - buildDockerCommand() -- lines 155-202: Docker CLI builder (security-relevant)
# - buildBatchDockerCommand() -- lines 387-404: Docker CLI for batch mode
# - executeBatch() -- lines 210-260: Batch Docker process lifecycle
#
# Methods to extract to CodeExecutionHelper (per-language):
# - buildWrapperScript() -- lines 262-272: language switch
# - buildJavaScriptBatchWrapper() -- lines 274-288
# - buildPythonBatchWrapper() -- lines 291-308
# - buildCBatchWrapper() -- lines 311-333
# - buildCppBatchWrapper() -- lines 336-358
# - buildJavaBatchWrapper() -- lines 361-385
# - wrapJavaScript() -- lines 472-479
# - wrapPython() -- lines 482-490
# - wrapJava() -- lines 493-509
# - extractFunctionName() -- lines 512-528
# - buildBatchInputsJson() -- lines 406-414
# - parseBatchResults() -- lines 416-468
#
# Utility methods staying in CodeExecutionService (thin facade):
# - execute() -- main entry point, orchestrates
# - emptyResult(), buildCaseResult(), buildInputsJson(), parseInputValue(), normalizeOutput(), parseRuntimeMs(), sanitizeSandboxOutput()
</context>

<tasks>

<task type="auto">
  <name>Task 6: Create SandboxService interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/submission/service/SandboxService.java
    backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SandboxServiceImpl.java
  </files>
  <action>
Create SandboxService interface with these method signatures:
- RunResultDTO.RunCaseResult executeInSandbox(String language, String code, RunSubmissionDTO.RunTestCase testCase, String runId, String userId)
- List&lt;RunResultDTO.RunCaseResult&gt; executeBatch(String language, String code, List&lt;RunSubmissionDTO.RunTestCase&gt; testCases, String runId, String userId)
- List&lt;String&gt; buildDockerCommand(String language, String code)
- List&lt;String&gt; buildBatchDockerCommand(String language, String wrapperScript)

Create SandboxServiceImpl implementing constructor injection:
- Dependencies: DockerSandboxConfig sandboxConfig
- Move executeInSandbox() and buildDockerCommand() from CodeExecutionService
- Move executeBatch() and buildBatchDockerCommand() from CodeExecutionService
- Size target: 200-250 lines
- This class handles all Docker security parameters (cgroup, --network none, --cap-drop ALL, etc.)
</action>
  <verify>
grep -c "public interface SandboxService" backend-spring/src/main/java/com/ulticode/modules/submission/service/SandboxService.java && \
grep -c "public class SandboxServiceImpl" backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SandboxServiceImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/SandboxServiceImpl.java | awk '{print ($1 &lt;= 250 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>SandboxService interface exists; SandboxServiceImpl is 250 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 7: Create CodeExecutionHelper interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionHelper.java
    backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/CodeExecutionHelperImpl.java
  </files>
  <action>
Create CodeExecutionHelper interface with these method signatures:
- String buildWrapperScript(String language, String code, List&lt;RunSubmissionDTO.RunTestCase&gt; testCases)
- String buildBatchInputsJson(List&lt;RunSubmissionDTO.RunTestCase&gt; testCases)
- List&lt;RunResultDTO.RunCaseResult&gt; parseBatchResults(String stdout, List&lt;RunSubmissionDTO.RunTestCase&gt; testCases, String runId, String userId)
- String wrapJavaScript(String code)
- String wrapPython(String code)
- String wrapJava(String code)
- String extractFunctionName(String code, String keyword)
- String buildInputsJson(RunSubmissionDTO.RunTestCase testCase)
- String parseInputValue(String value)
- String normalizeOutput(String output)
- String sanitizeSandboxOutput(String output)
- RunResultDTO emptyResult(Long problemId, String userId)
- RunResultDTO.RunCaseResult buildCaseResult(...)

Create CodeExecutionHelperImpl implementing constructor injection:
- Dependencies: ObjectMapper objectMapper
- Move all per-language wrapper builders and utility methods
- Size target: 300-350 lines
- This class is pure logic (no Docker, no security concerns)
</action>
  <verify>
grep -c "public interface CodeExecutionHelper" backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionHelper.java && \
grep -c "public class CodeExecutionHelperImpl" backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/CodeExecutionHelperImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/submission/service/impl/CodeExecutionHelperImpl.java | awk '{print ($1 &lt;= 350 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>CodeExecutionHelper interface exists with 13 methods; CodeExecutionHelperImpl is 350 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 8: Refactor CodeExecutionService into thin facade</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java
  </files>
  <action>
Refactor CodeExecutionService to thin facade (target: 80-100 lines):

1. Inject SandboxService and CodeExecutionHelper via constructor
2. Keep the public execute() method as the single entry point
3. Remove all implementation: executeInSandbox, buildDockerCommand, executeBatch, buildBatchDockerCommand, buildWrapperScript, all wrapper builders, all utilities
4. The execute() method orchestrates: calls helper.buildInputsJson(), sandboxService.executeInSandbox() or sandboxService.executeBatch(), then helper.buildCaseResult() etc.
5. Verify all original public API preserved (only execute() public method)
6. Verify SUPPORTED_LANGUAGES constant moved to CodeExecutionHelper
</action>
  <verify>
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java | awk '{print ($1 &lt;= 100 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}' && \
grep "private final SandboxService\|private final CodeExecutionHelper" backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java | wc -l | awk '{print ($1 == 2 ? "PASS: 2 services injected" : "FAIL: " $1 " services")}'
  </verify>
  <done>CodeExecutionService is 100 lines or fewer, injects both services, execute() delegates to them</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 9: Verify REF-02 Wave 2 tests pass</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/submission/service/CodeExecutionService.java
  </files>
  <action>
After Tasks 6-8 complete, run submission tests to verify the CodeExecutionService refactor did not break existing behavior.

Execute in backend-spring directory:
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Execution*,*Sandbox*" -q 2&gt;&amp;1 | tail -30

Expected: BUILD SUCCESS with all execution tests passing.
</action>
  <verify>
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Execution*,*Sandbox*" -q 2&gt;&amp;1 | grep -E "BUILD SUCCESS|BUILD FAILURE|Tests run:"
  </verify>
  <done>All execution/sandbox tests pass; CodeExecutionService is facade with 100 lines or fewer</done>
</task>

</tasks>

<success_criteria_wave2>
- SandboxService.java interface exists with 4 methods
- SandboxServiceImpl.java 250 lines or fewer
- CodeExecutionHelper.java interface exists with 13 methods
- CodeExecutionHelperImpl.java 350 lines or fewer
- CodeExecutionService.java 100 lines or fewer (facade)
- Both new services use constructor injection
- No new public API methods introduced
- ./mvnw compile succeeds
- ./mvnw test -Dtest="*Execution*,*Sandbox*" succeeds
</success_criteria_wave2>

---

## WAVE 3 -- REF-03: ContestServiceImpl Split

<objective>
Split ContestServiceImpl (633 lines) into ContestRankingService + ContestSchedulerService, with ContestServiceImpl becoming facade. RankingServiceImpl already exists separately.

Per D-14: ContestRankingService already exists (extracted in Phase 14); extract scheduling to ContestSchedulerService; ContestServiceImpl becomes facade.
</objective>

<context>
@backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestServiceImpl.java
@backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/RankingServiceImpl.java

# ContestServiceImpl method groups:
#
# Methods to extract to ContestSchedulerService (scheduling/lifecycle):
# - registerForContest() -- lines 320-359: registration with race condition protection
# - unregisterFromContest() -- lines 361-384: unregister
# - getParticipationStatus() -- lines 386-423: participation status
# - getUserContests() -- lines 425-459: user contest history
# - startVirtualContest() -- lines 461-497: virtual contest start
# - getVirtualSession() -- lines 499-523: get virtual session
# - finishVirtualContest() -- lines 525-550: finish virtual contest
#
# Methods staying in ContestServiceImpl (facade -- admin + query):
# CRUD: createContest(), updateContest(), deleteContest()
# Query: findAll(), findById(), findBySlug(), getContestById(), findUpcoming(), findRunning(), findPast(), getStats(), getGlobalRanking()
# toVO(), generateSlug(), toRankingVO()
#
# RankingServiceImpl already exists and handles ranking computations.
# getGlobalRanking() in ContestServiceImpl already delegates to globalRankingMapper -- stays in facade.
</context>

<tasks>

<task type="auto">
  <name>Task 10: Create ContestSchedulerService interface + implementation</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/contest/service/ContestSchedulerService.java
    backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestSchedulerServiceImpl.java
  </files>
  <action>
Create ContestSchedulerService interface with these method signatures:
- void registerForContest(String contestId, String userId)
- void unregisterFromContest(String contestId, String userId)
- ParticipationStatusDTO getParticipationStatus(String contestId, String userId)
- List&lt;ContestVO&gt; getUserContests(String userId, String type)
- ParticipationStatusDTO startVirtualContest(String contestId, String userId)
- ParticipationStatusDTO getVirtualSession(String contestId, String userId)
- void finishVirtualContest(String contestId, String sessionId, String userId)

Create ContestSchedulerServiceImpl implementing constructor injection:
- Dependencies: ContestMapper, ContestParticipantMapper
- Move all 7 scheduling/lifecycle methods from ContestServiceImpl
- Keep all logic including race condition protection (tryIncrementRegisteredCount)
- toVO conversion calls back to ContestServiceImpl or takes ContestService as dependency
</action>
  <verify>
grep -c "public interface ContestSchedulerService" backend-spring/src/main/java/com/ulticode/modules/contest/service/ContestSchedulerService.java && \
grep -c "public class ContestSchedulerServiceImpl" backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestSchedulerServiceImpl.java && \
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestSchedulerServiceImpl.java | awk '{print ($1 &lt;= 250 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}'
  </verify>
  <done>ContestSchedulerService interface exists with 7 methods; ContestSchedulerServiceImpl is 250 lines or fewer</done>
</task>

<task type="auto">
  <name>Task 11: Refactor ContestServiceImpl into thin facade</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestServiceImpl.java
  </files>
  <action>
Refactor ContestServiceImpl to thin facade (target: 350-400 lines -- more than others since it keeps admin CRUD + query logic):

1. Inject ContestSchedulerService via constructor
2. Remove all 7 scheduling/lifecycle methods -- delegate to ContestSchedulerService
3. Keep all admin CRUD + query operations in this facade
4. For getUserContests() -- delegate to scheduler, then enrich with toVO
5. For getParticipationStatus() -- delegate to scheduler
6. For virtual contest methods -- delegate to scheduler
7. Keep toVO(), generateSlug(), toRankingVO() (used by multiple)
8. Size target: 350-400 lines (facade keeps meaningful admin/query work)
</action>
  <verify>
wc -l &lt; backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestServiceImpl.java | awk '{print ($1 &lt;= 400 ? "PASS: " $1 " lines" : "FAIL: " $1 " lines")}' && \
grep "private final ContestSchedulerService" backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestServiceImpl.java | wc -l | awk '{print ($1 &gt;= 1 ? "PASS: scheduler injected" : "FAIL: no scheduler")}'
  </verify>
  <done>ContestServiceImpl is 400 lines or fewer, injects ContestSchedulerService, delegates scheduling operations</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 12: Verify REF-03 Wave 3 tests pass</name>
  <files>
    backend-spring/src/main/java/com/ulticode/modules/contest/service/impl/ContestServiceImpl.java
  </files>
  <action>
After Tasks 10-11 complete, run contest tests to verify the ContestServiceImpl refactor did not break existing behavior.

Execute in backend-spring directory:
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Contest*,*Ranking*" -q 2&gt;&amp;1 | tail -30

Expected: BUILD SUCCESS with all contest and ranking tests passing.
</action>
  <verify>
cd backend-spring &amp;&amp; ./mvnw test -Dtest="*Contest*,*Ranking*" -q 2&gt;&amp;1 | grep -E "BUILD SUCCESS|BUILD FAILURE|Tests run:"
  </verify>
  <done>All contest/ranking tests pass; ContestServiceImpl is facade with 400 lines or fewer</done>
</task>

</tasks>

<success_criteria_wave3>
- ContestSchedulerService.java interface exists with 7 methods
- ContestSchedulerServiceImpl.java 250 lines or fewer
- ContestServiceImpl.java 400 lines or fewer (facade)
- ContestSchedulerService uses constructor injection
- No new public API methods introduced (all existing ContestService interface methods preserved)
- ./mvnw compile succeeds
- ./mvnw test -Dtest="*Contest*,*Ranking*" succeeds
</success_criteria_wave3>

---

## Final Verification

After all 3 waves pass, run the full test suite:

```bash
cd backend-spring &amp;&amp; ./mvnw test -q 2&gt;&amp;1 | tail -20
```

JaCoCo coverage should remain above Phase 20 baseline (target/site/jacoco/index.html).
