/**
 * Sandbox execution primitives owned by the shared {@code judge-runtime}
 * library.
 *
 * <p>This package is not an App-private business domain. It is reused by the
 * independent Judge Worker and by the explicitly configured App compatibility
 * path, and it must remain storage-free.</p>
 */
package com.ulticode.modules.submission.sandbox;
