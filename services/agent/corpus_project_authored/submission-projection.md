# 只读提交投影包含什么

> Provenance: project-authored-for-u02。由本项目助手为 U02 学习切片撰写；不是用户作品，也不是授权第三方材料。

只读代理拿到的提交信息是**元数据投影**：提交 ID、题目 ID、语言、判题状态、创建时间。
提交源码、测试载荷、错误细节，以及嵌套的用户与题目对象**不会**进入模型上下文。
因此当只有状态可用时，可以讨论观察到的结果与可能原因，但不得声称定位到具体代码行、复现失败输入或运行过代码。
需要源码时必须另行核验授权、最小投影与外发范围，不默认扩权。
