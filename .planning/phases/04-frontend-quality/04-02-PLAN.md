---
phase: 04-frontend-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - management/src/views/problems/ProblemsListView.vue
  - management/src/views/problems/components/ProblemBulkActions.vue
  - management/src/views/problems/composables/useProblemFilters.ts
  - management/src/views/problems/composables/useProblemActions.ts
  - management/src/views/analytics/AnalyticsView.vue
  - management/src/views/analytics/components/UserActivityReport.vue
  - management/src/views/analytics/components/ProblemCompletionReport.vue
  - management/src/views/analytics/components/ContestParticipationReport.vue
  - management/src/views/analytics/components/RevenueReport.vue
  - management/src/views/analytics/components/PerformanceReport.vue
  - management/src/views/analytics/composables/useAnalyticsReports.ts
  - management/src/views/moderation/ModerationQueueView.vue
  - management/src/views/moderation/components/BatchActionDialog.vue
  - management/src/views/moderation/composables/useModerationFilters.ts
  - management/src/views/settings/SettingsView.vue
  - management/src/views/settings/components/GeneralSettings.vue
  - management/src/views/settings/components/EmailSettings.vue
  - management/src/views/settings/components/RateLimitSettings.vue
  - management/src/views/settings/components/UploadSettings.vue
  - management/src/views/settings/components/FeatureToggleSettings.vue
  - management/src/components/problem/HiddenTestCasesEditor.vue
  - management/src/components/problem/components/TestCaseForm.vue
  - management/src/components/problem/components/TestCaseList.vue
  - management/src/components/problem/components/TestCaseDetail.vue
  - management/src/components/problem/composables/useTestCases.ts
  - management/src/stores/admin/moderation.ts
  - management/src/stores/admin/moderation/queue.ts
  - management/src/stores/admin/moderation/reports.ts
  - management/src/stores/admin/moderation/appeals.ts
  - management/src/stores/admin/moderation/actions.ts
  - management/src/stores/admin/moderation/index.ts
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "ProblemsListView (1224 lines) has 913 lines of script extracted into composables; parent under 500 lines"
    - "AnalyticsView (881 lines) split by report tabs; parent under 500 lines"
    - "ModerationQueueView (768 lines) split with composable and dialog; parent under 500 lines"
    - "SettingsView (627 lines) split into 5 settings tab components; parent under 500 lines"
    - "HiddenTestCasesEditor (602 lines) split into form, list, detail components and composable; parent under 500 lines"
    - "Moderation store (600 lines) split into domain-focused sub-modules with re-exporting index"
    - "Management frontend builds with zero TypeScript errors (vue-tsc --build && vite build)"
  artifacts:
    - path: "management/src/views/problems/ProblemsListView.vue"
      provides: "Problem management orchestrator under 500 lines"
      max_lines: 500
    - path: "management/src/views/problems/composables/useProblemFilters.ts"
      provides: "Filter state management with URL sync"
    - path: "management/src/views/problems/composables/useProblemActions.ts"
      provides: "CRUD and bulk operation handlers"
    - path: "management/src/views/analytics/AnalyticsView.vue"
      provides: "Analytics tab orchestrator under 500 lines"
      max_lines: 500
    - path: "management/src/views/settings/SettingsView.vue"
      provides: "Settings tab orchestrator under 500 lines"
      max_lines: 500
    - path: "management/src/components/problem/HiddenTestCasesEditor.vue"
      provides: "Test case editor orchestrator under 500 lines"
      max_lines: 500
    - path: "management/src/stores/admin/moderation/index.ts"
      provides: "Re-exported combined moderation store"
  key_links:
    - from: "management/src/views/problems/ProblemsListView.vue"
      to: "management/src/views/problems/composables/useProblemFilters.ts"
      via: "composable import"
      pattern: "useProblemFilters\\(\\)"
    - from: "management/src/views/problems/ProblemsListView.vue"
      to: "management/src/views/problems/composables/useProblemActions.ts"
      via: "composable import"
      pattern: "useProblemActions\\(\\)"
    - from: "management/src/views/analytics/AnalyticsView.vue"
      to: "management/src/views/analytics/components/UserActivityReport.vue"
      via: "import + tab rendering"
      pattern: "import UserActivityReport"
    - from: "management/src/stores/admin/moderation/index.ts"
      to: "management/src/stores/admin/moderation/queue.ts"
      via: "re-export"
      pattern: "export.*from.*queue"
    - from: "management/src/views/moderation/ModerationQueueView.vue"
      to: "management/src/stores/admin/moderation/index.ts"
      via: "store import"
      pattern: "useModerationStore|moderation"
---

<objective>
Split 5 oversized management Vue components and 1 oversized Pinia store into smaller co-located sub-components, composables, and store modules.

Purpose: No management component exceeds 500 lines after splitting (QUAL-01). Includes the location-corrected SettingsView and HiddenTestCasesEditor (both are in management/, not console per RESEARCH.md correction). Pure refactoring with zero behavioral regressions.

Output: 5 parent components reduced under 500 lines each, 1 store split into domain sub-modules, ~15+ extracted sub-components, ~5+ extracted composables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-quality/04-CONTEXT.md
@.planning/phases/04-frontend-quality/04-RESEARCH.md

<interfaces>
<!-- Key patterns executor must follow. All from RESEARCH.md verified against codebase. -->

Co-location pattern (D-01, D-02):
- Sub-components: `views/{feature}/components/{ComponentName}.vue`
- Composables: `views/{feature}/composables/use{Feature}.ts`
- Only promote to global `composables/` if reused across 3+ views

Communication pattern (D-08, D-09):
- Props down, emit events up
- Dialog open/close state stays in parent (D-04)
- No new Pinia stores for component-local state (D-09)

Target sizes (D-10, D-11, D-12):
- Sub-components: 100-300 lines target, 500 lines hard cap
- Composables: 100-250 lines target
- Parent orchestrator: 200-450 lines target, 500 lines hard cap

Store splitting pattern (D-14):
- Split into domain-focused sub-modules: queue, reports, appeals, actions
- Re-export from index.ts for backward compatibility
- Consumers import from same path

Management-specific existing assets:
- `management/src/composables/useDataTable.ts` -- existing data table pattern
- `management/src/views/problems/components/` -- ProblemForm.vue, ProblemImportDialog.vue, BulkActionDialog.vue, BulkEditDialog.vue, FlagInfoDialog.vue already exist
- `management/src/views/moderation/components/` -- ActionHistoryTimeline.vue, EntityPreviewCard.vue, ModerationActionPanel.vue, ModerationDetailDrawer.vue already exist
- `management/src/components/analytics/` -- AnalyticsNav, AnalyticsMetricCard, AnalyticsBarList, AnalyticsTagCloud, AnalyticsHeatmap already exist

IMPORTANT: SettingsView and HiddenTestCasesEditor are in MANAGEMENT, not console.
This corrects the ROADMAP assignment error documented in RESEARCH.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split ProblemsListView, AnalyticsView, ModerationQueueView</name>
  <files>
    management/src/views/problems/ProblemsListView.vue
    management/src/views/problems/components/ProblemBulkActions.vue
    management/src/views/problems/composables/useProblemFilters.ts
    management/src/views/problems/composables/useProblemActions.ts
    management/src/views/analytics/AnalyticsView.vue
    management/src/views/analytics/components/UserActivityReport.vue
    management/src/views/analytics/components/ProblemCompletionReport.vue
    management/src/views/analytics/components/ContestParticipationReport.vue
    management/src/views/analytics/components/RevenueReport.vue
    management/src/views/analytics/components/PerformanceReport.vue
    management/src/views/analytics/composables/useAnalyticsReports.ts
    management/src/views/moderation/ModerationQueueView.vue
    management/src/views/moderation/components/BatchActionDialog.vue
    management/src/views/moderation/composables/useModerationFilters.ts
  </files>
  <read_first>
    management/src/views/problems/ProblemsListView.vue
    management/src/views/problems/components/BulkActionDialog.vue
    management/src/views/problems/components/BulkEditDialog.vue
    management/src/composables/useDataTable.ts
    management/src/views/analytics/AnalyticsView.vue
    management/src/views/moderation/ModerationQueueView.vue
    management/src/views/moderation/components/ModerationActionPanel.vue
  </read_first>
  <action>
Split 3 largest management components. Process each independently. After each component split, run `cd management && npx vue-tsc --build` to verify before proceeding to the next.

**Component 1: ProblemsListView.vue (1224 lines -> ~350 lines parent)**

Extreme script-heavy (913 lines script, 187 template). Already uses `useDataTable` composable. Has existing co-located dialog components (BulkActionDialog, BulkEditDialog, FlagInfoDialog, ProblemImportDialog). Primary strategy: composable extraction (D-05) for the massive script section.

1. Create `management/src/views/problems/composables/useProblemFilters.ts`:
   - Extract: filter state refs (difficulty, status, published, sortBy, sortOrder, searchQuery)
   - Extract: URL synchronization logic (reading from/writing to route query params)
   - Extract: debounced filter update logic (use @vueuse/core watchDebounced if original does)
   - Extract: computed filter params object for API calls
   - Return filter state and update functions

2. Create `management/src/views/problems/composables/useProblemActions.ts`:
   - Extract: single operations (delete, flag, publish/unpublish)
   - Extract: bulk operations (bulk delete, bulk flag, bulk publish)
   - Extract: import/export operations
   - Extract: operation loading states and success/error feedback
   - Return action functions and loading states

3. Create `management/src/views/problems/components/ProblemBulkActions.vue`:
   - Extract: bulk action buttons UI (select all, bulk delete, bulk flag, bulk publish)
   - Props: `selectedCount`, `loading`. Emits: `bulk-delete`, `bulk-flag`, `bulk-publish`, `clear-selection`
   - Note: Do NOT duplicate existing BulkActionDialog/BulkEditDialog -- this component is the toolbar that triggers them

4. Rewrite ProblemsListView.vue:
   - Import both composables, ProblemBulkActions, and existing dialog components
   - Wire useProblemFilters with useDataTable
   - Wire useProblemActions with dialog triggers
   - Keep template structure identical

**Component 2: AnalyticsView.vue (881 lines -> ~250 lines parent)**

Tab-heavy with multiple report sections. Each report has its own data source, loading state, and display logic. Already imports shared analytics components (AnalyticsNav, AnalyticsMetricCard, etc.). Strategy: tab splitting (D-03).

1. Create `management/src/views/analytics/composables/useAnalyticsReports.ts`:
   - Extract: report data fetching functions for each report type
   - Extract: time formatting and number formatting utilities
   - Extract: date range state and loading states per report
   - Return data and loading states

2. Create tab components in `management/src/views/analytics/components/`:
   - `UserActivityReport.vue` -- user activity metrics and charts. Receives data via props from parent
   - `ProblemCompletionReport.vue` -- problem completion stats. Receives data via props
   - `ContestParticipationReport.vue` -- contest participation metrics. Receives data via props
   - `RevenueReport.vue` -- revenue data display. Receives data via props
   - `PerformanceReport.vue` -- system performance metrics. Receives data via props
   Each tab component receives its specific report data as props and renders using existing analytics shared components.

3. Rewrite AnalyticsView.vue:
   - Import composable and tab components
   - Own active report tab state
   - Render tab navigation + selected report component

**Component 3: ModerationQueueView.vue (768 lines -> ~350 lines parent)**

Already has co-located components (ActionHistoryTimeline, EntityPreviewCard, ModerationActionPanel, ModerationDetailDrawer). Uses `useModerationStore` (the store being split in Task 2). Strategy: composable for filter state + batch action dialog.

1. Create `management/src/views/moderation/composables/useModerationFilters.ts`:
   - Extract: filter state (status, category, entityType), pagination state
   - Extract: computed filter params for API calls
   - Return filter state and update functions

2. Create `management/src/views/moderation/components/BatchActionDialog.vue`:
   - Batch moderation operations dialog
   - Props: `open`, `selectedItems`, `actionType`. Emits: `update:open`, `execute`

3. Rewrite ModerationQueueView.vue:
   - Import composable, BatchActionDialog, and existing co-located components
   - Compose queue view with filter panel, queue list, action panel, detail drawer
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/management && npx vue-tsc --build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ProblemsListView.vue under 500 lines: `wc -l management/src/views/problems/ProblemsListView.vue` shows < 500
    - AnalyticsView.vue under 500 lines: `wc -l management/src/views/analytics/AnalyticsView.vue` shows < 500
    - ModerationQueueView.vue under 500 lines: `wc -l management/src/views/moderation/ModerationQueueView.vue` shows < 500
    - useProblemFilters.ts exists: `test -f management/src/views/problems/composables/useProblemFilters.ts`
    - useProblemActions.ts exists: `test -f management/src/views/problems/composables/useProblemActions.ts`
    - useAnalyticsReports.ts exists: `test -f management/src/views/analytics/composables/useAnalyticsReports.ts`
    - useModerationFilters.ts exists: `test -f management/src/views/moderation/composables/useModerationFilters.ts`
    - UserActivityReport.vue exists: `test -f management/src/views/analytics/components/UserActivityReport.vue`
    - BatchActionDialog.vue exists: `test -f management/src/views/moderation/components/BatchActionDialog.vue`
    - vue-tsc --build exits with code 0
  </acceptance_criteria>
  <done>
ProblemsListView, AnalyticsView, and ModerationQueueView are all under 500 lines. Each has extracted composables and sub-components. useProblemFilters extracts ~200 lines of filter+URL logic from ProblemsListView's 913-line script. vue-tsc --build passes with zero errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split SettingsView, HiddenTestCasesEditor, and moderation store</name>
  <files>
    management/src/views/settings/SettingsView.vue
    management/src/views/settings/components/GeneralSettings.vue
    management/src/views/settings/components/EmailSettings.vue
    management/src/views/settings/components/RateLimitSettings.vue
    management/src/views/settings/components/UploadSettings.vue
    management/src/views/settings/components/FeatureToggleSettings.vue
    management/src/components/problem/HiddenTestCasesEditor.vue
    management/src/components/problem/components/TestCaseForm.vue
    management/src/components/problem/components/TestCaseList.vue
    management/src/components/problem/components/TestCaseDetail.vue
    management/src/components/problem/composables/useTestCases.ts
    management/src/stores/admin/moderation.ts
    management/src/stores/admin/moderation/queue.ts
    management/src/stores/admin/moderation/reports.ts
    management/src/stores/admin/moderation/appeals.ts
    management/src/stores/admin/moderation/actions.ts
    management/src/stores/admin/moderation/index.ts
  </files>
  <read_first>
    management/src/views/settings/SettingsView.vue
    management/src/components/problem/HiddenTestCasesEditor.vue
    management/src/stores/admin/moderation.ts
  </read_first>
  <action>
Split SettingsView, HiddenTestCasesEditor, and the moderation Pinia store. Process each independently.

**Component 4: SettingsView.vue (627 lines -> ~250 lines parent)**

Template-heavy (169 script, 453 template) with 5 settings sections. Each section is a natural extraction boundary. Strategy: tab/section splitting (D-03).

1. Create tab components in `management/src/views/settings/components/`:
   - `GeneralSettings.vue` -- general platform settings form. Props: `settings` (general settings object). Emits: `update` with changed fields
   - `EmailSettings.vue` -- email configuration settings. Props: `settings`. Emits: `update`
   - `RateLimitSettings.vue` -- rate limiting configuration. Props: `settings`. Emits: `update`
   - `UploadSettings.vue` -- file upload settings. Props: `settings`. Emits: `update`
   - `FeatureToggleSettings.vue` -- feature flag toggles. Props: `settings`. Emits: `update`

   Each component: call `useI18n()` directly, keep same i18n key paths as original SettingsView.

2. Rewrite SettingsView.vue:
   - Import 5 settings tab components
   - Own active settings tab state and settings data
   - Render tab navigation + selected settings component
   - Handle update events from child components

**Component 5: HiddenTestCasesEditor.vue (602 lines -> ~250 lines parent)**

Mixed script/template (319 script, 278 template). CRUD editor for test cases. Strategy: composable for CRUD + components for form, list, and detail.

1. Create `management/src/components/problem/composables/useTestCases.ts`:
   - Extract: CRUD operations for test cases (add, update, delete, reorder)
   - Extract: form state management (current test case, dirty state)
   - Extract: import/export logic if present
   - Return test case data, CRUD functions, loading states

2. Create components in `management/src/components/problem/components/`:
   - `TestCaseForm.vue` -- create/edit test case form. Props: `testCase`, `loading`. Emits: `submit`, `cancel`
   - `TestCaseList.vue` -- sidebar list of test cases. Props: `testCases`, `activeId`. Emits: `select`, `delete`, `reorder`
   - `TestCaseDetail.vue` -- active test case detail display. Props: `testCase`

3. Rewrite HiddenTestCasesEditor.vue:
   - Import composable and components
   - Compose editor layout: TestCaseList (sidebar) + TestCaseForm/TestCaseDetail (main area)

**Store 6: moderation.ts (600 lines -> index.ts re-exporting sub-modules)**

Pinia store split into domain-focused sub-modules per D-14. Strategy: split by domain, re-export for backward compatibility.

1. Before modifying, grep for all imports of `moderation` store across management/:
   `grep -rn "from.*stores/admin/moderation" management/src/` -- identify all consumers

2. Create sub-module files in `management/src/stores/admin/moderation/`:
   - `queue.ts` -- queue CRUD operations, queue pagination state
   - `reports.ts` -- report management functions and state
   - `appeals.ts` -- appeal management functions and state
   - `actions.ts` -- moderation actions (resolve, ban, etc.) and action state
   - `index.ts` -- re-export combined store using Pinia's `defineStore` that composes the sub-modules

3. The index.ts pattern:
   ```typescript
   // Each sub-module exports a setup function with its state/getters/actions
   // index.ts combines them into a single defineStore for backward compatibility
   export const useModerationStore = defineStore('admin/moderation', () => {
     const queue = useQueueModule();
     const reports = useReportsModule();
     const appeals = useAppealsModule();
     const actions = useActionsModule();
     return { ...queue, ...reports, ...appeals, ...actions };
   });
   ```

4. Update the original `management/src/stores/admin/moderation.ts` to re-export from the new directory:
   ```typescript
   export { useModerationStore } from './moderation/index';
   ```
   This preserves backward compatibility for any direct imports from the old path.

5. Verify: `grep -rn "useModerationStore" management/src/` -- all imports still resolve correctly.

After all 3 splits, run: `cd management && npx vue-tsc --build && npx vite build`
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/management && npx vue-tsc --build 2>&1 | tail -5 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsView.vue under 500 lines: `wc -l management/src/views/settings/SettingsView.vue` shows < 500
    - HiddenTestCasesEditor.vue under 500 lines: `wc -l management/src/components/problem/HiddenTestCasesEditor.vue` shows < 500
    - GeneralSettings.vue exists: `test -f management/src/views/settings/components/GeneralSettings.vue`
    - EmailSettings.vue exists: `test -f management/src/views/settings/components/EmailSettings.vue`
    - TestCaseForm.vue exists: `test -f management/src/components/problem/components/TestCaseForm.vue`
    - useTestCases.ts exists: `test -f management/src/components/problem/composables/useTestCases.ts`
    - moderation/index.ts exists: `test -f management/src/stores/admin/moderation/index.ts`
    - moderation/queue.ts exists: `test -f management/src/stores/admin/moderation/queue.ts`
    - Original moderation.ts re-exports: `grep "export.*from.*moderation/index" management/src/stores/admin/moderation.ts`
    - vue-tsc --build exits with code 0
    - vite build exits with code 0
  </acceptance_criteria>
  <done>
SettingsView, HiddenTestCasesEditor are under 500 lines. Moderation store split into domain sub-modules (queue, reports, appeals, actions) with backward-compatible re-export from original path. Management frontend builds successfully with vue-tsc --build and vite build. All 14 items (13 components + 1 store) are now under 500 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure frontend refactoring with no security-relevant changes. No new endpoints, no data flow changes, no authentication/authorization modifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | S | N/A | accept | No security-sensitive changes in this phase |
| T-04-08 | T | N/A | accept | No data flow changes -- same API calls, same stores |
| T-04-09 | I | N/A | accept | No input/output changes -- same rendering logic |
| T-04-10 | D | N/A | accept | No availability impact from file restructuring |
| T-04-11 | E | N/A | accept | No privilege changes |
| T-04-12 | E | N/A | accept | No new dependencies or external calls |
</threat_model>

<verification>
1. All 5 management source files under 500 lines: `for f in management/src/views/problems/ProblemsListView.vue management/src/views/analytics/AnalyticsView.vue management/src/views/moderation/ModerationQueueView.vue management/src/views/settings/SettingsView.vue management/src/components/problem/HiddenTestCasesEditor.vue; do echo "$f: $(wc -l < $f) lines"; done`
2. Store file under 500 lines: `wc -l management/src/stores/admin/moderation.ts`
3. TypeScript check passes: `cd management && npx vue-tsc --build`
4. Production build passes: `cd management && npx vite build`
5. Store imports still resolve: `grep -rn "useModerationStore" management/src/ | head -10`
</verification>

<success_criteria>
- All 5 management components are under 500 lines each
- Moderation store split into sub-modules under 300 lines each
- All extracted sub-components and composables exist in co-located directories
- `vue-tsc --build` passes with zero errors
- `vite build` completes successfully
- All existing imports of useModerationStore still resolve correctly
- No behavioral changes visible to users
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-quality/04-02-SUMMARY.md`
</output>
