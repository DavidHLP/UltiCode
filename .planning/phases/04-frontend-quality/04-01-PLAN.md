---
phase: 04-frontend-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/views/personal/ProblemListsView.vue
  - console/src/views/personal/components/MyListsTab.vue
  - console/src/views/personal/components/SavedListsTab.vue
  - console/src/views/personal/components/CategoriesTab.vue
  - console/src/views/personal/components/CreateListDialog.vue
  - console/src/views/personal/components/DeleteListDialog.vue
  - console/src/views/personal/components/CreateCategoryDialog.vue
  - console/src/views/personal/components/EditCategoryDialog.vue
  - console/src/views/personal/composables/useProblemLists.ts
  - console/src/views/problem-list/ProblemListView.vue
  - console/src/views/problem-list/components/EditListDialog.vue
  - console/src/views/problem-list/components/DeleteListDialog.vue
  - console/src/views/problem-list/components/AddProblemsDialog.vue
  - console/src/views/problem-list/composables/useProblemListOperations.ts
  - console/src/views/contest/detailed/ContestDetailView.vue
  - console/src/views/contest/detailed/components/ContestHeader.vue
  - console/src/views/contest/detailed/components/ContestRegistration.vue
  - console/src/views/contest/detailed/components/ContestProblemList.vue
  - console/src/views/contest/detailed/components/ContestRankingTable.vue
  - console/src/views/contest/detailed/composables/useContestStatus.ts
  - console/src/views/contest/detailed/composables/useContestRankings.ts
  - console/src/views/problems/submissions/SubmissionsDetail.vue
  - console/src/views/problems/submissions/components/SubmissionCodeBlock.vue
  - console/src/views/problems/submissions/components/SubmissionTestResults.vue
  - console/src/views/problems/submissions/composables/useSubmissionDetail.ts
  - console/src/views/personal/PersonalView.vue
  - console/src/views/personal/components/UserProfileCard.vue
  - console/src/views/personal/components/UserStatsPanel.vue
  - console/src/views/problems/ProblemDetailView.vue
  - console/src/views/problems/composables/useProblemLayout.ts
  - console/src/views/problems/composables/useProblemPanels.ts
  - console/src/components/problem/ProblemExplorer.vue
  - console/src/components/problem/components/ProblemFilterPanel.vue
  - console/src/components/problem/components/ProblemResultList.vue
  - console/src/components/problem/composables/useProblemExplorer.ts
  - console/src/features/sider/Calendars.vue
  - console/src/features/sider/components/CalendarGrid.vue
  - console/src/features/sider/components/ContestList.vue
  - console/src/features/sider/components/CalendarEventDialog.vue
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "ProblemListsView (1356 lines) split into tab components and composable; parent under 500 lines"
    - "ProblemListView (804 lines) split into dialog components and composable; parent under 500 lines"
    - "ContestDetailView (1039 lines) split into section components and composables; parent under 500 lines"
    - "SubmissionsDetail (867 lines) split with composable extracting 500+ script lines; parent under 500 lines"
    - "PersonalView (666 lines) split into profile card and stats components; parent under 500 lines"
    - "ProblemDetailView (692 lines) has layout and panel logic extracted to composables; parent under 500 lines"
    - "ProblemExplorer (642 lines) split into filter panel, result list, and composable; parent under 500 lines"
    - "Calendars (790 lines) split into calendar grid, contest list, and event dialog; parent under 500 lines"
    - "Console frontend builds with zero TypeScript errors (vue-tsc --build && vite build)"
  artifacts:
    - path: "console/src/views/personal/ProblemListsView.vue"
      provides: "Tab orchestrator under 500 lines"
      max_lines: 500
    - path: "console/src/views/personal/components/MyListsTab.vue"
      provides: "My lists tab content"
    - path: "console/src/views/personal/composables/useProblemLists.ts"
      provides: "Problem lists data fetching and computed state"
    - path: "console/src/views/contest/detailed/ContestDetailView.vue"
      provides: "Contest detail orchestrator under 500 lines"
      max_lines: 500
    - path: "console/src/views/contest/detailed/composables/useContestStatus.ts"
      provides: "Timer and status computation"
    - path: "console/src/views/problems/submissions/composables/useSubmissionDetail.ts"
      provides: "Submission status computation and data logic"
    - path: "console/src/components/problem/composables/useProblemExplorer.ts"
      provides: "Problem filter and pagination state"
  key_links:
    - from: "console/src/views/personal/ProblemListsView.vue"
      to: "console/src/views/personal/components/MyListsTab.vue"
      via: "import + component rendering"
      pattern: "import MyListsTab"
    - from: "console/src/views/personal/ProblemListsView.vue"
      to: "console/src/views/personal/composables/useProblemLists.ts"
      via: "composable destructuring"
      pattern: "useProblemLists\\(\\)"
    - from: "console/src/views/contest/detailed/ContestDetailView.vue"
      to: "console/src/views/contest/detailed/composables/useContestStatus.ts"
      via: "composable import"
      pattern: "useContestStatus\\(\\)"
    - from: "console/src/views/problems/submissions/SubmissionsDetail.vue"
      to: "console/src/views/problems/submissions/composables/useSubmissionDetail.ts"
      via: "composable import"
      pattern: "useSubmissionDetail\\(\\)"
---

<objective>
Split 8 oversized console Vue components into smaller co-located sub-components and composables.

Purpose: No console component exceeds 500 lines after splitting (QUAL-01). Largest components first per D-13. Pure refactoring with zero behavioral regressions.

Output: 8 parent components reduced under 500 lines each, ~15+ extracted sub-components in co-located `components/` directories, ~7+ extracted composables in co-located `composables/` directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-quality/04-CONTEXT.md
@.planning/phases/04-frontend-quality/04-RESEARCH.md

<interfaces>
<!-- Key patterns executor must follow. All from RESEARCH.md verified against codebase. -->

Co-location pattern (D-01, D-02):
- Sub-components: `views/{feature}/components/{ComponentName}.vue`
- Composables: `views/{feature}/composables/use{Feature}.ts`
- Only promote to global `composables/` if reused across 3+ views

Communication pattern (D-08, D-09):
- Props down, emit events up
- Dialog open/close state stays in parent (D-04)
- provide/inject only for 3+ level nesting

Target sizes (D-10, D-11, D-12):
- Sub-components: 100-300 lines target, 500 lines hard cap
- Composables: 100-250 lines target
- Parent orchestrator: 200-450 lines target, 500 lines hard cap

i18n rule:
- Keep exact same `t("key.path")` calls in extracted components
- Call `useI18n()` directly in child components (works via Vue provide/inject)

Existing composable patterns to follow:
- `console/src/views/problems/useProblemDetail.ts` -- data fetching composable
- `console/src/composables/useSWR.ts` -- data fetching with caching
- `console/src/composables/contest/useContestSocket.ts` -- lifecycle cleanup pattern
- `console/src/views/problems/problem-context.ts` -- provide/inject with typed InjectionKey
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split ProblemListsView, ProblemListView, ContestDetailView</name>
  <files>
    console/src/views/personal/ProblemListsView.vue
    console/src/views/personal/components/MyListsTab.vue
    console/src/views/personal/components/SavedListsTab.vue
    console/src/views/personal/components/CategoriesTab.vue
    console/src/views/personal/components/CreateListDialog.vue
    console/src/views/personal/components/DeleteListDialog.vue
    console/src/views/personal/components/CreateCategoryDialog.vue
    console/src/views/personal/components/EditCategoryDialog.vue
    console/src/views/personal/composables/useProblemLists.ts
    console/src/views/problem-list/ProblemListView.vue
    console/src/views/problem-list/components/EditListDialog.vue
    console/src/views/problem-list/components/DeleteListDialog.vue
    console/src/views/problem-list/components/AddProblemsDialog.vue
    console/src/views/problem-list/composables/useProblemListOperations.ts
    console/src/views/contest/detailed/ContestDetailView.vue
    console/src/views/contest/detailed/components/ContestHeader.vue
    console/src/views/contest/detailed/components/ContestRegistration.vue
    console/src/views/contest/detailed/components/ContestProblemList.vue
    console/src/views/contest/detailed/components/ContestRankingTable.vue
    console/src/views/contest/detailed/composables/useContestStatus.ts
    console/src/views/contest/detailed/composables/useContestRankings.ts
  </files>
  <read_first>
    console/src/views/personal/ProblemListsView.vue
    console/src/views/personal/components/PersonalPageShell.vue
    console/src/views/problem-list/ProblemListView.vue
    console/src/views/contest/detailed/ContestDetailView.vue
    console/src/composables/contest/useContestSocket.ts
    console/src/composables/useSWR.ts
  </read_first>
  <action>
Split 3 largest console components. Process each independently. After each component split, run `cd console && npx vue-tsc --build` to verify before proceeding to the next.

**Component 1: ProblemListsView.vue (1356 lines -> ~300 lines parent)**

This is a 4-tab interface (my-lists, saved, categories, plus a featured/header section) with 5+ dialogs for CRUD operations. Split strategy: Tab splitting (D-03) + dialog extraction (D-04) + composable for data (D-05).

1. Create `console/src/views/personal/composables/useProblemLists.ts`:
   - Extract: data fetching (fetchProblemListsOverview call), loading state, searchQuery ref
   - Extract: sorted computed properties (sortedMyLists, sortedSavedLists, sortedCategories, totalSavedCount)
   - Extract: CRUD operation functions (createList, deleteList, saveList, unsaveList, createCategory, editCategory, deleteCategory)
   - Use `onMounted` for initial data load (follow useProblemDetail.ts pattern)
   - Return all state and functions for parent to destructure and pass as props

2. Create tab components in `console/src/views/personal/components/`:
   - `MyListsTab.vue` -- renders the "my lists" tab content. Receives `lists` prop, emits `delete` and `create` events
   - `SavedListsTab.vue` -- renders saved/bookmarked lists. Receives `lists` prop, emits `unsave` event
   - `CategoriesTab.vue` -- renders categories. Receives `categories` prop, emits `edit` and `delete` events

3. Create dialog components in `console/src/views/personal/components/`:
   - `CreateListDialog.vue` -- form with name, description, isPublic. Props: `open`, `loading`. Emits: `update:open`, `submit`
   - `DeleteListDialog.vue` -- confirmation dialog. Props: `open`, `listName`. Emits: `update:open`, `confirm`
   - `CreateCategoryDialog.vue` -- category creation form. Props: `open`. Emits: `update:open`, `submit`
   - `EditCategoryDialog.vue` -- category edit form. Props: `open`, `category`. Emits: `update:open`, `submit`

4. Rewrite ProblemListsView.vue as tab orchestrator:
   - Import composable, tab components, dialog components
   - Own `activeTab` ref, dialog open/close refs (D-04)
   - Template: PersonalPageShell > Tabs > TabsContent wrapping each tab component, Dialog wrappers for each dialog
   - Keep exact same i18n key paths from original

**Component 2: ProblemListView.vue (804 lines -> ~350 lines parent)**

Dialog-heavy component with CRUD operations for a single problem list. Has existing co-located `ProblemListAnalytics.vue`.

1. Create `console/src/views/problem-list/composables/useProblemListOperations.ts`:
   - Extract: list data fetching, fork, delete, update operations
   - Extract: add/remove problem, save/unsave, move to category functions
   - Extract: loading states for each operation

2. Create dialog components in `console/src/views/problem-list/components/`:
   - `EditListDialog.vue` -- edit list metadata form. Props: `open`, `list`. Emits: `update:open`, `submit`
   - `DeleteListDialog.vue` -- delete confirmation. Props: `open`, `listName`. Emits: `update:open`, `confirm`
   - `AddProblemsDialog.vue` -- search and add problems to list. Props: `open`, `listId`. Emits: `update:open`, `add`

3. Rewrite ProblemListView.vue:
   - Import composable and dialog components
   - Own dialog open/close state
   - Render list content + dialog wrappers

**Component 3: ContestDetailView.vue (1039 lines -> ~350 lines parent)**

Section-heavy component with timer logic, registration, problem list, and rankings. Uses existing `useContestSocket` composable and `VirtualContestTimer` component.

1. Create `console/src/views/contest/detailed/composables/useContestStatus.ts`:
   - Extract: formatCountdown, updateStatusTimer, getContestEndTimeMs functions
   - Extract: statusIntervalId ref and cleanup in onUnmounted (follow useContestSocket.ts cleanup pattern)
   - Extract: contest status computed (not started, running, ended)
   - CRITICAL: Call onUnmounted inside composable to clear interval (Pitfall 6 from RESEARCH.md)

2. Create `console/src/views/contest/detailed/composables/useContestRankings.ts`:
   - Extract: loadRankings function, ranking data state
   - Extract: rankingIntervalId for live polling (setInterval every 30s)
   - CRITICAL: Call onUnmounted inside composable to clear interval

3. Create section components in `console/src/views/contest/detailed/components/`:
   - `ContestHeader.vue` -- contest title, badges, status display. Props: `contest`, `status`
   - `ContestRegistration.vue` -- register/unregister buttons. Props: `contest`, `isRegistered`. Emits: `register`, `unregister`
   - `ContestProblemList.vue` -- problem table. Props: `problems`, `contestStatus`
   - `ContestRankingTable.vue` -- rankings with country flags. Props: `rankings`, `loading`

4. Rewrite ContestDetailView.vue:
   - Import composables and section components
   - Own registration state, pass data down as props
   - Compose sections in template with same layout as original
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/console && npx vue-tsc --build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ProblemListsView.vue is under 500 lines: `wc -l console/src/views/personal/ProblemListsView.vue` shows < 500
    - ProblemListView.vue is under 500 lines: `wc -l console/src/views/problem-list/ProblemListView.vue` shows < 500
    - ContestDetailView.vue is under 500 lines: `wc -l console/src/views/contest/detailed/ContestDetailView.vue` shows < 500
    - useProblemLists.ts exists: `test -f console/src/views/personal/composables/useProblemLists.ts`
    - useProblemListOperations.ts exists: `test -f console/src/views/problem-list/composables/useProblemListOperations.ts`
    - useContestStatus.ts exists: `test -f console/src/views/contest/detailed/composables/useContestStatus.ts`
    - useContestRankings.ts exists: `test -f console/src/views/contest/detailed/composables/useContestRankings.ts`
    - MyListsTab.vue exists: `test -f console/src/views/personal/components/MyListsTab.vue`
    - ContestHeader.vue exists: `test -f console/src/views/contest/detailed/components/ContestHeader.vue`
    - vue-tsc --build exits with code 0
  </acceptance_criteria>
  <done>
ProblemListsView, ProblemListView, and ContestDetailView are all under 500 lines. Each has co-located components/ and composables/ directories with extracted sub-components and data logic. vue-tsc --build passes with zero errors. No behavioral changes -- all tabs, dialogs, and contest sections render identically.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split SubmissionsDetail, PersonalView, ProblemDetailView, ProblemExplorer, Calendars</name>
  <files>
    console/src/views/problems/submissions/SubmissionsDetail.vue
    console/src/views/problems/submissions/components/SubmissionCodeBlock.vue
    console/src/views/problems/submissions/components/SubmissionTestResults.vue
    console/src/views/problems/submissions/composables/useSubmissionDetail.ts
    console/src/views/personal/PersonalView.vue
    console/src/views/personal/components/UserProfileCard.vue
    console/src/views/personal/components/UserStatsPanel.vue
    console/src/views/problems/ProblemDetailView.vue
    console/src/views/problems/composables/useProblemLayout.ts
    console/src/views/problems/composables/useProblemPanels.ts
    console/src/components/problem/ProblemExplorer.vue
    console/src/components/problem/components/ProblemFilterPanel.vue
    console/src/components/problem/components/ProblemResultList.vue
    console/src/components/problem/composables/useProblemExplorer.ts
    console/src/features/sider/Calendars.vue
    console/src/features/sider/components/CalendarGrid.vue
    console/src/features/sider/components/ContestList.vue
    console/src/features/sider/components/CalendarEventDialog.vue
  </files>
  <read_first>
    console/src/views/problems/submissions/SubmissionsDetail.vue
    console/src/views/personal/PersonalView.vue
    console/src/views/personal/components/ActivityHeatmap.vue
    console/src/views/problems/ProblemDetailView.vue
    console/src/views/problems/useProblemDetail.ts
    console/src/views/problems/problem-context.ts
    console/src/components/problem/ProblemExplorer.vue
    console/src/features/sider/Calendars.vue
  </read_first>
  <action>
Split remaining 5 console components. Process each independently. After each component split, run `cd console && npx vue-tsc --build` to verify before proceeding to the next.

**Component 4: SubmissionsDetail.vue (867 lines -> ~300 lines parent)**

Script-heavy (568 lines script, 299 template). The script contains status computation, time parsing, runtime calculation, and all computed properties for submission state. Primary strategy: composable extraction (D-05).

1. Create `console/src/views/problems/submissions/composables/useSubmissionDetail.ts`:
   - Extract: status computation functions, parseMs, runtime calculation
   - Extract: all computed properties related to submission status (isAccepted, isWrongAnswer, isRuntimeError, etc.)
   - Extract: submission data fetching if present
   - Return state and computed values for parent

2. Create section components:
   - `SubmissionCodeBlock.vue` -- code display with syntax highlighting. Props: `code`, `language`
   - `SubmissionTestResults.vue` -- test result details table. Props: `results`, `testCases`

3. Rewrite SubmissionsDetail.vue:
   - Import composable and section components
   - Keep template structure identical, delegate to child components

**Component 5: PersonalView.vue (666 lines -> ~350 lines parent)**

Template-heavy (191 script, 475 template). Already has co-located components (ActivityHeatmap, SkillRadarChart, SubmissionHistoryChart, LearningProgressChart, PersonalPageShell, PersonalPageHeader). Strategy: extract remaining profile sections.

1. Create section components in `console/src/views/personal/components/`:
   - `UserProfileCard.vue` -- avatar, name, bio, social links section. Props: `user` (user profile data). Keep same i18n keys
   - `UserStatsPanel.vue` -- easy/medium/hard stats display. Props: `stats` (stats object with difficulty breakdown)

2. Rewrite PersonalView.vue:
   - Import new components alongside existing ones
   - Compose profile page with UserProfileCard, UserStatsPanel, plus existing chart components

**Component 6: ProblemDetailView.vue (692 lines -> ~400 lines parent)**

Already well-decomposed (uses useProblemDetail.ts composable, problem-context.ts for provide/inject, and 6 sub-views). Script bloat (606 lines) is from layout configuration (header groups, panel config), connector component definitions, and panel toggle logic.

1. Create `console/src/views/problems/composables/useProblemLayout.ts`:
   - Extract: layout tree configuration, header group setup
   - Extract: LayoutNode and HeaderGroup configuration arrays
   - Return layout configuration for parent to consume

2. Create `console/src/views/problems/composables/useProblemPanels.ts`:
   - Extract: side panel toggle logic, notes panel toggle
   - Extract: panel visibility state refs

3. Rewrite ProblemDetailView.vue:
   - Import both new composables alongside existing useProblemDetail
   - Keep provide/inject for ProblemContextKey (already established pattern)
   - Keep connector component definitions inline (they are tightly coupled to the layout)
   - Import layout config and panel state from composables

**Component 7: ProblemExplorer.vue (642 lines -> ~250 lines parent)**

Located in shared `console/src/components/problem/` (not a view directory). Mixed script/template. Strategy: composable for filter/pagination state, components for filter panel and result list.

1. Create `console/src/components/problem/composables/useProblemExplorer.ts`:
   - Extract: filter state (difficulty, tags, search query), pagination state, sorting state
   - Extract: API fetching logic for problem list
   - Extract: computed filtered/sorted results
   - Use @vueuse/core `watchDebounced` for search if original does

2. Create components in `console/src/components/problem/components/`:
   - `ProblemFilterPanel.vue` -- difficulty badges, tag selection, search input. Props: `filters`. Emits: `update:filters`
   - `ProblemResultList.vue` -- scrollable problem list items. Props: `problems`, `loading`. Emits: `select`

3. Rewrite ProblemExplorer.vue:
   - Import composable and components
   - Orchestrator: wire filter panel events to composable, pass results to result list

**Component 8: Calendars.vue (790 lines -> ~300 lines parent)**

Template-heavy (280 script, 510 template). Has calendar grid, contest list, and event dialogs. Located in `features/sider/`. Strategy: extract calendar grid, contest list, and event dialog.

1. Create components in `console/src/features/sider/components/`:
   - `CalendarGrid.vue` -- the actual calendar grid display. Props: `events`, `selectedDate`. Emits: `select-date`, `select-event`
   - `ContestList.vue` -- contest items list below/beside calendar. Props: `contests`, `loading`. Emits: `select`
   - `CalendarEventDialog.vue` -- create/edit contest event dialog. Props: `open`, `event`. Emits: `update:open`, `submit`

2. Rewrite Calendars.vue:
   - Import components
   - Own selected date state, event data, dialog open/close state
   - Compose calendar layout with child components
  </action>
  <verify>
    <automated>cd /home/davidhlp/project/UltiCode-Public-Next/console && npx vue-tsc --build 2>&1 | tail -5 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionsDetail.vue under 500 lines: `wc -l console/src/views/problems/submissions/SubmissionsDetail.vue` shows < 500
    - PersonalView.vue under 500 lines: `wc -l console/src/views/personal/PersonalView.vue` shows < 500
    - ProblemDetailView.vue under 500 lines: `wc -l console/src/views/problems/ProblemDetailView.vue` shows < 500
    - ProblemExplorer.vue under 500 lines: `wc -l console/src/components/problem/ProblemExplorer.vue` shows < 500
    - Calendars.vue under 500 lines: `wc -l console/src/features/sider/Calendars.vue` shows < 500
    - useSubmissionDetail.ts exists: `test -f console/src/views/problems/submissions/composables/useSubmissionDetail.ts`
    - useProblemLayout.ts exists: `test -f console/src/views/problems/composables/useProblemLayout.ts`
    - useProblemExplorer.ts exists: `test -f console/src/components/problem/composables/useProblemExplorer.ts`
    - UserProfileCard.vue exists: `test -f console/src/views/personal/components/UserProfileCard.vue`
    - CalendarGrid.vue exists: `test -f console/src/features/sider/components/CalendarGrid.vue`
    - vue-tsc --build exits with code 0
    - vite build exits with code 0
  </acceptance_criteria>
  <done>
All 8 console components are under 500 lines. Each has extracted sub-components in co-located components/ directories and data logic in co-located composables/ directories. Console frontend builds successfully with vue-tsc --build and vite build. No behavioral regressions -- all views render identically to pre-split state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure frontend refactoring with no security-relevant changes. No new endpoints, no data flow changes, no authentication/authorization modifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | N/A | accept | No security-sensitive changes in this phase |
| T-04-02 | T | N/A | accept | No data flow changes -- same API calls, same stores |
| T-04-03 | I | N/A | accept | No input/output changes -- same rendering logic |
| T-04-04 | D | N/A | accept | No availability impact from file restructuring |
| T-04-05 | E | N/A | accept | No privilege changes |
| T-04-06 | E | N/A | accept | No new dependencies or external calls |
</threat_model>

<verification>
1. All 8 console source files are under 500 lines: `for f in console/src/views/personal/ProblemListsView.vue console/src/views/problem-list/ProblemListView.vue console/src/views/contest/detailed/ContestDetailView.vue console/src/views/problems/submissions/SubmissionsDetail.vue console/src/views/personal/PersonalView.vue console/src/views/problems/ProblemDetailView.vue console/src/components/problem/ProblemExplorer.vue console/src/features/sider/Calendars.vue; do echo "$f: $(wc -l < $f) lines"; done`
2. TypeScript check passes: `cd console && npx vue-tsc --build`
3. Production build passes: `cd console && npx vite build`
4. No i18n key changes: grep extracted components for useI18n usage -- all t() calls use same key paths as original
</verification>

<success_criteria>
- All 8 console components are under 500 lines each
- All extracted sub-components and composables exist in co-located directories
- `vue-tsc --build` passes with zero errors
- `vite build` completes successfully
- No behavioral changes visible to users (same UI, same interactions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-quality/04-01-SUMMARY.md`
</output>
