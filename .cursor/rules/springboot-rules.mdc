---
description: UltiCode Spring Boot backend project rules
alwaysApply: true
globs: ["services/**/src/**/*.java", "{backend-auth,backend-admin,backend-app}/**/*.java"]
---

# UltiCode Spring Boot 项目规则

## 架构分层

三个 root-level owner 服务遵循 `Controller → Service/Projection/Port → Mapper → Entity` 分层，但入口 package 不同：
- `services/auth/`：`com.ulticode.auth`，HTTP 入口在 `adapter/in/web/`，账号/session/security 由 owner 自持。
- `services/admin/`：`com.ulticode.admin` 与 `com.ulticode.modules.admin`，管理 BFF 和治理能力由 owner 自持。
- `services/app/`：`com.ulticode.app` 与 `com.ulticode.modules.*`，OJ 和普通用户业务由 owner 自持。

`services/` shared modules (platform, api) and app-private domains (app/modules/) continue the same controller/service/projection/port/mapper/entity seams; do not infer a root-owner package from the shared reactor layout.

## 响应封装

所有 API 响应使用 `Result<T>`：
- `Result.success(data)` / `Result.success()` — 成功
- `throw new BusinessException(ErrorCode.XXX)` — 业务异常
- `Result.error(code, message)` — 错误响应
- 分页：`PageResult.of(list, total, page, limit)`

Result 结构：`{ code: 0, message: "success", data: {}, traceId: "t-xxx" }`

## 安全注解

- `@RequireRole("ADMIN")` — 角色校验
- `@CheckBan` — 封禁检查
- `@RateLimit` — 速率限制
- `@Audited` — 操作审计
- `@CurrentUser` — 当前用户注入

## Entity 约定

- 主键 String (UUID)，必备字段 id/create_time/update_time
- 逻辑删除：数据库 `is_deleted`，Entity 属性名 `deleted`
- 布尔字段：数据库 `is_xxx`，Entity 属性名 `xxx`
- 自定义查询用 `@Select/@Update` 注解，禁止 XML Mapper