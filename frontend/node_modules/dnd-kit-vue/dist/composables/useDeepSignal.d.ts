import { ShallowRef } from 'vue';
/** Trigger a re-render when reading signal properties of an object. */
export declare function useDeepSignal<T extends object | null | undefined>(target: T): ShallowRef<T>;
