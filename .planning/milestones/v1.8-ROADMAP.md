# Milestone v1.8: Technical Debt III

**Status:** ✅ SHIPPED 2026-04-22
**Phases:** 34-37
**Total Plans:** 4

## Overview

技术债修复第三阶段。修复 Swagger UI 可访问性、CI Flyway URL、Achievement 异步化、Forum Stats 真实数据。

## Phases

### Phase 34: Swagger UI 修复

**Goal**: Swagger UI 可通过 /swagger-ui.html 正常访问
**Depends on**: Nothing
**Requirements**: DEPS-01
**Success Criteria**:
  1. Swagger UI 页面可正常加载（HTTP 200）
  2. API 文档显示所有 REST endpoints
  3. Try-out 功能可正常发送请求
**Plans**: 1/1

Plans:
- [x] 34-01: 启用 SwaggerConfig.java + springdoc 配置

**Details:**
- SwaggerConfig.java 已启用，OpenAPI bean 配置 JWT Bearer 认证
- springdoc 降级到 2.6.0 解决与 Spring Boot 3.2.5 的兼容性问题
- API docs 路径为 `/api-docs`（springdoc 2.x）

---

### Phase 35: Flyway URL 修复

**Goal**: CI workflow 中 Flyway 下载不再返回 404
**Depends on**: Nothing
**Requirements**: DEPS-02
**Success Criteria**:
  1. CI workflow run 显示 Flyway 下载成功
  2. Flyway migration job 不再因 URL 404 失败
  3. 数据库迁移在 CI 环境正常执行
**Plans**: 1/1

Plans:
- [x] 35-01: 修复 Flyway URL — Update Flyway version 11.3.4 → 10.17.0

**Details:**
- Flyway 从 11.3.4 降级到 10.17.0 解决 CI 下载 404 问题

---

### Phase 36: Achievement 异步化

**Goal**: Achievement 检查不再阻塞主线程
**Depends on**: Nothing
**Requirements**: PITFALL-01
**Success Criteria**:
  1. Achievement 检查在 @Async 线程执行
  2. @EventListener 在 AFTER_COMMIT 阶段触发
  3. 主线程不等待 Achievement 检查完成
**Plans**: 1/1

Plans:
- [x] 36-01: Achievement Async — Create AchievementCheckEvent + AchievementCheckListener with @Async + @TransactionalEventListener(AFTER_COMMIT)

**Details:**
- 新增 `AchievementCheckEvent` record：`userId`, `AchievementType`, `currentValue`
- `AchievementCheckListener` 使用 `@Async` + `@TransactionalEventListener(phase = AFTER_COMMIT)`
- `AchievementTriggerServiceImpl` 的 12 个 trigger 方法改为 `void` 返回 + `@Async` + fire-and-forget

---

### Phase 37: Forum Stats 真实数据

**Goal**: Admin Forum Stats 返回真实统计而非硬编码零值
**Depends on**: Nothing
**Requirements**: BUG-01, PITFALL-02
**Success Criteria**:
  1. Admin dashboard Forum Stats 显示 forum_comments 表真实 count
  2. Admin dashboard Forum Stats 显示 forum_votes 表真实 count
  3. Stats 数据随实际数据变化而更新（非硬编码）
**Plans**: 1/1

Plans:
- [x] 37-01: Fix hardcoded zeros in AdminForumServiceImpl.toAdminVO()

**Details:**
- `AdminForumServiceImpl.toAdminVO()` 中的硬编码 0 替换为真实 DB 查询
- `ForumCommentMapper.count()` 和 `ForumVoteMapper.count()` 替代硬编码值

---

## Milestone Summary

**Key Decisions:**
- springdoc 2.6.0 与 Spring Boot 3.2.5 兼容（DEPS-01）
- Flyway 10.17.0 使用 Redgate 官方 URL 修复 CI 404（DEPS-02）
- Achievement 检查通过 @Async + @TransactionalEventListener(AFTER_COMMIT) 异步化，不阻塞主线程（PITFALL-01）
- Forum stats 通过 DB count 查询替换硬编码零值（BUG-01/PITFALL-02）

**Issues Resolved:**
- Swagger UI 无法访问（springdoc 版本兼容）
- CI Flyway 下载 404（版本降级）
- Achievement 检查阻塞主线程（异步事件处理）
- Forum Stats 返回硬编码零值（真实 DB 查询）

**Issues Deferred:**
- DEPS-03: springdoc 3.x 升级（等待官方兼容版本）
- PERF-01: Achievement N+1 查询优化
- PERF-02: Follow System 索引优化
- MISS-01: 测试覆盖率强制执行
- MISS-02: Rate Limiting 端到端测试

**Technical Debt Incurred:**
- 无新增技术债

---

_For current project status, see .planning/ROADMAP.md_
