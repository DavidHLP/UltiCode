# UltiCode 在线编程平台 — 系统说明文档

> **版本**: v1.0  
> **日期**: 2025年5月  
> **项目**: UltiCode — 在线编程评测系统（类 LeetCode）  

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构](#2-系统架构)
3. [技术选型与开发环境](#3-技术选型与开发环境)
4. [功能模块设计](#4-功能模块设计)
5. [数据库设计](#5-数据库设计)
6. [接口设计](#6-接口设计)
7. [安全设计](#7-安全设计)
8. [部署方案](#8-部署方案)
9. [项目结构](#9-项目结构)
10. [总结](#10-总结)

---

## 1. 系统概述

### 1.1 项目背景

随着互联网技术的飞速发展和计算机编程教育的普及，在线编程评测系统（Online Judge）已成为计算机专业学生、程序员和技术爱好者提升编程能力的重要工具。传统的 LeetCode、Codeforces 等平台虽然功能完善，但在国内部署、定制化需求和数据安全方面存在一定局限性。

**UltiCode** 是一个面向计算机专业毕业设计的全栈在线编程平台，旨在构建一个功能完备、架构清晰、易于扩展的编程练习与评测系统。系统支持题目练习、代码提交与自动评测、竞赛管理、论坛交流、题解分享等核心功能，同时提供管理后台用于内容审核、用户管理和系统监控。

### 1.2 系统目标

- **核心功能**: 提供稳定、高效的在线代码评测服务，支持多种编程语言
- **用户体验**: 打造现代化的 Web 界面，支持响应式设计和暗色主题
- **系统扩展**: 采用模块化架构，便于后续功能扩展和维护
- **教学辅助**: 支持竞赛模式、题单收藏、学习进度追踪等教学场景
- **管理运营**: 提供完善的后台管理系统，支持内容审核和数据分析

### 1.3 系统范围

| 范围项 | 说明 |
|--------|------|
| **用户端 (Console)** | 题目浏览、代码编辑与提交、评测结果查看、竞赛参与、论坛发帖、题单管理 |
| **管理端 (Management)** | 用户管理、题目管理、竞赛管理、审核日志、权限配置、系统监控 |
| **后端服务** | RESTful API 服务、代码评测引擎、消息通知、搜索服务、推荐服务 |
| **基础设施** | MySQL 数据库、Redis 缓存、Docker 容器化部署 |

---

## 2. 系统架构

### 2.1 整体架构

UltiCode 采用 **前后端分离 + 微服务扩展** 的现代化架构设计：

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层                                │
├──────────────────────────┬──────────────────────────────────────┤
│      用户端 (Console)     │         管理端 (Management)           │
│      Vue 3 + Vite         │         Vue 3 + Vite                  │
│      Port: 9002           │         Port: 9003                    │
└──────────┬───────────────┴───────────────┬──────────────────────┘
           │                               │
           └───────────────┬───────────────┘
                           │ HTTPS / HTTP
┌──────────────────────────▼──────────────────────────────────────┐
│                      网关 / 负载均衡层                           │
│              Nginx / Spring Cloud Gateway (预留)                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                      业务服务层 (Backend)                        │
│              Spring Boot 3.2.5 + Java 17                        │
│              Port: 9001                                         │
│  ┌─────────┬─────────┬─────────┬─────────┬──────────────────┐   │
│  │ 用户模块 │ 题目模块 │ 提交模块 │ 竞赛模块 │ 其他 22+ 模块     │   │
│  └─────────┴─────────┴─────────┴─────────┴──────────────────┘   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
┌──────────▼────┐ ┌────────▼──────┐ ┌─────▼────────┐
│   MySQL       │ │    Redis      │ │  MeiliSearch  │
│   主数据库     │ │   缓存/会话   │ │   搜索引擎     │
│   Port: 23306 │ │   Port: 26379 │ │   (可选)      │
└───────────────┘ └───────────────┘ └──────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                   推荐服务 (可选微服务)                           │
│              Dubbo3 + Spark                                     │
│              Port: 9004 / 9005                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 架构特点

| 特点 | 说明 |
|------|------|
| **前后端分离** | 前端 Vue 3 SPA 应用通过 RESTful API 与后端通信，完全解耦 |
| **模块化后端** | 26 个独立模块，职责单一，便于维护和扩展 |
| **分层架构** | Controller → Service → Mapper → Entity，经典三层架构 |
| **统一响应** | 全局 `Result<T>` 响应包装器，标准化 API 返回格式 |
| **缓存加速** | Redis 缓存热点数据，减轻数据库压力 |
| **容器化部署** | Docker Compose 编排所有服务，一键启动 |

---

## 3. 技术选型与开发环境

### 3.1 后端技术栈

| 技术/框架 | 版本 | 用途 |
|-----------|------|------|
| **Spring Boot** | 3.2.5 | 核心应用框架，自动配置、内嵌容器 |
| **Java** | 17 (LTS) | 编程语言，长期支持版本 |
| **MyBatis-Plus** | 3.5.16 | ORM 框架，简化数据库操作 |
| **MySQL** | 8.0+ | 关系型数据库，主数据存储 |
| **Redis** | 7.0+ | 缓存、会话、分布式锁、限流 |
| **Redisson** | 4.3.1 | Redis 高级客户端，分布式锁、限流器 |
| **Spring Security** | 6.x | 安全框架，认证与授权 |
| **JWT (jjwt)** | 0.13.0 | JSON Web Token，无状态认证 |
| **SpringDoc OpenAPI** | 2.6.0 | 自动生成 Swagger API 文档 |
| **Dubbo** | 3.2.14 | RPC 框架，微服务间通信 |
| **MeiliSearch** | 0.20.0 | 轻量级搜索引擎 |
| **Hutool** | 5.8.44 | Java 工具类库 |
| **MapStruct** | 1.6.3 | 对象映射（DTO/Entity 转换） |
| **Lombok** | 1.18.44 | 代码简化（Getter/Setter/Builder） |
| **Flyway** | 10.x | 数据库版本管理 |
| **Testcontainers** | - | 集成测试（MySQL/Redis 容器） |

### 3.2 前端技术栈

| 技术/框架 | 版本 | 用途 |
|-----------|------|------|
| **Vue** | 3.4+ | 渐进式前端框架 |
| **Vite** | 5.x | 构建工具，快速 HMR |
| **TypeScript** | 5.4+ | 类型安全的 JavaScript 超集 |
| **Tailwind CSS** | v4 | 原子化 CSS 框架 |
| **shadcn-vue** | latest | 组件库（基于 Radix Vue） |
| **Radix Vue** | latest | 无头 UI 组件基座 |
| **Pinia** | 2.x | 状态管理（Vue 官方推荐） |
| **Vue Router** | 4.x | 客户端路由 |
| **Axios** | 1.x | HTTP 客户端 |
| **KaTeX** | - | 数学公式渲染 |
| **highlight.js** | - | 代码语法高亮 |
| **Lucide Icons** | - | 图标库 |

### 3.3 开发环境

| 环境项 | 配置 |
|--------|------|
| **操作系统** | Linux / macOS / Windows (WSL2) |
| **JDK** | OpenJDK 17 (Temurin) |
| **Node.js** | 22.x LTS |
| **包管理器** | pnpm 10.x |
| **构建工具** | Maven 3.9+ (后端) / Vite (前端) |
| **数据库** | MySQL 8.0 (Docker) |
| **缓存** | Redis 7 (Docker) |
| **服务发现** | Nacos 2.x (Docker，推荐服务专用) |
| **容器** | Docker 24+ / Docker Compose |
| **进程管理** | PM2 (Node.js) |

### 3.4 端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端服务 | 9001 | Spring Boot 主服务 |
| 用户端前端 | 9002 | Console 开发服务器 |
| 管理端前端 | 9003 | Management 开发服务器 |
| 推荐服务-Provider | 9004 | Dubbo 服务提供者 |
| 推荐服务-Web | 9005 | Dubbo 服务消费者 |
| MySQL | 23306 | 数据库（映射 3306） |
| Redis | 26379 | 缓存（映射 6379） |
| Nacos | 28848 | 服务注册与配置中心 |

---

## 4. 功能模块设计

### 4.1 后端模块概览（26 个模块）

后端采用模块化设计，每个模块独立负责一个业务领域：

| 模块名 | 英文标识 | 核心职责 |
|--------|---------|---------|
| **认证模块** | auth | 用户登录、注册、登出、JWT 颁发 |
| **用户模块** | user | 用户信息、个人资料、学习统计 |
| **题目模块** | problem | 题目 CRUD、难度分级、标签管理 |
| **提交模块** | submission | 代码提交、评测队列、结果查询 |
| **竞赛模块** | contest | 竞赛创建、报名、计时、排名 |
| **论坛模块** | forum | 帖子、评论、点赞、板块管理 |
| **题解模块** | solution | 题解发布、审核、点赞收藏 |
| **通知模块** | notification | 系统通知、站内信、消息推送 |
| **订阅模块** | subscription | 会员订阅、套餐管理、支付对接 |
| **审核模块** | moderation | 内容审核、举报处理、违规记录 |
| **搜索模块** | search | 全文检索、模糊查询、MeiliSearch |
| **成就模块** | achievement | 徽章、积分、排行榜 |
| **国际化模块** | i18n | 多语言支持、翻译管理 |
| **备份模块** | backup | 数据备份、恢复策略 |
| **邮件模块** | email | 邮件发送、模板管理 |
| **监控模块** | monitoring | 系统监控、性能指标、日志分析 |
| **投票模块** | vote | 点赞、踩、评分系统 |
| **管理模块** | admin | 后台管理、系统配置 |
| **收藏模块** | bookmark | 题目收藏、题单收藏 |
| **边缘操作模块** | edgeoperations | CDN 刷新、边缘计算 |
| **权限模块** | permission | RBAC 权限模型、角色管理 |
| **题单模块** | problemlist | 题单创建、分类、学习路径 |
| **队列模块** | queue | 异步任务队列、消息调度 |
| **推荐模块** | recommendation | 个性化推荐、协同过滤 |
| **刷新令牌模块** | refreshtoken | JWT 刷新令牌管理 |
| **WebSocket 模块** | websocket | 实时通信、竞赛实时排名 |

### 4.2 用户端 (Console) 功能

#### 4.2.1 核心功能模块

| 功能模块 | 详细功能点 |
|----------|-----------|
| **用户认证** | 注册/登录/登出、JWT Token 管理、第三方登录（预留） |
| **题目中心** | 题目列表、难度筛选、标签筛选、题目搜索、题目详情 |
| **代码编辑器** | Web 端代码编辑、语法高亮、代码补全、主题切换 |
| **提交评测** | 代码提交、评测状态实时推送、结果详情、运行日志 |
| **竞赛系统** | 竞赛列表、竞赛报名、实时排名、竞赛记录 |
| **论坛交流** | 发帖/回帖、Markdown 支持、@用户、帖子搜索 |
| **题单学习** | 官方题单、自定义题单、学习进度、收藏题单 |
| **个人中心** | 个人资料、提交历史、解题统计、成就徽章 |
| **消息通知** | 系统通知、站内信、消息已读/未读 |

#### 4.2.2 特色功能

| 功能 | 说明 |
|------|------|
| **实时评测** | WebSocket 推送评测状态，无需刷新页面 |
| **暗色主题** | 支持亮色/暗色主题切换，OKLCH 色彩空间 |
| **数学公式** | KaTeX 渲染题目中的数学公式 |
| **响应式设计** | 适配桌面端和移动端 |

### 4.3 管理端 (Management) 功能

| 功能模块 | 详细功能点 |
|----------|-----------|
| **用户管理** | 用户列表、用户详情、封禁/解封、角色分配 |
| **题目管理** | 题目 CRUD、测试用例管理、题目审核 |
| **竞赛管理** | 竞赛创建/编辑、参赛者管理、成绩导出 |
| **内容审核** | 帖子审核、题解审核、评论审核、举报处理 |
| **系统监控** | 在线用户、系统日志、性能指标、异常监控 |
| **权限管理** | 角色管理、菜单权限、API 权限 |
| **数据看板** | 用户统计、提交统计、活跃趋势、数据可视化 |

---

## 5. 数据库设计

### 5.1 数据库选型

- **主数据库**: MySQL 8.0，utf8mb4 字符集，支持中文和 Emoji
- **缓存**: Redis 7，用于会话、热点数据、分布式锁
- **搜索引擎**: MeiliSearch，用于题目和帖子的全文检索

### 5.2 核心数据表

#### 5.2.1 用户相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `users` | 用户基础信息 | id, username, email, password_hash, avatar, role, status, created_at |
| `user_profiles` | 用户详细资料 | user_id, nickname, bio, school, github_url, website |
| `user_stats` | 用户解题统计 | user_id, solved_count, submission_count, rating, streak_days |

#### 5.2.2 题目相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `problems` | 题目主表 | id, title, description, difficulty, time_limit, memory_limit, source |
| `problem_tags` | 题目标签关联 | problem_id, tag_id |
| `tags` | 标签表 | id, name, color, category |
| `test_cases` | 测试用例 | problem_id, input, expected_output, is_sample, order |

#### 5.2.3 提交相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `submissions` | 代码提交记录 | id, user_id, problem_id, code, language, status, runtime, memory, submitted_at |
| `submission_results` | 评测结果详情 | submission_id, test_case_id, status, runtime, memory, message |

#### 5.2.4 竞赛相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `contests` | 竞赛信息 | id, title, description, start_time, end_time, type, status |
| `contest_participants` | 竞赛参与者 | contest_id, user_id, registered_at, rank, score |
| `contest_problems` | 竞赛题目关联 | contest_id, problem_id, order, score |

#### 5.2.5 论坛相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `forum_posts` | 论坛帖子 | id, author_id, title, content, category_id, view_count, created_at |
| `forum_comments` | 评论/回复 | id, post_id, author_id, parent_id, content, created_at |
| `forum_categories` | 帖子分类 | id, name, description, icon, sort_order |

#### 5.2.6 题单相关表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `problem_lists` | 题单信息 | id, title, description, author_id, is_official, created_at |
| `problem_list_items` | 题单题目关联 | list_id, problem_id, order |

#### 5.2.7 其他表

| 表名 | 说明 |
|------|------|
| `solutions` | 题解表 |
| `notifications` | 通知表 |
| `achievements` | 成就/徽章表 |
| `user_achievements` | 用户成就关联 |
| `bookmarks` | 收藏表 |
| `votes` | 投票/点赞表 |
| `audit_logs` | 审计日志表 |
| `refresh_tokens` | JWT 刷新令牌表 |
| `subscriptions` | 订阅套餐表 |
| `user_subscriptions` | 用户订阅记录 |

### 5.3 数据库版本管理

系统使用 **Flyway** 进行数据库版本控制：

- 迁移文件位于 `db-manager/migrations/` 目录
- 命名规范: `V{version}__description.sql`
- 支持回滚和修复操作
- Python CLI 工具 `db-manager` 封装 Flyway 命令

```bash
# 常用命令
db-manager migrate    # 执行待处理的迁移
db-manager info       # 查看迁移状态
db-manager repair     # 修复校验和问题
db-manager validate   # 验证当前状态
```

---

## 6. 接口设计

### 6.1 API 设计规范

系统采用 RESTful API 设计规范：

| 规范项 | 说明 |
|--------|------|
| **通信协议** | HTTP/1.1 (开发) / HTTPS (生产) |
| **数据格式** | JSON |
| **字符编码** | UTF-8 |
| **认证方式** | JWT Token (httpOnly Cookie) + CSRF Token |
| **API 文档** | Swagger UI (/swagger-ui.html) |

### 6.2 统一响应格式

所有 API 响应均使用统一的 `Result<T>` 包装器：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "traceId": "t-1234567890"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | Integer | 业务状态码，0 表示成功 |
| `message` | String | 提示信息 |
| `data` | T | 响应数据，泛型支持 |
| `traceId` | String | 请求追踪 ID，用于日志排查 |

### 6.3 认证机制

#### 6.3.1 JWT Token 认证

| Token 类型 | 存储方式 | 有效期 | 用途 |
|-----------|---------|--------|------|
| **Access Token** | httpOnly Cookie | 15-30 分钟 | 身份认证 |
| **Refresh Token** | httpOnly Cookie | 7-30 天 | 刷新 Access Token |
| **CSRF Token** | localStorage + Header | 每次请求轮换 | 防止跨站请求伪造 |

#### 6.3.2 认证流程

```
1. 用户登录 → 后端验证用户名密码
2. 颁发 Access Token + Refresh Token (httpOnly Cookie)
3. 返回 CSRF Token (响应体)
4. 前端存储 CSRF Token 到 localStorage
5. 后续请求：携带 Cookie + X-CSRF-Token Header
6. 后端验证 JWT + CSRF Token
7. 响应头返回新的 CSRF Token (X-New-CSRF-Token)
8. 前端更新 localStorage 中的 CSRF Token
```

#### 6.3.3 CSRF 防护细节

- CSRF Token 每次请求后轮换
- 旧 Token 保留 5 分钟宽限期（Redis TTL）
- 403 CSRF 错误触发一次自动重试

### 6.4 核心 API 接口

#### 6.4.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/register` | 用户注册 |
| POST | `/auth/login` | 用户登录 |
| POST | `/auth/logout` | 用户登出 |
| POST | `/auth/refresh` | 刷新 Token |
| GET | `/auth/csrf` | 获取 CSRF Token |

#### 6.4.2 题目接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/problems` | 题目列表（分页、筛选） |
| GET | `/problems/{id}` | 题目详情 |
| POST | `/problems` | 创建题目（管理员） |
| PUT | `/problems/{id}` | 更新题目（管理员） |
| DELETE | `/problems/{id}` | 删除题目（管理员） |

#### 6.4.3 提交接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/submissions` | 提交代码 |
| GET | `/submissions/{id}` | 提交详情 |
| GET | `/submissions` | 提交记录列表 |
| GET | `/submissions/{id}/result` | 评测结果 |

#### 6.4.4 竞赛接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/contests` | 竞赛列表 |
| GET | `/contests/{id}` | 竞赛详情 |
| POST | `/contests` | 创建竞赛（管理员） |
| POST | `/contests/{id}/register` | 报名竞赛 |
| GET | `/contests/{id}/rank` | 实时排名 |

#### 6.4.5 论坛接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/forum/posts` | 帖子列表 |
| GET | `/forum/posts/{id}` | 帖子详情 |
| POST | `/forum/posts` | 发布帖子 |
| POST | `/forum/posts/{id}/comments` | 发表评论 |

### 6.5 前端 API 封装

前端使用统一的 API 客户端封装，基于 Axios：

```typescript
// 示例：题目相关 API
export const problemApi = {
  async getList(params?: ProblemQuery): Promise<PageResult<Problem>> {
    return apiGet<PageResult<Problem>>("/problems", params);
  },
  
  async getById(id: string): Promise<ProblemDetail> {
    return apiGet<ProblemDetail>(`/problems/${id}`);
  },
  
  async create(data: CreateProblemDTO): Promise<Problem> {
    return apiPost<Problem>("/problems", data);
  }
};
```

---

## 7. 安全设计

### 7.1 认证与授权

| 安全措施 | 实现方式 |
|---------|---------|
| **密码加密** | BCrypt 哈希存储，带盐值 |
| **JWT 安全** | httpOnly Cookie 存储，防止 XSS 窃取 |
| **Token 刷新** | Refresh Token 轮换机制 |
| **会话管理** | Redis 存储会话状态，支持强制下线 |
| **RBAC 权限** | 基于角色的访问控制，细粒度到 API 级别 |
| **密码重置** | 统一返回时间，防止用户枚举攻击 |

### 7.2 输入安全

| 安全措施 | 实现方式 |
|---------|---------|
| **SQL 注入防护** | MyBatis-Plus 参数化查询，禁止字符串拼接 |
| **XSS 防护** | 输入过滤 + 输出编码（OWASP Encoder） |
| **HTML 过滤** | 富文本内容使用 DOMPurify 过滤危险标签 |
| **参数校验** | Bean Validation（@NotNull, @Size 等） |
| **文件上传** | 类型白名单、大小限制、路径安全校验 |

### 7.3 请求安全

| 安全措施 | 实现方式 |
|---------|---------|
| **CSRF 防护** | Token 机制 + SameSite Cookie |
| **请求去重** | 关键接口（登录、支付）禁止请求去重 |
| **限流** | Redis 令牌桶算法，防止暴力破解和 DDoS |
| **CORS** | 白名单配置，禁止通配符 + 凭据同时使用 |
| **HTTPS** | 生产环境强制 HTTPS |

### 7.4 安全响应

| 措施 | 说明 |
|------|------|
| **错误信息** | 不暴露内部错误详情（SQL 错误、堆栈） |
| **敏感数据** | 日志中不记录密码、Token、个人信息 |
| **审计日志** | 记录所有管理操作，支持追溯 |

---

## 8. 部署方案

### 8.1 环境依赖

| 依赖项 | 版本 | 说明 |
|--------|------|------|
| Docker | 24+ | 容器运行时 |
| Docker Compose | 2.x | 容器编排 |
| JDK | 17 | Java 运行环境 |
| Node.js | 22.x | 前端构建 |
| pnpm | 10.x | Node 包管理 |
| Maven | 3.9+ | Java 构建 |
| PM2 | latest | Node 进程管理 |

### 8.2 Docker 服务编排

系统使用 Docker Compose 编排基础设施服务：

```yaml
# docker-compose.yml 核心服务
services:
  mysql:
    image: mysql:8.0
    ports:
      - "23306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=ulticode
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  redis:
    image: redis:7-alpine
    ports:
      - "26379:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data

  nacos:
    image: nacos/nacos-server:v2.x
    ports:
      - "28848:8848"
    environment:
      - MODE=standalone
```

### 8.3 部署流程

#### 8.3.1 开发环境部署

```bash
# 1. 克隆代码并进入目录
git clone <repository-url>
cd UltiCode-Public-Next

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填写数据库密码、JWT 密钥等

# 3. 启动基础设施（MySQL、Redis、Nacos）
docker compose up -d

# 4. 安装前端依赖
pnpm install

# 5. 数据库迁移
cd db-manager
.venv/bin/python -m db_manager.cli migrate

# 6. 使用 PM2 启动所有服务
pm2 start ecosystem.config.cjs

# 7. 查看服务状态
pm2 status
```

#### 8.3.2 PM2 服务配置

```javascript
// ecosystem.config.cjs
module.exports = {
  apps: [
    {
      name: 'ulticode-9001',
      cwd: './backend-spring',
      script: './mvnw',
      args: 'spring-boot:run',
      env: { PORT: 9001 }
    },
    {
      name: 'ulticode-9002',
      cwd: './console',
      script: 'pnpm',
      args: 'run dev',
      env: { PORT: 9002 }
    },
    {
      name: 'ulticode-9003',
      cwd: './management',
      script: 'pnpm',
      args: 'run dev',
      env: { PORT: 9003 }
    }
  ]
};
```

#### 8.3.3 生产环境部署

```bash
# 1. 构建前端
 cd console && pnpm build
cd management && pnpm build

# 2. 构建后端
cd backend-spring && ./mvnw package -DskipTests

# 3. 使用生产配置启动
docker compose -f docker-compose.prod.yml up -d
```

### 8.4 环境变量配置

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `DB_HOST` | 数据库主机 | localhost |
| `DB_PORT` | 数据库端口 | 23306 |
| `DB_USER` | 数据库用户 | ulticode |
| `DB_PASSWORD` | 数据库密码 | ulticode123 |
| `DB_NAME` | 数据库名称 | ulticode |
| `REDIS_HOST` | Redis 主机 | localhost |
| `REDIS_PORT` | Redis 端口 | 26379 |
| `REDIS_PASSWORD` | Redis 密码 | redis123 |
| `JWT_SECRET` | JWT 密钥 | min-32-characters-secret-key |
| `NACOS_PORT` | Nacos 端口 | 28848 |
| `VITE_API_BASE_URL` | 前端 API 地址 | http://localhost:9001 |

---

## 9. 项目结构

### 9.1 根目录结构

```
UltiCode-Public-Next/
├── backend-spring/          # 后端 Spring Boot 项目
│   ├── src/main/java/com/ulticode/
│   │   └── [26 个模块目录]
│   ├── src/main/resources/
│   ├── pom.xml
│   └── ecosystem.config.cjs
├── console/                 # 用户端前端 (Vue 3)
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── vite.config.ts
├── management/              # 管理端前端 (Vue 3)
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── vite.config.ts
├── shared/                  # 共享代码
│   ├── auth-core/           # 认证核心库
│   └── types/               # 共享类型定义
├── db-manager/              # 数据库迁移工具 (Python)
│   ├── migrations/          # Flyway 迁移文件
│   ├── src/
│   └── .venv/
├── recommendation/          # 推荐服务 (Dubbo + Spark)
│   ├── recommend-api/
│   ├── recommend-provider/
│   └── recommend-web/
├── docker-compose.yml       # Docker 编排配置
├── docker-compose.prod.yml  # 生产环境配置
├── ecosystem.config.cjs     # PM2 进程配置
├── .env                     # 环境变量
├── .env.example             # 环境变量示例
└── README.md                # 项目说明
```

### 9.2 后端模块结构

每个后端模块遵循统一的分层结构：

```
[module-name]/
├── controller/          # 控制器层，处理 HTTP 请求
│   └── XxxController.java
├── service/             # 业务逻辑层
│   ├── XxxService.java
│   └── impl/
│       └── XxxServiceImpl.java
├── entity/              # 数据实体
│   └── Xxx.java
├── mapper/              # MyBatis-Plus 数据访问层
│   └── XxxMapper.java
├── dto/                 # 数据传输对象
│   ├── XxxDTO.java
│   └── XxxVO.java
└── config/              # 模块配置（如有）
```

### 9.3 前端项目结构

```
src/
├── api/                 # API 接口封装
├── assets/              # 静态资源
├── components/          # 公共组件
│   ├── ui/             # 基础 UI 组件
│   └── [feature]/      # 业务组件
├── composables/         # 组合式函数
├── layouts/             # 页面布局
├── router/              # 路由配置
├── stores/              # Pinia 状态管理
├── styles/              # 全局样式
├── utils/               # 工具函数
│   ├── request.ts      # Axios 封装
│   └── sanitize.ts     # XSS 过滤
├── views/               # 页面视图
└── App.vue
```

---

## 10. 总结

### 10.1 系统特点

UltiCode 在线编程平台具有以下核心特点：

| 特点 | 说明 |
|------|------|
| **技术先进** | 采用 Spring Boot 3、Vue 3、Java 17 等主流技术栈 |
| **架构清晰** | 前后端分离，模块化设计，职责单一 |
| **功能完备** | 覆盖题目练习、代码评测、竞赛、论坛、管理后台等完整功能 |
| **安全可靠** | JWT + CSRF 双重认证，XSS/SQL 注入防护，RBAC 权限控制 |
| **易于部署** | Docker Compose 一键启动，PM2 进程管理，CI/CD 支持 |
| **可扩展** | 微服务架构预留，Dubbo RPC 支持，模块化易于新增功能 |

### 10.2 技术亮点

1. **现代化前端**: Vue 3 Composition API + TypeScript + Tailwind CSS v4，类型安全、开发效率高
2. **统一响应格式**: 全局 `Result<T>` 包装器，简化前后端交互
3. **CSRF Token 轮换**: 每次请求后更新 Token，5 分钟宽限期保证并发安全
4. **数据库版本管理**: Flyway 自动化迁移，支持多环境部署
5. **代码评测系统**: 支持多语言、实时推送、详细结果展示
6. **搜索服务集成**: MeiliSearch 提供高性能全文检索

### 10.3 适用场景

本系统适用于：
- 计算机专业毕业设计项目
- 高校在线编程教学平台
- 企业技术面试题库系统
- 编程社区和算法竞赛平台

### 10.4 后续优化方向

| 方向 | 说明 |
|------|------|
| **性能优化** | 引入缓存策略、CDN 加速、数据库读写分离 |
| **评测引擎** | 支持更多编程语言，优化沙箱安全性 |
| **AI 辅助** | 集成代码纠错、智能提示、难度推荐 |
| **移动端** | 开发小程序或 App，支持移动端刷题 |
| **国际化** | 完善多语言支持，支持英文界面 |

---

> **文档结束**  
> 如有疑问，请参考项目 `README.md` 或联系开发团队。
