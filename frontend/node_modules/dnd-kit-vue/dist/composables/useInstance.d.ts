import { DragDropManager } from '@dnd-kit/dom';
import { CleanupFunction } from '@dnd-kit/state';
import { ShallowRef } from 'vue';
export interface Instance<T extends DragDropManager<any, any> = DragDropManager<any, any>> {
    manager: T | undefined;
    register: () => CleanupFunction | void;
}
export declare function useInstance<T extends Instance>(initializer: (manager: DragDropManager<any, any> | undefined) => T): ShallowRef<T>;
